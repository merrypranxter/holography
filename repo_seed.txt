Hell yes. Do **not** make this repo as “physics notes.” Make it as a **translation engine** from holography math → programmable visual logic → shader/raymarch output.

I set up the repo blueprint in the canvas already. Here’s the first real chunk I’d put in the repo: the structure, the encoding philosophy, and the first core doc you want your app/API to understand.

---

# Repo architecture

```text
holography-shader-lab/
  README.md
  docs/
    00_repo_vision.md
    01_holographic_principle.md
    02_ads_geometry_and_scale.md
    03_bulk_boundary_dictionary.md
    04_entanglement_geometry.md
    05_holographic_renormalization.md
    06_black_holes_and_horizons.md
    07_light_sheets_and_screens.md
    08_nonlocality_and_smearing.md
    09_cosmology_and_holographic_matter.md
    10_shader_translation_guide.md
    11_visual_motif_library.md
    12_glossary.md
  theory/
    equations.json
    concepts.json
    visual_mappings.json
    presets.json
  src/
    js/
    glsl/
  examples/
  research/
```

---

# What this repo should actually do

Your app should not try to “simulate string theory.”
It should expose **visual primitives implied by holography**:

* **area controls information**
* **boundary generates bulk**
* **radial depth = scale / resolution**
* **light propagates along null structures**
* **entanglement appears as minimal bridges / shared geometry**
* **nonlocality means distant surface patterns co-control interior form**

That is the usable core.

The source material supports exactly that framing: black-hole entropy scales with **horizon area**, not volume; the covariant entropy bound uses **light-sheets** with nonpositive expansion; AdS/CFT maps **boundary data to bulk fields**; and holographic entanglement entropy is computed by **minimal-area bulk surfaces**.    

---

# `README.md` draft

```md
# Holography Shader Lab

A research-and-implementation repo for translating holography mathematics into procedural visual systems for shader art, raymarching, GLSL, and JavaScript.

## Goal
Build computational analogs of holographic principles:
- surface-area encoded complexity
- boundary-to-bulk generation
- AdS radial depth / UV-IR warping
- light-sheet scanning geometries
- entanglement bridges and minimal surfaces
- nonlocal reconstruction fields

## This repo is not
A literal quantum gravity simulator.

## This repo is
A mathematically-informed visual engine that turns holographic ideas into:
- shader parameter systems
- SDF/raymarch spaces
- post-processing motifs
- API-ready concept schemas
- prompt/control vocabularies for generative art systems

## Core rule
When in doubt, convert the physics idea into:
1. geometry
2. field behavior
3. information-density rule
4. rendering consequence
5. controllable parameters
```

---

# `docs/01_holographic_principle.md`

```md
# 01 — The Holographic Principle

## 1.1 Core statement

The holographic principle says that the amount of information needed to describe a region is bounded by its boundary area, not by its volume.

In gravity, this shows up first in black holes:
S_BH = A / (4 G_N ħ)

This means black-hole entropy scales with event-horizon area.

In general spacetimes, the covariant entropy bound says:
S[L(B)] ≤ A(B) / 4

where L(B) is a light-sheet generated by null rays orthogonal to a codimension-2 surface B, chosen so that their expansion is non-positive.

## 1.2 Why it matters for visuals

This completely changes how “complexity” should be rendered.

A naive graphics intuition:
- more volume = more detail

A holographic intuition:
- more exposed boundary = more possible detail
- surface patterns generate inner structure
- interiors are projections, reconstructions, or extrapolations of boundary code

## 1.3 Visual laws to extract

### Law A — Surface controls interior
A surface texture, symbol map, or signal lattice should define the internal volume field.

### Law B — Area controls detail budget
The amount of visible microstructure should depend on visible area or effective screen area.

### Law C — Projection follows null/light directions
Interior structure should often emerge by inward projection, scanlines, radial beams, or caustic envelopes rather than uniform fill.

### Law D — Volume can be hollow
The bulk may look sparse, ghosted, translucent, or reconstructed from shell emissions.

## 1.4 Coding abstraction

Represent any holographic object as:

boundary_data → projection_operator → bulk_field → render_response

where:

- boundary_data = texture/symbol/noise/graph on a 2D manifold
- projection_operator = ray bundle / diffusion / smearing / inverse-distance warp
- bulk_field = scalar or vector field in 3D
- render_response = glow, density, normals, emission, distortions

## 1.5 Minimal parameter vocabulary

- boundaryDensity
- areaBudget
- bitDepth
- shellBrightness
- projectionSharpness
- nullDirectionality
- interiorOpacity
- entropyGlow
- reconstructionNoise
- causticCompression

## 1.6 First shader behaviors

1. Surface-encoded volume
2. Inward light-sheet scanning
3. Area-limited detail
4. Hollow bulk / glowing shell
5. Boundary-controlled color palette
```

---

# Mathematical spine for this first module

The essential equations I’d keep in `01_holographic_principle.md` are:

[
S_{BH} = \frac{A}{4 G_N \hbar}
]

[
S[L(B)] \le \frac{A(B)}{4}
]

[
N_{\text{dof}} \lesssim \frac{A}{4}
]

The first is the Bekenstein–Hawking area law. The second is Bousso’s covariant entropy bound on light-sheets. The third is the practical rendering interpretation: the available degrees of freedom scale like area, not volume.  

One of the most useful clarifications from the sources is that holography is **not** generally a simple spacelike “flattening” of a 3D region onto a 2D shell. The covariant formulation is **null**: it projects along converging light-like directions. That is gold for shader design because it tells you to think in terms of **ray bundles, scanfronts, shells, caustics, and inward beam reconstruction**, not just “put texture on the surface.” 

---

# How to code this idea

## 1. Boundary-driven volume field

Instead of filling space with independent noise, derive the bulk from a surface signal.

### Concept

For a point `p` in space, compute nearest surface point `s`, sample boundary texture there, and extend that value inward with a falloff and directional warp.

### JS pseudocode

```js
function holographicBulkValue(p, surfaceSampler, sdfSurface) {
  const d = sdfSurface(p);               // signed distance to shell
  const s = projectToSurface(p);         // nearest or directional shell point
  const b = surfaceSampler(s.uv);        // boundary data
  const inward = Math.exp(-Math.abs(d) * 6.0);
  const shellBias = smoothstep(0.12, 0.0, Math.abs(d));
  return b * inward + shellBias * 0.5;
}
```

### Why this is faithful

It enforces “interior derives from boundary,” not “interior is independently authored.”

---

## 2. Area-limited detail budget

Tie spatial detail to effective exposed area.

### Principle

More area = more degrees of freedom = more microstructure.

### Simple model

```js
function detailBudget(area, k = 1.0) {
  return k * Math.sqrt(Math.max(area, 1e-4));
}
```

Use that to control:

* octaves of noise
* number of glyph layers
* number of raymarch secondary samples
* symbol population density
* frequency cap

This is not literal Planck-scale physics. It is the right **rendering analog** of the area law.

---

## 3. Light-sheet renderer

The sources say the useful hypersurfaces are generated by orthogonal null rays with nonpositive expansion. In art code, that becomes **converging directed beams from a boundary surface**. 

### GLSL sketch

```glsl
float lightSheetField(vec3 p, vec3 center, float radius) {
    vec3 dir = normalize(center - p);
    float r = length(p - center);

    // shell proximity
    float shell = exp(-abs(r - radius) * 40.0);

    // inward convergence
    float inward = smoothstep(radius, radius * 0.35, r);

    // beam modulation
    float beams = 0.5 + 0.5 * cos(24.0 * atan(p.y, p.x) + 8.0 * p.z);

    return shell * inward * beams;
}
```

### Visual result

* shell emits structured beams inward
* interior appears reconstructed by scanning
* bulk is a consequence of boundary emission

That is much more holographic than ordinary volumetric fog.

---

## 4. Hollow bulk + emissive shell

The black-hole / entropy logic strongly suggests a useful aesthetic rule:

* **shell = information-dense**
* **interior = sparse, encoded, reconstructed, or partially unreal**

That’s not just style. It is the correct visual translation of area-law dominance. 

### Raymarch rule

Weight emission toward thin shells and suppress homogeneous volume fill.

```glsl
float shellWeight(float sd) {
    return exp(-abs(sd) * 32.0);
}

float interiorWeight(float sd) {
    return exp(-abs(sd) * 4.0) * 0.2;
}
```

---

# First JSON schema for the API

`theory/concepts.json`

```json
[
  {
    "id": "holographic_principle",
    "name": "Holographic Principle",
    "physics_meaning": "Information content of a region is bounded by boundary area rather than volume.",
    "math_forms": [
      "S_BH = A / (4 G_N ħ)",
      "S[L(B)] <= A(B) / 4"
    ],
    "visual_translation": [
      "surface encodes volume",
      "hollow interior",
      "glowing shell",
      "inward light-sheet projection",
      "area-limited detail budget"
    ],
    "shader_parameters": [
      "boundaryDensity",
      "areaBudget",
      "projectionSharpness",
      "nullDirectionality",
      "entropyGlow",
      "interiorOpacity"
    ],
    "rendering_strategies": [
      "surface-sampled volumetric field",
      "shell-weighted emission",
      "ray-bundle reconstruction",
      "boundary-controlled palette"
    ],
    "failure_modes": [
      "uniform detail everywhere",
      "volume-defined complexity without surface relation",
      "opaque solid interiors with no boundary causation"
    ]
  }
]
```

---

# The first example scene I would build

## `examples/01_surface_encodes_volume`

**Scene brief:**
A dark sphere or humanoid SDF whose shell is tiled with animated glyph cells. Each cell emits converging light sheets inward. The interior is mostly empty except where overlapping projections reconstruct temporary organs, masks, lattices, or ghost topology.

### Scene logic

* boundary UV map drives all color and structure
* detail count depends on shell area
* interior structures only exist where multiple projected sheets intersect
* camera motion causes parallax in the projected bulk
* bloom favors shell edges and caustic knots

### Parameters

```js
{
  shellRadius: 1.0,
  areaBudget: 2.5,
  projectionSharpness: 7.0,
  nullDirectionality: 0.9,
  entropyGlow: 1.4,
  interiorOpacity: 0.18,
  glyphDensity: 18.0
}
```

---

# The one conceptual mistake to avoid

Do **not** encode holography as “2D sticker wrapped around a 3D object.”

That is too flat.

The sources make the stronger claim: information is bounded by area, and the covariant formulation works through **light-sheets** and boundary/bulk encoding, not just a decorative shell. In code, that means the shell must be **causal**, **generative**, and **structurally upstream** of the interior.  

---

# What I would do next in the repo

The next file should be:

`docs/02_ads_geometry_and_scale.md`

That one will give you the most usable second layer for shader art:

* AdS metric as a warp field
* radial coordinate as scale/depth
* UV/IR mapping
* recursive zoom tunnels
* raymarching in warped spaces
* how to fake AdS geometry without full GR

Say **next** and I’ll write that file directly, with the equations, code strategy, GLSL structures, and API schema additions.

Good. Here is the next repo file.

# `docs/02_ads_geometry_and_scale.md`

```md
# 02 — AdS Geometry and Scale

## 2.1 Core idea

Anti-de Sitter space (AdS) is the canonical holographic bulk geometry.
Its extra radial direction is not just “another axis” — it encodes scale.
Near the boundary, you are in the ultraviolet: fine detail, short-distance structure, source data.
Deeper into the bulk, you move toward infrared structure: coarse behavior, emergent organization, larger-scale response.

For shader art, this is one of the most useful ideas in the entire repo:

radial depth = scale
boundary proximity = high-frequency control
bulk depth = low-frequency emergence

This is the engine for recursive zoom worlds, cathedral-like tunnels, warped raymarch depth, and scale-aware procedural structure.

## 2.2 Basic AdS metrics

### Global AdS

A standard global form is

ds^2 = (L^2 / cos^2 rho) [ -d tau^2 + d rho^2 + sin^2 rho dOmega^2_(d-1) ]

with
0 <= rho < pi/2

The boundary is at rho -> pi/2.

### Static radial form

Another useful form is

ds^2 = -(1 + r^2 / L^2) d tau^2 + dr^2 / (1 + r^2 / L^2) + r^2 dOmega^2_(d-1)

with boundary at r -> infinity.

### Poincare patch / near-boundary workhorse

The most programmable form is

ds^2 = (L^2 / z^2) [ dz^2 + dx_mu dx^mu ]

or in Euclidean signature

ds^2 = (L^2 / z^2) [ dz^2 + dx^2 ]

Boundary:
z -> 0

Deep bulk:
larger z

This is the version you want for most shader logic.
It is conformally flat, but with a diverging scale factor L^2 / z^2.
So the same coordinate step means something wildly different depending on depth.

## 2.3 The real visual gold: scale as geometry

The rescaling symmetry of the Poincare patch is

(t, x, z) -> (lambda t, lambda x, lambda z)

So scaling x and scaling z go together.
That is the origin of the famous “scale/radius” intuition:
smaller boundary scales are associated with near-boundary structure;
larger scales correlate with deeper bulk organization.

For art, that gives you a powerful rule:

- high-frequency glyphs should live near z ~ 0
- broad smooth fields should dominate at large z
- moving radially should feel like changing observational scale, not just moving through space

## 2.4 Important warning

Do not turn “scale/radius” into a fake absolute law.

The source material makes this subtle:
the rescaling isometry strongly motivates scale/radius,
but it is not generally valid as a full dynamical statement for every bulk process.
It is useful as a design principle and local heuristic, not a universal truth-condition for your renderer.

So in the repo:
- use scale/radius aesthetically and structurally
- do not oversell it as an exact theorem for every scene

## 2.5 UV / IR

A central AdS/CFT lesson:

bulk IR divergences near the boundary correspond to UV divergences in the boundary theory.

That means:
- near the boundary, the bulk metric blows up
- naive quantities diverge there
- near-boundary structure is where source data and renormalization live

For rendering, reinterpret this as:

- near-boundary regions are detail-heavy and unstable
- deep bulk regions are smoother and more collective
- a radial cutoff is not just clipping geometry; it is choosing a resolution regime

So your engine should support:
- boundary cutoff
- radial fade
- scale-aware sampling
- depth-dependent frequency filtering

## 2.6 Fefferman–Graham coordinates

For asymptotically AdS spaces, a standard near-boundary form is

ds^2 = d rho^2 / (4 rho^2) + (1/rho) g_ij(x, rho) dx^i dx^j

with expansion

g(x, rho) = g_(0) + rho g_(2) + ... + rho^(d/2) g_(d) + rho^(d/2) log(rho) h_(d) + ...

Interpretation:
- g_(0): boundary geometry / source data
- subleading terms: bulk response
- deeper coefficients encode expectation values, anomalies, and state data

For code, this is insanely useful:
treat bulk appearance as an expansion from boundary data, not as independent interior paint.

## 2.7 Coding translation

### Rule A — Radial coordinate is a control channel
Every field should be allowed to vary with z or rho.

### Rule B — Frequency should redshift with depth
As z increases, suppress high frequencies and widen structures.

### Rule C — Boundary conditions matter
The look of the deep bulk should inherit from near-boundary source maps.

### Rule D — Cutoffs are meaningful
A z_min or rho_max cutoff is not arbitrary; it chooses what level of detail / source access is allowed.

## 2.8 Shader abstractions

Represent AdS-inspired space as

world_point p
-> warp into ads coordinates
-> evaluate depth z
-> modulate scale/frequency/opacity by z
-> raymarch in warped metric or metric-inspired fake distance

There are two levels of implementation:

### Cheap implementation
Fake AdS with a radial warp field.
Fast, art-friendly, stable.

### Serious implementation
Modify ray step size, normals, and sampling density using the AdS conformal factor.
Better for convincing depth logic.

## 2.9 Practical fake AdS warp

Let y be your “radial” or “holographic depth” coordinate.
Define

z = max(epsilon, depthMap(p))

Then use

warpScale = L / z

to control:
- local frequency
- symbol density
- glow sharpness
- ray step size
- refractive intensity

Near boundary:
z small -> warpScale huge

Deeper bulk:
z larger -> warpScale softer

## 2.10 Visual motifs from AdS

1. Infinite zoom cathedrals
2. Boundary-bright tunnels fading into low-frequency interior fog
3. Nested shells whose apparent spacing changes with radial depth
4. Coordinate grids that stretch hyperbolically near edges
5. Objects whose texture resolution depends on how close they are to the boundary
6. Scale recursion: one motif repeated at larger and larger wavelengths as depth increases

## 2.11 What to avoid

- uniform frequency across all depth
- Euclidean tunnel with no scale logic
- “deep bulk” that is more high-frequency than the boundary unless deliberately inverted
- radial motion that feels like plain translation instead of change of descriptive scale
```

That’s the doc core. Now the code side.

## Equations to keep in `theory/equations.json`

```json
[
  {
    "id": "ads_global_metric",
    "equation": "ds^2 = (L^2 / cos^2 rho) [ -d tau^2 + d rho^2 + sin^2 rho dOmega^2_(d-1) ]",
    "meaning": "Global AdS metric with boundary at rho = pi/2"
  },
  {
    "id": "ads_static_metric",
    "equation": "ds^2 = -(1 + r^2/L^2) d tau^2 + dr^2/(1 + r^2/L^2) + r^2 dOmega^2_(d-1)",
    "meaning": "Static radial AdS form with boundary at r -> infinity"
  },
  {
    "id": "ads_poincare_metric",
    "equation": "ds^2 = (L^2 / z^2) (dz^2 + dx_mu dx^mu)",
    "meaning": "Poincare patch; z is holographic depth"
  },
  {
    "id": "ads_scaling_symmetry",
    "equation": "(t, x, z) -> (lambda t, lambda x, lambda z)",
    "meaning": "Scale/radius motivation"
  },
  {
    "id": "fg_metric",
    "equation": "ds^2 = d rho^2/(4 rho^2) + (1/rho) g_ij(x,rho) dx^i dx^j",
    "meaning": "Fefferman-Graham near-boundary form"
  }
]
```

The global metric, static radial metric, Poincaré form, and Fefferman–Graham near-boundary expansion are all explicitly supported by the uploaded sources. Skenderis gives the asymptotically AdS and Fefferman–Graham forms, including the near-boundary expansion and the fact that the boundary data sit in the leading term. Rosenhaus discusses UV/IR and the scale/radius relation, including the warning that scale/radius is not generally valid as a full dynamical rule.     

---

# How I would code this

## 1. AdS-inspired coordinate warp in GLSL

This is the cheap, stable version.

```glsl
float adsDepth(vec3 p) {
    // choose one axis or a derived coordinate as holographic depth
    return max(0.02, p.y + 1.2);
}

vec3 adsWarp(vec3 p, float L) {
    float z = adsDepth(p);
    float s = L / z;
    return vec3(p.x * s, z, p.z * s);
}
```

This does not simulate geodesics in AdS. It gives you the right artistic behavior:
near the boundary, coordinates stretch brutally; deeper in, they relax.

## 2. Depth-dependent frequency redshift

```glsl
float adsFrequency(float z, float baseFreq, float L) {
    return baseFreq * (z / L);
}
```

Then:

* near boundary `z` small → tiny features in coordinate space, visually dense after warp
* deep bulk `z` larger → broader structures

A more art-directable version:

```glsl
float depthBand(float z, float a, float b) {
    return smoothstep(a, b, z);
}

float layeredFreq(float z) {
    return mix(18.0, 2.5, smoothstep(0.05, 2.0, z));
}
```

## 3. Boundary-sourced field expansion

Use a near-boundary expansion logic inspired by Fefferman–Graham:

```glsl
float sourceField(vec2 uv);
float responseField(vec3 p);

float bulkField(vec3 p) {
    float z = adsDepth(p);
    vec2 uv = p.xz;

    float g0 = sourceField(uv);              // boundary/source term
    float g2 = responseField(p);             // subleading response
    float logTerm = log(1.0 + 1.0 / z);

    return g0 + z * g2 + 0.08 * logTerm * g0;
}
```

That tracks the real philosophy:
bulk = source + radial response, not random interior noise.  

## 4. Raymarching with AdS-aware step control

One of the strongest visual upgrades is to vary step size with depth.

```glsl
float stepScaleFromDepth(float z, float L) {
    return clamp(0.15 * z / L, 0.01, 0.3);
}
```

In the march loop:

```glsl
float t = 0.0;
for (int i = 0; i < MAX_STEPS; i++) {
    vec3 p = ro + rd * t;
    float z = adsDepth(p);
    float d = sceneSDF(adsWarp(p, 1.0));
    t += d * stepScaleFromDepth(z, 1.0);
}
```

That makes near-boundary zones effectively harder, denser, more resolution-sensitive.

## 5. Hyperbolic grid motif

AdS wants coordinate lines that look compressed and recursively stretched.

```glsl
float adsGrid(vec3 p) {
    float z = adsDepth(p);
    float s = 1.0 / z;
    vec2 q = p.xz * s;

    vec2 g = abs(fract(q) - 0.5);
    float line = min(g.x, g.y);
    return smoothstep(0.08, 0.0, line);
}
```

Use it sparingly as a scaffold, not wallpaper.

---

# JS parameter system for this module

```js
export const adsParams = {
  adsRadius: 1.0,
  boundaryDepth: 0.03,
  deepBulkDepth: 2.5,
  warpIntensity: 1.0,
  radialFrequencyBias: 0.85,
  boundaryGlow: 1.4,
  bulkSoftness: 0.6,
  uvIrMix: 0.75,
  cutoffFeather: 0.2,
  renormFade: 0.4
};
```

Meanings:

* `boundaryDepth`: how close the renderer lets you get to the UV edge
* `warpIntensity`: how hard the metric-like scaling bends coordinates
* `uvIrMix`: how strongly fine detail fades into coarse bulk modes
* `renormFade`: how aggressively divergences are visually tamed

---

# `theory/concepts.json` addition

```json
{
  "id": "ads_geometry_and_scale",
  "name": "AdS Geometry and Scale",
  "physics_meaning": "The radial direction of AdS organizes scale; near-boundary behavior corresponds to UV data, while deeper bulk encodes larger-scale structure.",
  "math_forms": [
    "ds^2 = (L^2 / z^2)(dz^2 + dx_mu dx^mu)",
    "(t,x,z) -> (lambda t, lambda x, lambda z)",
    "ds^2 = d rho^2/(4 rho^2) + (1/rho) g_ij(x,rho) dx^i dx^j"
  ],
  "visual_translation": [
    "recursive zoom depth",
    "boundary-bright fine structure",
    "deeper bulk coarse fields",
    "hyperbolic grid stretch",
    "radial detail filtering"
  ],
  "shader_parameters": [
    "adsRadius",
    "boundaryDepth",
    "warpIntensity",
    "radialFrequencyBias",
    "uvIrMix",
    "renormFade"
  ],
  "rendering_strategies": [
    "AdS-inspired coordinate warp",
    "depth-dependent frequency control",
    "boundary-sourced field expansion",
    "AdS-aware ray step scaling"
  ],
  "failure_modes": [
    "uniform detail with depth",
    "no special role for boundary",
    "treating scale/radius as an exact law in all scenes"
  ]
}
```

---

# Example scene I would build next from this

## `examples/02_ads_zoom_tunnel`

A raymarched tunnel with a bright mathematically ornamented boundary lip and a deep interior that unfolds into larger and smoother recursive motifs.

### Scene law

* Near boundary: thin glyph lattices, sharp caustics, unstable fine detail
* Mid depth: braided geometric scaffolds
* Deep bulk: broad luminous manifolds and slower curvature fields
* Camera motion inward feels like “changing the scale of description,” not simply moving through a hallway

### Visual trick

Make motifs recur at different depth bands with different spatial frequencies, so the scene feels like a renormalization flow.

---

# One subtle repo note you absolutely want

There are really **three** different but related ideas here:

1. **AdS metric geometry**
2. **UV/IR relation**
3. **scale/radius intuition**

Do not collapse them into one slogan.

The sources support all three, but they are not identical. UV/IR is the statement that bulk IR and boundary UV cutoffs correspond. Scale/radius is a useful but limited intuition from AdS rescaling symmetry. Holographic renormalization says near-boundary analysis controls divergences and source structure. Keeping those distinct will make your repo feel much smarter and prevent fake-physics shader mush.   

---

# What I would do in codebase terms

After this doc, I would add:

* `src/glsl/fields/uv_ir_field.glsl`
* `src/glsl/sdf/ads_spaces.glsl`
* `src/js/radialFlows.js`

And the next theory doc should be:

`docs/03_bulk_boundary_dictionary.md`

That one is where the repo becomes really dangerous in a good way:

* bulk fields from boundary operators
* source vs response
* smearing
* nonlocal reconstruction
* how to fake operator insertion visually
* how to make a boundary texture “summon” a 3D event

Say **next** and I’ll write that file.

Perfect. Now we hit the **bulk/boundary dictionary**—the part that teaches the repo how a boundary texture, field, symbol system, or operator map can actually **generate** interior events instead of just decorating them.

# `docs/03_bulk_boundary_dictionary.md`

```md id="fng7l9"
# 03 — Bulk / Boundary Dictionary

## 3.1 Core idea

In holography, the bulk is not independently authored.
Bulk fields are encoded by boundary data.

At the simplest level, a bulk field near the boundary determines a boundary operator.
More ambitiously, a bulk operator at finite depth can sometimes be reconstructed by smearing boundary operators over a region of space and time.

For shader art, this becomes the core computational law:

boundary signal -> reconstruction kernel -> bulk event

Do not think "texture wrapping."
Think:
- source inscription
- nonlocal projection
- interior emergence
- delayed or smeared causation
- partial reconstruction when visibility is obstructed

## 3.2 The extrapolate dictionary

A canonical leading statement is:

lim_{z -> 0} z^{-Delta} Phi(x,t,z) = O(x,t)

This says:
- Phi is a bulk field
- O is the dual local boundary operator
- Delta is the scaling dimension

So the near-boundary tail of the bulk field is what the boundary theory reads as the operator.

In repo language:
- the boundary is not just where the object ends
- it is where the bulk field exposes its readable code

## 3.3 Smearing: finite-depth bulk from boundary data

In favorable cases, the bulk operator can be reconstructed as:

Phi(B) = ∫ db' K(B|b') O(b') + ...

where:
- B is the bulk point
- b' are boundary points
- K is the smearing kernel
- the ellipsis means interactions / nonlinear corrections

This is the useful programmable version of holography.

The kernel K tells you:
- which boundary region contributes to a bulk point
- how strongly
- with what temporal lag / spatial spread
- whether the reconstruction is local-ish or violently nonlocal

## 3.4 Source versus response

Holographic renormalization makes the boundary/bulk relation more precise.

Near the boundary, bulk fields admit asymptotic expansions of the form:

F(x, rho) = rho^m [ f_(0)(x) + f_(2)(x) rho + ... + rho^n ( f_(2n)(x) + log(rho) f~_(2n)(x) ) + ... ]

The leading coefficient f_(0) is interpreted as the source for the dual boundary operator.
Subleading coefficients are fixed recursively by the bulk equations up to a certain order, while a deeper coefficient remains state-dependent.

In repo language:
- source = what you write on the boundary
- response = what the bulk gives back
- not every interior feature is directly painted; some are dynamically induced

## 3.5 Partition function view

The source/operator relation can be stated as:

Z_bulk[phi_(0)] = < exp( - ∫ boundary phi_(0) O ) >_QFT

This means the boundary values of bulk fields act as sources coupled to operators in the boundary theory.

Visual translation:
- boundary maps are not passive textures
- they are control voltages
- they inject deformations into the whole interior logic

## 3.6 Local operators are sometimes enough — and sometimes not

In global AdS, local boundary operators can be sufficient to reconstruct the bulk field in the classical limit.
But this fails in more complicated settings.

Important failures:
- certain subregions
- AdS-Rindler wedges
- black-hole backgrounds
- configurations with trapped null geodesics
- geometries supporting evanescent modes

In those cases, local boundary data is not enough for stable reconstruction.
The missing information lives in more nonlocal boundary structures.

This is where "precursors" enter:
highly nonlocal boundary operators that encode deep-bulk events before ordinary local boundary observables can register them.

## 3.7 Null geodesics as visibility law

A strong geometric rule from the literature:

continuous local reconstruction is expected only when null geodesics from the relevant bulk region reach the corresponding boundary region.

Translation for art/code:
- if a bulk event has light-like access to the boundary, it can be rendered as boundary-readable
- if it is trapped, shadowed, or geometrically screened, the reconstruction should become unstable, blurred, delayed, or impossible

## 3.8 Smearing function support

The smearing kernel is not just a blur.
It has support over regions of the boundary that are "responsible" for the bulk point.

This support may be:
- broad in space and time
- spacelike
- subregion-limited
- nonlocal in shape
- state-dependent in pathological backgrounds

Therefore:
a deep bulk event should often be controlled by a distributed boundary pattern, not one pixel or one UV point.

## 3.9 Precursors

A precursor is a nonlocal boundary operator that already encodes a deep-bulk event before local boundary operators can detect it causally.

In art-engine terms:
- precursors are hidden global control channels
- they should be able to affect bulk structure "too early"
- they are ideal for delayed revelation visuals, ghost geometry, and bulk events appearing before their surface evidence

## 3.10 Black holes, barriers, and failure of simple smearing

Pure AdS allows clean smearing reconstruction.
AdS black holes generally do not.

Why:
- horizons / barriers allow modes with exponentially small near-boundary imprint
- trapped null geodesics obstruct state-independent smearing kernels
- evanescent modes require exponential precision for local reconstruction

So the repo must not assume every holographic scene supports a nice clean boundary-to-bulk kernel.
Sometimes you only get:
- coarse-grained reconstruction
- approximate reconstruction
- nonlocal precursor channels
- missing or shadowed interior zones

## 3.11 Euclidean propagator is not the same thing

Do not confuse:
- bulk-to-boundary propagator for nonnormalizable/source modes
with
- Lorentzian smearing function for reconstructing normalizable/state modes

These are related but not identical objects.
The repo should keep them separate.

## 3.12 Shader commandments

### Commandment A — Boundary drives interior through kernels
No free interior detail without some boundary channel.

### Commandment B — Reconstruction is often distributed
One bulk point can depend on many boundary points over time.

### Commandment C — Obstruction matters
Black holes, wedges, barriers, and hidden regions should degrade local reconstruction.

### Commandment D — Add a nonlocal channel
To mimic precursors, every scene system should have at least one global/nonlocal control path.

### Commandment E — Distinguish source from response
Some boundary maps are explicit controls; other interior structures are derived reactions.

## 3.13 Minimal API vocabulary

- sourceMap
- operatorDensity
- smearRadius
- smearTimeWindow
- nonlocalMix
- precursorWeight
- visibilityMask
- trappedModeWeight
- responseGain
- boundaryResponsibility
- causalLag
- reconstructionConfidence

## 3.14 Failure modes to avoid

- one-to-one UV mapping from shell to interior point
- assuming all bulk structure is locally reconstructible
- treating black-hole scenes as if pure AdS smearing still works
- confusing source injection with state response
- making nonlocality purely decorative instead of structural
```

That is the theory file. Now the coding translation.

---

# Equations to add to `theory/equations.json`

```json id="fznjv5"
[
  {
    "id": "extrapolate_dictionary",
    "equation": "lim_{z->0} z^{-Delta} Phi(x,t,z) = O(x,t)",
    "meaning": "Near-boundary bulk field tail is identified with a local boundary operator"
  },
  {
    "id": "bulk_smearing",
    "equation": "Phi(B) = ∫ db' K(B|b') O(b') + ...",
    "meaning": "Bulk field reconstructed from boundary operators via a smearing kernel"
  },
  {
    "id": "source_partition_function",
    "equation": "Z_bulk[phi_(0)] = < exp( -∫ phi_(0) O ) >",
    "meaning": "Boundary values of bulk fields act as sources for dual boundary operators"
  },
  {
    "id": "near_boundary_expansion",
    "equation": "F(x,rho)=rho^m[f_(0)(x)+f_(2)(x)rho+...+rho^n(f_(2n)(x)+log(rho)f~_(2n)(x))+...]",
    "meaning": "Near-boundary expansion with leading source term and subleading response structure"
  }
]
```

The extrapolate dictionary and finite-depth smearing form are stated explicitly in Rosenhaus’s discussion of local reconstruction and in the later bulk-boundary mapping chapters, including the formulas (\lim z^{-\Delta}\Phi = O) and (\Phi(B)=\int K O). The same material emphasizes that in global AdS this can work with local operators, while in AdS-Rindler and black-hole settings nonlocal operators become essential.    

The source-functional statement and the interpretation of the leading near-boundary coefficient as the source for the dual operator come from holographic renormalization, where Skenderis writes the bulk partition function as a functional of boundary values and identifies the leading coefficient (f_{(0)}) as the source.  

---

# How I would code this

## 1. Source vs response architecture

Do not give every field one texture.
Give it two layers:

* `sourceMap`: explicit boundary inscription
* `responseField`: interior field evolved from source and geometry

### JS structure

```js id="rr5s9n"
export function makeBulkField({ sourceMap, responseSolver, kernel }) {
  return function bulkField(p, t) {
    const source = kernel.integrateBoundary((b) => {
      return kernel.weight(p, t, b) * sourceMap(b, t);
    });

    const response = responseSolver(p, t, source);
    return {
      source,
      response,
      value: source + response
    };
  };
}
```

This matches the actual dictionary much better than “sample one UV and extrude.”

---

## 2. Boundary smearing kernel

The cleanest shader-art analog of the smearing function is a weighted boundary integral over space and time.

### Pseudocode

```js id="7yoob0"
function smearFromBoundary(p, t, samples, sourceMap, params) {
  let acc = 0.0;
  let norm = 0.0;

  for (const s of samples) {
    const dt = t - s.time;
    const dx = length(p.xz - s.pos);
    const wSpace = Math.exp(-dx * params.smearRadiusInv);
    const wTime  = Math.exp(-Math.abs(dt) * params.smearTimeInv);
    const wDepth = Math.exp(-p.y * params.depthFalloff);

    const w = wSpace * wTime * wDepth;
    acc += w * sourceMap(s.uv, s.time);
    norm += w;
  }

  return norm > 0.0 ? acc / norm : 0.0;
}
```

This is the first usable analog of

[
\Phi(B) = \int db' , K(B|b') O(b')
]

but with art-friendly numerics.  

---

## 3. GLSL local-smear field

```glsl id="ya9w10"
float sourceTexture(vec2 uv);

float localKernel(vec3 p, vec3 b, float dt, vec4 prm) {
    float spaceW = exp(-length(p.xz - b.xz) * prm.x);
    float timeW  = exp(-abs(dt) * prm.y);
    float depthW = exp(-max(p.y, 0.0) * prm.z);
    return spaceW * timeW * depthW;
}

float smearedBulkField(vec3 p, float t) {
    float acc = 0.0;
    float norm = 0.0;

    for (int i = 0; i < BOUNDARY_SAMPLES; i++) {
        vec3 b = boundarySamplePos(i);
        float tb = boundarySampleTime(i);
        float w = localKernel(p, b, t - tb, vec4(4.0, 2.5, 1.2, 0.0));
        float src = sourceTexture(boundarySampleUV(i));
        acc += w * src;
        norm += w;
    }

    return acc / max(norm, 1e-4);
}
```

Use this only for scenes that are supposed to feel like **clean global AdS reconstruction**.

---

## 4. Add a precursor channel

The sources are very clear that some bulk information is encoded in **nonlocal boundary operators** rather than local ones, especially when local reconstruction fails or is delayed. That is exactly what your engine should mimic with a second channel: a global transform, graph mode, or long-range coupling layer.  

### JS precursor layer

```js id="ff7le8"
function precursorSignal(boundaryState, p, t, params) {
  const globalMode = boundaryState.fourierLowModes(p, t);
  const graphMode = boundaryState.entanglementGraphInfluence(p);
  const hiddenLag = Math.sin(params.precursorFreq * t + length(p) * params.phaseWarp);

  return params.precursorWeight * (
    0.5 * globalMode +
    0.35 * graphMode +
    0.15 * hiddenLag
  );
}
```

### Rule

The precursor channel should be able to alter the deep bulk **before** the local smear channel strongly responds.

That makes the scene feel eerily correct.

---

## 5. Visibility / null-geodesic mask

Rosenhaus’s subregion results suggest a clean geometric law: continuous reconstruction works only when the relevant null geodesics are captured by the chosen boundary region. In code, fake that with a **visibility confidence field**.  

```glsl id="mjqni3"
float reconstructionConfidence(vec3 p, vec3 boundaryCenter, float aperture) {
    vec3 dir = normalize(p - boundaryCenter);
    float cone = dot(dir, vec3(0.0, 1.0, 0.0));
    return smoothstep(aperture, aperture + 0.1, cone);
}
```

Then:

* high confidence → crisp local reconstruction
* low confidence → blur, delay, precursor-only behavior, or data loss

---

## 6. Trapped-mode / black-hole degradation

The black-hole and evanescent-mode results mean the repo should include a mode where local reconstruction becomes exponentially unreliable. In graphics terms, this is not just fog. It is **information loss from boundary readout**.   

### GLSL obstruction field

```glsl id="7o9n7v"
float trappedModeWeight(vec3 p, vec3 trapCenter, float trapRadius) {
    float d = length(p - trapCenter);
    float shell = smoothstep(trapRadius, trapRadius * 0.7, d);
    return 1.0 - shell;
}

float degradedReconstruction(float localValue, float precursorValue, float trapW) {
    float localSuppressed = localValue * exp(-4.0 * trapW);
    float nonlocalBoosted = precursorValue * (0.3 + 0.7 * trapW);
    return localSuppressed + nonlocalBoosted;
}
```

This is exactly how to make a “holographic black-hole scene” feel different from plain AdS.

---

## 7. Responsibility map

A super useful engineering abstraction from the smearing-function idea is:

**For each bulk point, compute which subset of the boundary is responsible for it.** 

### Data structure

```js id="wmy5hf"
{
  bulkPoint: [x, y, z],
  responsibilityRegion: {
    centerUV: [u, v],
    spatialRadius: 0.18,
    timeWindow: 0.44,
    confidence: 0.76
  }
}
```

This can drive:

* UI overlays
* debug views
* prompt generation
* live modulation routing

---

# `theory/concepts.json` addition

```json id="89nqey"
{
  "id": "bulk_boundary_dictionary",
  "name": "Bulk Boundary Dictionary",
  "physics_meaning": "Bulk fields are encoded by boundary operators; near-boundary behavior gives operator data, and finite-depth bulk reconstruction may be achieved through smearing kernels or more nonlocal precursor structures.",
  "math_forms": [
    "lim_{z->0} z^{-Delta} Phi(x,t,z) = O(x,t)",
    "Phi(B) = ∫ db' K(B|b') O(b') + ...",
    "Z_bulk[phi_(0)] = <exp(-∫ phi_(0) O)>",
    "F(x,rho)=rho^m[f_(0)+f_(2)rho+...+rho^n(f_(2n)+log(rho)f~_(2n))+...]"
  ],
  "visual_translation": [
    "boundary inscriptions generate bulk events",
    "distributed control of interior points",
    "nonlocal influence halos",
    "precursor ghost structures",
    "shadowed or obstructed reconstruction"
  ],
  "shader_parameters": [
    "smearRadius",
    "smearTimeWindow",
    "nonlocalMix",
    "precursorWeight",
    "visibilityMask",
    "trappedModeWeight",
    "responseGain",
    "causalLag",
    "reconstructionConfidence"
  ],
  "rendering_strategies": [
    "boundary integral kernels",
    "source/response separation",
    "global precursor channel",
    "confidence-weighted reconstruction",
    "obstruction-aware degradation"
  ],
  "failure_modes": [
    "one UV point controls one bulk point",
    "all scenes use local reconstruction only",
    "black-hole scenes without information degradation",
    "source and response collapsed into one texture"
  ]
}
```

---

# Example scene for this file

## `examples/03_boundary_operator_projection`

**Scene:**
A dark AdS chamber with a luminous boundary ring covered in operator glyphs. A bulk event—say, a floating mask, crystal organ, or angelic manifold—does not exist on its own. It appears only when a distributed set of boundary operator cells phase-lock across space and time.

### Scene logic

* local kernel reconstructs the visible body
* precursor layer creates faint early ghost geometry
* a central trap region suppresses local readability
* boundary UI shows “responsibility arcs” linking active shell zones to the current bulk event
* when the camera moves behind a causal wedge, reconstruction confidence drops and the body breaks into nonlocal shimmer

### Why this is good

It teaches the user that bulk form is a **readout**, not a solid thing.

---

# One deeply important repo distinction

You want three separate control classes in the engine:

## A. Source channels

What the boundary explicitly writes into the system.

Examples:

* glyph masks
* operator textures
* injected frequencies
* UI events
* external API prompts

## B. Response channels

What the bulk computes from those sources.

Examples:

* interior glow
* deformed SDF
* emergent lattices
* color-field drift
* entanglement bridges

## C. Precursor channels

What the system knows globally before local readout catches up.

Examples:

* low-frequency hidden modes
* graph couplings
* latent scene states
* global rhythm locks
* delayed reveal memory

If you keep those separate, your app starts feeling like an actual holographic machine instead of just a shader toy.

---

# One subtle but crucial warning

Do **not** confuse the Lorentzian smearing function for reconstructing normalizable/state data with the Euclidean bulk-to-boundary propagator for source insertion. The source material explicitly warns these are different objects. For the repo, that means you should keep two APIs:

* `injectSource(...)`
* `reconstructBulk(...)`

not one mushy “project boundary inward” function. 

---

# What I would build next in code after this

Immediate files:

* `src/glsl/fields/boundary_data_field.glsl`
* `src/glsl/fields/operator_smear.glsl`
* `src/glsl/fields/precursor_field.glsl`
* `src/js/boundaryEncoders.js`

The next theory file should be:

`docs/04_entanglement_geometry.md`

That’s where we turn:

* entanglement entropy
* Ryu–Takayanagi surfaces
* wormhole-ish bridges
* mutual information
* bit-thread aesthetics

into shader systems and raymarched scene logic.

Say **next**.

Excellent. This is the file where the repo stops being “holography aesthetics” and starts becoming **relationship-driven geometry**.

# `docs/04_entanglement_geometry.md`

```md id="e2dgmw"
# 04 — Entanglement Geometry

## 4.1 Core idea

In holography, entanglement is not just a hidden quantum statistic.
It is geometrized.

The Ryu–Takayanagi formula says that for a boundary region A in a static holographic state, the entanglement entropy is given by the area of a bulk minimal surface anchored on the boundary of A:

S(A) = Area(gamma_A) / (4 G_N)

where gamma_A is the bulk minimal surface with boundary
partial gamma_A = partial A.

This is one of the deepest usable ideas in the repo:
connectivity becomes geometry.

For shader art, that means:
- stronger relation -> thicker or broader bridge
- lower entanglement -> thinner membrane or disconnected pieces
- cuts in relational structure -> tears in space
- merged entanglement wedges -> shared interior chambers

## 4.2 Minimal surfaces are not decoration

The minimal surface is not just a visual flourish like a soap film.
It is the actual geometric object that computes the entropy in the holographic description.

So in the engine:
- a membrane can measure relation
- a bridge can represent shared information
- a surface can be both visible object and invisible structural constraint

## 4.3 RT formula

For static situations:

S_A = Area(gamma_A) / (4 G_N)

The bulk surface gamma_A:
- is codimension 2
- is anchored on partial A
- is minimal among surfaces homologous to A

Important repo translation:
entanglement is encoded by tension-minimizing geometry.

## 4.4 Mutual information

For two boundary regions A and B, define

I(A:B) = S_A + S_B - S_(A union B)

This measures how much information they share.

In holographic settings this is especially visual:
- if the entanglement wedge for A union B is connected, mutual information can be positive
- if the preferred surfaces disconnect, mutual information can drop to zero

That gives a powerful scene law:
two objects can appear disconnected until a threshold, then suddenly form a shared bridge.

## 4.5 Phase transitions of connectivity

A major visual gift of holographic entanglement is that the preferred minimal-surface configuration can change discontinuously.

For two separated regions:
- one candidate uses two disconnected surfaces
- another candidate uses a connected bridge-like configuration

Whichever has smaller total area wins.

This gives you:
- abrupt bridge formation
- sudden merge / unmerge behavior
- geometric phase transitions
- topology-feeling changes without changing object count

## 4.6 Entanglement wedge intuition

The surface gamma_A encloses a bulk region naturally associated with A.
For art use, call this the entanglement wedge.

Good engine rule:
- the wedge is the interior volume “owned” by the boundary region
- overlapping or touching wedges imply relation
- wedge separation implies informational disconnection

## 4.7 General entropy constraints

Any entanglement engine worth using should obey basic entropy logic.

### Subadditivity
S(AB) <= S(A) + S(B)

### Strong subadditivity
S(AB) + S(BC) >= S(B) + S(ABC)

These are not optional vibes.
They are part of the geometric consistency of RT.

In holographic theories there are even stronger constraints, such as monogamy of mutual information.

## 4.8 Monogamy of mutual information

Holographic states obey a special inequality often written as

I(A:BC) >= I(A:B) + I(A:C)

This means correlation shared with a union can exceed the pairwise pieces in a way that reflects constrained geometric organization.

For art:
- one entity can be deeply linked to a combined structure without being independently linked strongly to each part
- network geometry should not just be arbitrary spaghetti
- relational intensity should have global consistency

## 4.9 Time dependence warning

RT is for static situations.
If the scene is genuinely time-dependent, the correct geometry is more subtle than a simple spatial minimal surface.

Repo rule:
- use RT mode for frozen or quasi-static scenes
- use “covariant membrane” mode for animated causal scenes
- do not pretend a static soap film is always the correct object in dynamical geometry

## 4.10 Visual laws to extract

### Law A — Relation makes bridges
Shared information should literally create connecting membranes, tunnels, filaments, or chambers.

### Law B — Tension minimizes explanation
The connecting surface should look like the least extravagant geometric way to join regions.

### Law C — Disconnects cause tears
When mutual information falls, bridges thin, snap, or evaporate.

### Law D — Geometry can jump
Bridge formation can be sudden, not gradual.

### Law E — Networks obey consistency
Relational graphics should satisfy some monogamy logic, not all-to-all maximal linking.

## 4.11 API vocabulary

- entanglementWeight
- rtTension
- wedgeDepth
- bridgeThreshold
- mutualInfo
- monogamyBias
- disconnectSharpness
- membraneGlow
- cutSeverity
- sharedInteriorWeight
- covariantMode
- relationGraph

## 4.12 Failure modes to avoid

- random webbing with no metric meaning
- bridges that do not respond to relation strength
- every pair connected equally
- no phase transition between disconnected and connected surface regimes
- static membranes used blindly in fast causal scenes
```

That is the theory file. Now the actual repo-useful math and code.

---

# Core equations for `theory/equations.json`

```json id="1anv4p"
[
  {
    "id": "ryu_takayanagi",
    "equation": "S(A) = Area(gamma_A) / (4 G_N)",
    "meaning": "Entanglement entropy of a boundary region A is given by the area of a bulk minimal surface anchored on partial A"
  },
  {
    "id": "mutual_information",
    "equation": "I(A:B) = S(A) + S(B) - S(A union B)",
    "meaning": "Shared information between two regions"
  },
  {
    "id": "subadditivity",
    "equation": "S(AB) <= S(A) + S(B)",
    "meaning": "Basic entropy consistency condition"
  },
  {
    "id": "strong_subadditivity",
    "equation": "S(AB) + S(BC) >= S(B) + S(ABC)",
    "meaning": "Stronger entropy consistency condition"
  },
  {
    "id": "mmi",
    "equation": "I(A:BC) >= I(A:B) + I(A:C)",
    "meaning": "Monogamy of mutual information, special to holographic theories"
  }
]
```

The RT formula is stated directly in the uploaded entanglement notes and in your holographic summary doc, with the interpretation that a classical geometry problem computes the entropy by the area of a minimal bulk surface. Those same notes connect bulk geometry to entanglement so strongly that they explicitly say spacetime emergence from entanglement has been seriously proposed.   

Mutual information is defined as (I(A:B)=S(A)+S(B)-S(AB)), and the notes explain that holographic mutual information shows special geometric behavior, including connected/disconnected surface competition and monogamy of mutual information.  

The RT setup is static; Rosenhaus explicitly notes that RT only applies in static situations when discussing boundary subregions and minimal surfaces. 

---

# How I would code this

## 1. Treat entanglement as a weighted graph first

Before any membrane rendering, define a **relation graph**.

Each major object, region, or boundary patch gets a node.
Each pair gets an entanglement weight.

```js id="z8n38z"
export class EntanglementGraph {
  constructor(nodes) {
    this.nodes = nodes;
    this.weights = new Map();
  }

  key(a, b) {
    return a < b ? `${a}:${b}` : `${b}:${a}`;
  }

  setWeight(a, b, w) {
    this.weights.set(this.key(a, b), Math.max(0, w));
  }

  getWeight(a, b) {
    return this.weights.get(this.key(a, b)) ?? 0;
  }
}
```

This gives you the first real control plane for bridge logic.

---

## 2. Convert graph weights into candidate RT bridges

A nice art-engine approximation is:

* low weight → disconnected configuration
* high weight → connected membrane candidate
* compare a disconnected cost and connected cost
* choose the lower one

That mimics the RT phase transition logic.

```js id="mbh2q8"
function chooseBridgePhase(w, params) {
  const disconnectedCost = params.baseDisconnectCost - 0.15 * w;
  const connectedCost =
    params.baseConnectCost
    - params.bridgeGain * w
    + params.bridgeLengthPenalty;

  return connectedCost < disconnectedCost ? "connected" : "disconnected";
}
```

This is fake physics, but it is the **right fake**.

---

## 3. Minimal-surface-inspired membrane between two anchors

For two anchor loops or points, use a tensioned sheet, not a tube.

### GLSL-ish shape field

A cheap version is a warped signed-distance blend between two anchor sets plus a tension term.

```glsl id="ojm0tt"
float membraneBridge(vec3 p, vec3 a, vec3 b, float w) {
    vec3 mid = 0.5 * (a + b);
    float span = length(b - a);
    float radial = length(p - mix(a, b, 0.5));
    float axisDist = length(cross(normalize(b - a), p - a));
    float thickness = mix(0.02, 0.18, w);

    float saddle = axisDist - thickness * (1.2 - 0.8 * cos(6.2831 * dot(p - mid, normalize(b - a)) / max(span, 1e-3)));
    return saddle;
}
```

Better than a literal cylinder:

* let the bridge flatten into a saddle or soap-film surface
* make thickness respond nonlinearly to relation weight
* let it pinch before disconnect

---

## 4. Mutual-information thresholding

The cleanest control law in the whole section:

```js id="dcg8p2"
function mutualInformation(SA, SB, SAB) {
  return Math.max(0, SA + SB - SAB);
}

function bridgeStrengthFromMI(I, threshold, softness = 0.1) {
  const x = (I - threshold) / Math.max(softness, 1e-4);
  return 1 / (1 + Math.exp(-x));
}
```

So:

* below threshold, no bridge
* near threshold, flicker / unstable membrane
* above threshold, wedge merge or stable bridge

That gives you the signature RT feel.

---

## 5. Entanglement wedge renderer

Think of a wedge as a region “owned” by a boundary segment.

For a boundary interval or patch:

* shoot interior influence
* cap it by a minimal membrane
* render the interior volume behind that membrane as the wedge

### Cheap scalar field version

```glsl id="d6e1rs"
float wedgeField(vec3 p, vec3 anchorCenter, vec3 inwardDir, float depth, float spread) {
    vec3 q = p - anchorCenter;
    float axial = dot(q, inwardDir);
    float lateral = length(q - axial * inwardDir);

    float core = smoothstep(depth, 0.0, abs(axial - depth * 0.5));
    float fan = exp(-lateral * spread);
    return core * fan * step(0.0, axial);
}
```

Use two wedges and let them overlap:

* overlap region glows harder
* overlap can spawn bridges or shared chambers
* no overlap means informational separation

---

## 6. Monogamy bias

This is the part most art engines will skip and it would make yours smarter than 95% of them.

Holographic mutual information is **monogamous**, not free-for-all. The notes explicitly describe monogamy of mutual information as a special property of holographic theories. 

So if node A is strongly linked to B and C jointly, do not allow unlimited independent saturation to both.

```js id="txnjrq"
function applyMonogamyBias(graph, focus, others, strength = 0.35) {
  let total = 0;
  for (const j of others) total += graph.getWeight(focus, j);

  for (const j of others) {
    const w = graph.getWeight(focus, j);
    const suppressed = w / (1 + strength * Math.max(0, total - w));
    graph.setWeight(focus, j, suppressed);
  }
}
```

This makes networks look constrained and eerily believable.

---

## 7. Tear / cut rendering

If entanglement drops, space should not merely dim. It should **degrade structurally**.

```glsl id="viuv7n"
float cutField(float bridgeStrength, float noiseVal, float cutSeverity) {
    float instability = 1.0 - bridgeStrength;
    return smoothstep(0.5 - cutSeverity, 0.5 + cutSeverity, noiseVal * instability);
}
```

Visual results:

* pinched membranes
* severed luminous threads
* shadow shears
* missing interior geometry
* snapping shared chambers

That lines up with the “space woven from relationship” intuition in your source notes. 

---

## 8. Static vs covariant mode

Since RT is static, the repo should expose two render modes.

```js id="ypvut0"
export const entanglementModes = {
  STATIC_RT: "static_rt",
  COVARIANT: "covariant"
};
```

### Static RT mode

* membranes relax to minimal-area-like forms
* bridges depend on current graph weights only

### Covariant mode

* add lag
* add causal propagation
* let membranes trail, skew, or overshoot
* do not insist on instantaneous minimality

This keeps the repo honest about the static limitation of RT. 

---

# `theory/concepts.json` addition

```json id="v4v6je"
{
  "id": "entanglement_geometry",
  "name": "Entanglement Geometry",
  "physics_meaning": "In holographic theories, entanglement entropy of a boundary region is encoded by the area of a bulk minimal surface, and shared information controls bulk connectivity.",
  "math_forms": [
    "S(A) = Area(gamma_A) / (4 G_N)",
    "I(A:B) = S(A) + S(B) - S(A union B)",
    "S(AB) <= S(A) + S(B)",
    "S(AB) + S(BC) >= S(B) + S(ABC)",
    "I(A:BC) >= I(A:B) + I(A:C)"
  ],
  "visual_translation": [
    "soap-film membranes in warped bulk",
    "entanglement bridges",
    "shared interior chambers",
    "cut geometry when relations weaken",
    "connectivity phase transitions"
  ],
  "shader_parameters": [
    "entanglementWeight",
    "rtTension",
    "wedgeDepth",
    "bridgeThreshold",
    "mutualInfo",
    "monogamyBias",
    "disconnectSharpness",
    "membraneGlow",
    "cutSeverity",
    "sharedInteriorWeight",
    "covariantMode"
  ],
  "rendering_strategies": [
    "relation graph to geometry",
    "minimal-surface-inspired membranes",
    "mutual-information thresholding",
    "wedge overlap rendering",
    "monogamy-constrained network layout"
  ],
  "failure_modes": [
    "random webs with no relation metric",
    "all-to-all maximum connectivity",
    "no disconnected/connected phase transition",
    "using static RT membranes for every time-dependent scene"
  ]
}
```

---

# Example scene for this section

## `examples/04_entanglement_bridge`

**Scene:**
Two luminous boundary arcs float on opposite sides of an AdS chamber. Each arc controls a semi-transparent humanoid or mask-like volume. As the mutual information rises, a soap-film bridge blooms between them, then thickens into a shared interior sanctuary. When relation drops, the membrane pinches, develops tears, and finally snaps into disconnected wedges.

### Scene logic

* each boundary arc has its own wedge field
* overlap is computed in the bulk
* overlap plus graph weight determines bridge phase
* monogamy bias prevents one figure from fully connecting to every other figure at once
* cut events produce visible geometry failure, not just opacity loss

### Parameters

```js id="jchxqs"
{
  entanglementWeight: 0.82,
  bridgeThreshold: 0.44,
  rtTension: 0.73,
  membraneGlow: 1.35,
  wedgeDepth: 1.8,
  monogamyBias: 0.28,
  cutSeverity: 0.19,
  sharedInteriorWeight: 0.67,
  covariantMode: false
}
```

---

# What this unlocks for the app

After this file, your app can support three insanely useful design modes:

## 1. Relationship-built worlds

Geometry appears only when relations exist.

## 2. Shared-chamber portraits

Two entities get one interior cathedral when mutual information crosses a threshold.

## 3. Severed-space aesthetics

Broken entanglement becomes torn spacetime, fractured membranes, and shadow voids.

This is where holography starts feeling emotionally and structurally different from ordinary sci-fi graphics.

---

# One very important design distinction

Do not confuse:

* **entanglement web** as visual ornament
  with
* **entanglement geometry** as the actual rule that decides whether bulk regions connect

The latter is what you want. The notes are explicit that RT is not merely suggestive art language; it is a geometric prescription for entropy in holographic theories.  

---

# What I would add to the codebase right after this

* `src/js/entanglementGraphs.js`
* `src/glsl/fields/entanglement_field.glsl`
* `src/glsl/scenes/scene_rt_surface.glsl`

Next file should be:

`docs/05_holographic_renormalization.md`

That one will give you:

* near-boundary expansions
* source/response extraction
* counterterm intuition
* divergence taming
* how to make “renormalization” into a visual pipeline instead of a math footnote

Say **next**.


Yes. This is the layer that teaches the repo how to survive the boundary without exploding into nonsense.

# `docs/05_holographic_renormalization.md`

```md id="ylm9q4"
# 05 — Holographic Renormalization

## 5.1 Core idea

AdS has a boundary at infinity, and near that boundary many bulk quantities diverge.
In the dual picture, these bulk IR divergences match boundary UV divergences.

So holographic renormalization is the procedure that:
- expands fields near the boundary
- isolates divergent terms
- adds local covariant counterterms on the cutoff surface
- removes the cutoff
- extracts finite physical data such as 1-point functions, anomalies, and RG behavior

For this repo, that means:

boundary blow-up -> regulated cutoff -> subtract local artifacts -> keep finite source/response structure

## 5.2 The essential moral

Do not treat the near-boundary singularity as a bug.
It is where the source data lives.

The source material is explicit that holographic renormalization is driven by near-boundary analysis, because the divergent structure is determined there, while exact correlation functions still need full bulk solutions. The same source also stresses that divergences should be removed by covariant counterterms rather than by ad hoc dropping of infinite pieces. :contentReference[oaicite:0]{index=0}

## 5.3 Fefferman–Graham form

A standard asymptotically AdS metric near the boundary is

ds^2 = d rho^2 / (4 rho^2) + (1 / rho) g_ij(x, rho) dx^i dx^j

with rho -> 0 at the boundary.

This is the natural coordinate system for the repo’s “boundary expansion” logic. :contentReference[oaicite:1]{index=1}

## 5.4 Generic near-boundary expansion

Near the boundary, a bulk field has an asymptotic expansion of the form

F(x, rho) = rho^m [ f_(0)(x) + f_(2)(x) rho + ... + rho^n ( f_(2n)(x) + log rho f~_(2n)(x) ) + ... ]

Interpretation:
- f_(0): leading boundary datum, usually the source
- recursively determined subleading coefficients: kinematic response pieces
- deeper undetermined coefficient: state-dependent data
- log terms: anomaly fingerprints, especially in even boundary dimension

That is almost the whole repo in one equation: source first, response later, anomaly if logs appear. :contentReference[oaicite:2]{index=2}

## 5.5 Metric coefficients and stress-tensor data

For asymptotically AdS gravity, the near-boundary analysis determines the lower-order coefficients of the metric expansion, while a higher coefficient remains state-dependent.
Skenderis notes that g_(2), ..., g_(d-2), h_(d), and the trace/divergence constraints are fixed near the boundary, while g_(d) is directly related to the 1-point function of the dual stress tensor. :contentReference[oaicite:3]{index=3}

Repo translation:
- some geometry is universal counterterm structure
- some geometry is actual scene state
- do not confuse the two

## 5.6 Partition-function view

The holographic dictionary packages sources as boundary values of bulk fields:

Z_bulk[phi_(0)] = < exp( - ∫ phi_(0) O ) >

So the leading boundary coefficient is not a decorative shell value.
It is a source coupling into the dual operator algebra. :contentReference[oaicite:4]{index=4}

## 5.7 What renormalization actually does

The method, as summarized by Skenderis, is:

1. solve asymptotically near the boundary
2. regulate at a cutoff surface
3. add local covariant counterterms
4. form the renormalized on-shell action
5. compute finite 1-point functions
6. derive Ward identities and anomalies
7. obtain RG equations
8. compute higher-point functions from renormalized 1-point data in the presence of sources

That is a nearly perfect engineering pipeline for the repo. :contentReference[oaicite:5]{index=5} :contentReference[oaicite:6]{index=6}

## 5.8 Why counterterms matter

Early AdS/CFT computations often just dropped divergences by hand.
Holographic renormalization instead uses covariant counterterms so that the whole subtraction scheme is consistent with symmetries and with the various observables derived from the action. :contentReference[oaicite:7]{index=7} :contentReference[oaicite:8]{index=8}

Repo translation:
- do not hide bad boundary behavior with random fades
- subtract it with structured, local visual counterterms

## 5.9 Ward identities and anomalies

A major point in the source text is that near-boundary analysis is enough to determine the holographic Ward identities and anomalies, because these are controlled by the divergence structure and symmetry properties of the UV/near-boundary sector. :contentReference[oaicite:9]{index=9}

Visual translation:
- some depth-dependent distortions are removable artifacts
- some residual patterns are true anomaly signatures and should remain

## 5.10 RG meaning

The same source emphasizes RG transformations as part of the renormalization method and ties holographic renormalization to holographic RG flow. In the repo, radial evolution is therefore not only a geometric motion, but a flow of effective description across scales. :contentReference[oaicite:10]{index=10} :contentReference[oaicite:11]{index=11}

## 5.11 Shader laws to extract

### Law A — Boundary divergence is structured
Near-boundary excess brightness, frequency, or density should come from known asymptotic terms, not arbitrary chaos.

### Law B — Separate divergent shell from finite body
Your renderer should identify what is “cutoff artifact” and what is “physical response.”

### Law C — Counterterms are local
A visual counterterm should depend only on local cutoff-surface data and its derivatives.

### Law D — Logs are special
Logarithmic terms should create anomaly-like residuals: thin interference bands, irreducible color skew, Weyl-fracture glows.

### Law E — State lives deeper than source
Leading coefficients are source code; deeper coefficients carry scene state.

## 5.12 API vocabulary

- cutoffRho
- sourceFieldWeight
- responseFieldWeight
- countertermStrength
- anomalyMix
- renormFade
- shellDivergenceGain
- finitePartGain
- wardRespect
- logTermGlow
- rgFlowRate
- sourceResponseSplit

## 5.13 Failure modes to avoid

- clipping the boundary with no interpretation
- confusing source divergence with physical response
- removing all near-boundary structure and losing the source layer
- using nonlocal “counterterms” for a local subtraction job
- treating anomaly leftovers as bugs instead of signatures
```

Now the code translation.

---

# Equations to add to `theory/equations.json`

```json id="1r8jkl"
[
  {
    "id": "fg_metric",
    "equation": "ds^2 = d rho^2/(4 rho^2) + (1/rho) g_ij(x,rho) dx^i dx^j",
    "meaning": "Fefferman–Graham near-boundary form for asymptotically AdS geometry"
  },
  {
    "id": "generic_near_boundary_field",
    "equation": "F(x,rho)=rho^m[f_(0)(x)+f_(2)(x)rho+...+rho^n(f_(2n)(x)+log(rho)f~_(2n)(x))+...]",
    "meaning": "Generic asymptotic expansion with source, response, and possible anomaly/log terms"
  },
  {
    "id": "source_partition_function",
    "equation": "Z_bulk[phi_(0)] = < exp( -∫ phi_(0) O ) >",
    "meaning": "Boundary values act as sources in the dual field theory"
  }
]
```

These three are the spine of the section: the Fefferman–Graham metric form, the generic near-boundary field expansion, and the partition-function source statement all appear directly in the uploaded renormalization notes. The same notes also state that the coefficient (g_{(d)}) is directly related to the 1-point function of the dual stress tensor, while lower coefficients and anomaly data are fixed by the near-boundary equations.  

---

# How I would code this

## 1. Treat every bulk field as four layers

Not one layer. Four.

* `source`
* `divergentShell`
* `finiteResponse`
* `anomalyResidue`

### JS structure

```js id="lg7j0s"
export function decomposeField(rawField, rho, params) {
  const source = rawField.source;
  const divergentShell = params.shellDivergenceGain * source / Math.max(rho, 1e-4);
  const finiteResponse = rawField.response;
  const anomalyResidue = params.anomalyMix * Math.log(1.0 / Math.max(rho, 1e-4)) * rawField.logTerm;

  return {
    source,
    divergentShell,
    finiteResponse,
    anomalyResidue
  };
}
```

That is the core repo move: stop treating all brightness near the boundary as one undifferentiated thing.

---

## 2. Explicit cutoff surface

You want an actual cutoff variable, not a hidden hard clip.

```js id="h8yqfq"
export const renormParams = {
  cutoffRho: 0.03,
  countertermStrength: 1.0,
  shellDivergenceGain: 0.9,
  finitePartGain: 1.2,
  anomalyMix: 0.18,
  renormFade: 0.4,
  sourceResponseSplit: 0.55,
  wardRespect: 0.8,
  logTermGlow: 0.22,
  rgFlowRate: 0.65
};
```

`cutoffRho` is not only a render parameter.
It is your selected renormalization surface.

---

## 3. Local counterterm shader

Counterterms should be local and shell-based.

```glsl id="c5u2d7"
float countertermLocal(float shellValue, float gradMag, float rho, vec4 prm) {
    float leading = prm.x * shellValue / max(rho, 1e-3);
    float gradTerm = prm.y * gradMag;
    float logTerm = prm.z * log(1.0 / max(rho, 1e-3));
    return leading + gradTerm + logTerm;
}
```

Then define a renormalized visible field:

```glsl id="v4hlyo"
float renormalizedField(float rawField, float counterterm, float finiteGain) {
    return finiteGain * (rawField - counterterm);
}
```

This is the direct visual analog of “regularize, add counterterms, keep finite part.”  

---

## 4. Near-boundary expansion evaluator

Make your renderer evaluate explicit asymptotic series instead of winging it.

```glsl id="ujsyww"
struct AsymptoticData {
    float f0;
    float f2;
    float f4;
    float logTerm;
};

float asymptoticField(float rho, AsymptoticData a) {
    return a.f0
         + a.f2 * rho
         + a.f4 * rho * rho
         + a.logTerm * rho * rho * log(max(rho, 1e-4));
}
```

This is how the repo learns “source first, response later, anomaly in the log.”

---

## 5. Visual anomaly layer

The log term should leave something irreducible behind.
Not huge. Precise.

```glsl id="u9lmv9"
vec3 anomalyColor(vec2 uv, float rho, float strength) {
    float rings = 0.5 + 0.5 * cos(18.0 * length(uv) - 2.0 * log(max(rho, 1e-4)));
    return strength * vec3(0.2, 0.8, 1.2) * rings;
}
```

Use this for:

* thin holographic chroma fractures
* residual interference halos
* Weyl-anomaly-looking shell scars

The reason to keep it is that the notes explicitly single out anomalies as a real renormalized output of the near-boundary analysis, not disposable junk.  

---

## 6. Extracting a finite 1-point response

Skenderis emphasizes renormalized 1-point functions in the presence of sources as the central object, with higher-point functions built from them. In the repo, that means every scene should expose a finite response observable after renormalization. 

### JS pattern

```js id="9e9l92"
export function finiteOnePoint(decomp, params) {
  return params.finitePartGain * (
    decomp.finiteResponse +
    0.15 * decomp.anomalyResidue
  );
}
```

That value can drive:

* bloom
* geometry deformation
* sound reactivity
* UI meters
* API summaries

---

## 7. RG flow as controlled depth filtering

Renormalization group flow should be implemented as depth-dependent coarse-graining.

```js id="6qjczf"
export function rgFilteredFrequency(baseFreq, rho, params) {
  const x = Math.log(1.0 / Math.max(rho, 1e-4));
  return baseFreq / (1.0 + params.rgFlowRate * x);
}
```

Near boundary:

* very fine structure
* unstable detail
* high-frequency source inscriptions

Deeper:

* coarser, more collective response

That matches the notes’ framing of holographic RG and the connection between radial structure and renormalization.  

---

## 8. Counterterm debug mode

This repo should absolutely include a mode that visualizes:

* raw field
* divergent shell
* counterterm
* finite remainder
* anomaly residue

Because this is where the system becomes legible to you while designing scenes.

```js id="jlwmnz"
export const renormDebugModes = {
  RAW: "raw",
  DIVERGENT: "divergent",
  COUNTERTERM: "counterterm",
  FINITE: "finite",
  ANOMALY: "anomaly"
};
```

---

# `theory/concepts.json` addition

```json id="nxxu4a"
{
  "id": "holographic_renormalization",
  "name": "Holographic Renormalization",
  "physics_meaning": "Near-boundary divergences in asymptotically AdS bulk fields are regulated and removed by local covariant counterterms, leaving finite source/response data, anomalies, and RG structure.",
  "math_forms": [
    "ds^2 = d rho^2/(4 rho^2) + (1/rho) g_ij(x,rho) dx^i dx^j",
    "F(x,rho)=rho^m[f_(0)+f_(2)rho+...+rho^n(f_(2n)+log(rho)f~_(2n))+...]",
    "Z_bulk[phi_(0)] = <exp(-∫ phi_(0) O)>"
  ],
  "visual_translation": [
    "boundary blow-up shells",
    "local subtraction layers",
    "finite response extraction",
    "log-anomaly glows",
    "radial RG filtering"
  ],
  "shader_parameters": [
    "cutoffRho",
    "countertermStrength",
    "shellDivergenceGain",
    "finitePartGain",
    "anomalyMix",
    "renormFade",
    "sourceResponseSplit",
    "wardRespect",
    "logTermGlow",
    "rgFlowRate"
  ],
  "rendering_strategies": [
    "explicit asymptotic series evaluation",
    "local counterterm subtraction",
    "finite one-point extraction",
    "boundary debug passes",
    "depth-based RG coarse-graining"
  ],
  "failure_modes": [
    "hard-clipping the boundary with no meaning",
    "dropping divergences by hand with no counterterm layer",
    "confusing source shell with physical response",
    "discarding anomaly residues"
  ]
}
```

---

# Example scene for this section

## `examples/05_renormalized_boundary_fire`

A raymarched AdS chamber whose boundary appears almost catastrophically overexposed with glyph-fire, scanline singularities, and spectral shell noise. But the scene includes a renormalization pass that subtracts local shell artifacts and reveals a finite interior response: a stable cathedral, entity, or membrane hidden beneath the blow-up. Thin anomaly bands remain as iridescent scars on the boundary.

### Scene law

* raw pass is overwhelming
* counterterm pass is surgical and local
* finite response pass is calmer, more legible, more structural
* anomaly pass adds irreducible interference filaments
* changing cutoff changes the apparent “theory of the image”

That is exactly the right vibe for holographic renormalization.

---

# One repo-level distinction you absolutely want

There are three different things near the boundary:

## 1. Source

What you intentionally inject.

## 2. Divergent artifact

What appears because the cutoff surface is too close to the boundary.

## 3. Finite response

What remains after the local subtraction.

And then a fourth:

## 4. Anomaly residue

What survives subtraction because it is structurally real.

If the engine separates those, your holography pipeline becomes much more powerful than “glow at the edges.”

---

# What I would build next in the codebase

* `src/glsl/fields/renorm_field.glsl`
* `src/js/holographyParams.js` updates for cutoff/counterterm controls
* `src/glsl/render/post_renorm_debug.glsl`

The next theory doc should be:

`docs/06_black_holes_and_horizons.md`

That one will give you:

* Bekenstein–Hawking area law as a render budget
* horizons as information membranes
* stretched horizon aesthetics
* Hawking glow analogs
* black-hole scene logic for shader art and raymarching

Say **next**.


Absolutely. This is the horizon file.

# `docs/06_black_holes_and_horizons.md`

```md id="k5w4fm"
# 06 — Black Holes and Horizons

## 6.1 Core idea

Black holes are where holography stops being a metaphor and becomes a hard limit.

The key fact is the Bekenstein–Hawking area law:

S_BH = A_hor / (4 G_N)

Entropy is proportional to the area of the horizon, not the volume hidden behind it.
That is the original shockwave behind holography:
the horizon acts like an information membrane.

For shader art, this gives the master rule:

horizon area = information budget

not:
interior volume = information budget

## 6.2 Why black holes matter for the repo

Black holes are not just one scene type.
They are the prototype for:
- surface-dominant encoding
- trapped visibility
- thermalized local information
- boundary readout limits
- horizon membranes
- reconstruction failure in hidden regions

This is where the repo learns how to make information disappear locally while remaining globally constrained.

## 6.3 Bekenstein–Hawking entropy

The basic formula is

S_BH = A_hor / (4 G_N)

The source material emphasizes that Bekenstein proposed black holes carry entropy to preserve the generalized second law, and Hawking’s discovery of black-hole radiation made them genuine thermodynamic systems with both temperature and entropy. :contentReference[oaicite:0]{index=0} :contentReference[oaicite:1]{index=1} :contentReference[oaicite:2]{index=2}

Repo translation:
- the horizon is an information-dense shell
- increasing horizon area increases allowed complexity
- the interior may be visually large, but the controlling data budget lives on the horizon

## 6.4 Generalized second law

A useful conceptual law is:

S_total = S_matter + S_BH

and this total should not decrease.

That is the conceptual machine behind the original entropy argument:
if an object with too much entropy could fit inside a region without violating area scaling, one could collapse it into a black hole and make entropy decrease, which is forbidden. This is part of the argument that the maximum entropy in a gravitating region is set by the entropy of the largest black hole that fits. :contentReference[oaicite:3]{index=3} :contentReference[oaicite:4]{index=4} :contentReference[oaicite:5]{index=5}

Visual translation:
- if a region gets “too information dense,” it should undergo horizon formation
- compression should produce a shell event, not infinitely increasing interior detail
- collapse scenes should increase shell dominance

## 6.5 Schwarzschild baseline

A standard Schwarzschild metric is

ds^2 = (1 - 2MG/r) dt^2 - (1 - 2MG/r)^(-1) dr^2 - r^2 dOmega^2

with horizon at

r_s = 2 M G

This is the cleanest baseline for black-hole scene logic: a spherical causal membrane dividing readable exterior from hidden interior. :contentReference[oaicite:6]{index=6}

## 6.6 Horizon as membrane

The horizon should not be rendered as a mere outline.
In holographic and membrane-paradigm language, it behaves like an information-bearing surface with thermodynamic properties. The complementarity literature explicitly ties this to the stretched horizon. :contentReference[oaicite:7]{index=7}

Repo rule:
- horizon = active shell
- shell stores heat, noise, glyphs, scrambling patterns, and readout artifacts
- the horizon is where infalling structure becomes thermally encoded from the outside point of view

## 6.7 Stretched horizon

The stretched horizon is an effective membrane placed just outside the true event horizon.
It is not the exact geometric horizon; it is the operational surface where an outside observer can attribute dynamics, temperature, and information processing.

This is one of the best visual devices in the whole repo:
- the true horizon is the ideal causal boundary
- the stretched horizon is the visible glowing skin your renderer actually works with

The source material explicitly references “the stretched horizon and black hole complementarity,” and Susskind’s earlier discussion describes the local state near the stretched horizon as thermal and largely insensitive to details of the infalling matter. :contentReference[oaicite:8]{index=8} :contentReference[oaicite:9]{index=9}

## 6.8 Black hole complementarity

The black-hole information puzzle led to black-hole complementarity:
preserve quantum unitarity, but give up naive assumptions about a single observer-independent local story for all events across the horizon.

For the repo, complementarity means:
- outside view: information appears processed, scrambled, and thermalized on or near the horizon
- infalling view: crossing may appear smooth or at least differently organized
- do not try to show both viewpoints as one naive Euclidean object

The TASI lectures directly frame black-hole complementarity as a counterproposal to information loss that preserves quantum mechanics while questioning naive locality and observer-independent spacetime events. :contentReference[oaicite:10]{index=10} :contentReference[oaicite:11]{index=11}

## 6.9 Thermalization and local forgetting

Susskind’s horizon discussion says the local state near the stretched horizon is thermal and has lost almost all detailed information about specific infalling matter, even if the full system is not fundamentally information-destroying. :contentReference[oaicite:12]{index=12}

That is huge for visuals.

It means:
- the horizon should locally erase detail
- fine object identity should smear into thermal texture
- global correlations may remain, but local appearance should look universal

## 6.10 Hawking radiation and horizon glow

Hawking radiation means black holes are thermodynamic emitters, not perfectly black sinks. The uploaded sources describe Hawking’s discovery as turning the thermodynamic analogy into physical reality. :contentReference[oaicite:13]{index=13} :contentReference[oaicite:14]{index=14}

Repo translation:
- the horizon should emit faint structured radiation
- not necessarily literal photons; any sparse flicker, thermal grain, or escaping spectral mist works
- black-hole scenes should breathe outward as well as absorb inward

## 6.11 AdS black holes and deconfinement

In holographic AdS/CFT settings, a high-temperature deconfined phase corresponds to a bulk black hole, while the low-temperature confined phase corresponds to horizonless AdS. The entanglement notes explicitly describe the Hawking–Page transition between global AdS and AdS-Schwarzschild black holes, with the order-\(N^2\) entropy in the deconfined phase accounted for by Bekenstein–Hawking entropy. :contentReference[oaicite:15]{index=15}

That gives the repo a gorgeous phase logic:
- cool phase: cathedral-like empty AdS
- hot phase: bulk horizon appears
- temperature can literally decide whether your scene contains a black hole

## 6.12 Horizon as minimal surface link

The entanglement notes motivate RT by pointing out that a static black-hole horizon is itself a minimal surface, and its area gives the thermal entropy. :contentReference[oaicite:16]{index=16} :contentReference[oaicite:17]{index=17}

Repo translation:
- horizons and entanglement membranes are cousins
- your horizon renderer and your RT-surface renderer should share code patterns
- a horizon can be treated as a special entropy membrane that encloses unreadable bulk

## 6.13 Reconstruction failure behind horizons

From the earlier bulk-reconstruction material:
- trapped null geodesics
- black-hole barriers
- evanescent modes

all degrade simple local boundary reconstruction.

So in black-hole scenes:
- the outside can only access coarse or thermalized information
- local interior readout should become unreliable near or behind the horizon
- hidden zones should feel causally screened, not merely darkened

This ties cleanly to the earlier warning that pure AdS smearing logic does not survive unchanged in black-hole backgrounds. :contentReference[oaicite:18]{index=18} :contentReference[oaicite:19]{index=19} :contentReference[oaicite:20]{index=20}

## 6.14 Visual laws to extract

### Law A — Area controls hidden complexity
Use horizon area to set detail budget.

### Law B — Horizon is an active shell
It glows, scrambles, absorbs, emits, and thermalizes.

### Law C — Exterior view loses local detail
Infalling forms should melt into universal shell texture.

### Law D — Interior readability collapses
Behind the horizon, local reconstruction confidence drops sharply.

### Law E — Emission is sparse but real
A black hole should leak structured thermal light, mist, or quanta.

### Law F — Phase changes matter
Black hole appearance can switch on as a temperature/deconfinement transition.

## 6.15 API vocabulary

- horizonRadius
- horizonAreaBudget
- stretchedHorizonWidth
- complementarityMix
- thermalScramble
- hawkingGlow
- absorptionStrength
- redshiftStrength
- interiorReadoutLoss
- deconfinementThreshold
- trappedModeWeight
- shellTemperature
- photonRingGain
- singularitySuppression

## 6.16 Failure modes to avoid

- black hole = matte black sphere
- no distinction between true horizon and stretched horizon
- interior treated as clearly visible ordinary volume
- infalling object passing through horizon with no scrambling or shell event
- no faint emission or thermal structure
- ignoring horizon-area control of complexity
```

Now the code side.

---

# Equations for `theory/equations.json`

```json id="jw2uys"
[
  {
    "id": "bekenstein_hawking_entropy",
    "equation": "S_BH = A_hor / (4 G_N)",
    "meaning": "Black-hole entropy is proportional to the horizon area"
  },
  {
    "id": "generalized_second_law",
    "equation": "S_total = S_matter + S_BH",
    "meaning": "Matter entropy plus black-hole entropy should not decrease"
  },
  {
    "id": "schwarzschild_metric",
    "equation": "ds^2 = (1 - 2MG/r) dt^2 - (1 - 2MG/r)^(-1) dr^2 - r^2 dOmega^2",
    "meaning": "Baseline static black-hole geometry"
  },
  {
    "id": "schwarzschild_radius",
    "equation": "r_s = 2 M G",
    "meaning": "Schwarzschild horizon radius"
  }
]
```

These are the right backbone equations for the repo: the Bekenstein–Hawking area law and the generalized-second-law reasoning are central to the original holographic argument, while the Schwarzschild form gives the basic horizon geometry used in the complementarity discussion.    

---

# How I would code this

## 1. Horizon area as detail budget

Do not pick black-hole detail density arbitrarily.
Drive it from horizon area.

```js id="aujr7n"
export function horizonArea(radius) {
  return 4.0 * Math.PI * radius * radius;
}

export function horizonDetailBudget(radius, k = 1.0) {
  return k * Math.sqrt(Math.max(horizonArea(radius), 1e-4));
}
```

Then use that budget for:

* shell glyph density
* scrambling-cell count
* photon-ring complexity
* shimmer frequency bands

That is the cleanest implementation of the area law. 

---

## 2. True horizon vs stretched horizon

Always render two radii.

```js id="d6g3zn"
export function makeHorizonRadii(rs, width = 0.06) {
  return {
    trueHorizon: rs,
    stretchedHorizon: rs * (1.0 + width)
  };
}
```

Why:

* `trueHorizon` handles causal logic
* `stretchedHorizon` handles visible shell shading, thermalization, and glow

This directly matches the complementarity / stretched-horizon picture.  

---

## 3. Horizon shell SDF in GLSL

```glsl id="reh2n8"
float sdSphere(vec3 p, float r) {
    return length(p) - r;
}

float horizonShell(vec3 p, float rs, float width) {
    float d = abs(sdSphere(p, rs * (1.0 + width)));
    return exp(-d * 60.0);
}
```

Then layer:

* shell heat
* glyph noise
* tangential drift
* sparse Hawking emission

---

## 4. Thermal scrambling field

The horizon should locally erase detail.
Turn infalling object identity into drifting thermal texture.

```glsl id="q2zc2l"
float scrambleNoise(vec3 p, float t) {
    return 0.5 + 0.5 * sin(13.0 * p.x + 17.0 * p.y + 19.0 * p.z + 3.0 * t);
}

float thermalScrambleField(vec3 p, float shellW, float t) {
    float n = scrambleNoise(p, t);
    return shellW * smoothstep(0.3, 0.8, n);
}
```

Mix this harder as objects approach the stretched horizon.

This matches the literature’s idea that the local horizon state becomes thermal and largely universal. 

---

## 5. Infalling object melt

```js id="ehv6xw"
export function infallIdentityLoss(distanceToHorizon, width) {
  const x = 1.0 - Math.max(0, distanceToHorizon / Math.max(width, 1e-4));
  return Math.min(1.0, Math.max(0.0, x * x));
}
```

Use it to fade:

* sharp normals
* object-specific color coding
* coherent internal structure

and replace them with:

* shell-aligned streaking
* thermal grain
* redshifted emission

---

## 6. Hawking glow / sparse emission

Do not make it a giant flame.
Make it delicate.

```glsl id="2wcf1x"
vec3 hawkingGlow(vec3 p, float shellW, float t) {
    float flicker = 0.5 + 0.5 * cos(7.0 * t + 20.0 * length(p));
    float sparse = pow(flicker, 6.0);
    return shellW * sparse * vec3(0.12, 0.22, 0.35);
}
```

This is the right visual analog of “not truly black but very slowly emit radiation.” 

---

## 7. Readout loss behind the horizon

You already built reconstruction confidence.
Now crush it near or inside the horizon.

```glsl id="g1pg48"
float interiorReadoutConfidence(vec3 p, float rs) {
    float r = length(p);
    return smoothstep(rs * 1.15, rs * 1.35, r);
}
```

Then:

```glsl id="7ggu4a"
float blackHoleReconstruction(float localValue, float nonlocalValue, float conf) {
    return conf * localValue + (1.0 - conf) * 0.25 * nonlocalValue;
}
```

That gives:

* exterior crispness
* near-horizon degradation
* interior shadowed nonlocal residue only

---

## 8. Redshift and photon-ring feel

Even if you are not doing real geodesic lensing, you can fake the right optical mood.

```glsl id="9m2x7b"
float redshiftFactor(float r, float rs) {
    return sqrt(max(1e-4, 1.0 - rs / max(r, rs + 1e-4)));
}

float photonRingBand(float r, float rs) {
    float ringR = 1.5 * rs;
    return exp(-abs(r - ringR) * 25.0);
}
```

Use:

* lower brightness frequencies near horizon
* boosted thin ring around `~1.5 rs`
* slight chromatic compression

This is not a full GR raytracer, but it pushes the look in the right direction.

---

## 9. AdS deconfinement switch

```js id="5mfqgh"
export function blackHolePhase(temperature, threshold) {
  return temperature > threshold ? "ads_black_hole" : "thermal_ads";
}
```

That lets the app swap from:

* empty thermal AdS
  to
* horizon-bearing AdS black hole

which mirrors the Hawking–Page phase structure described in the notes. 

---

# `theory/concepts.json` addition

```json id="ivq1hp"
{
  "id": "black_holes_and_horizons",
  "name": "Black Holes and Horizons",
  "physics_meaning": "Black holes realize the holographic area law most sharply: the entropy is set by horizon area, the horizon behaves as an information membrane, and local reconstruction degrades near and behind it.",
  "math_forms": [
    "S_BH = A_hor / (4 G_N)",
    "S_total = S_matter + S_BH",
    "ds^2 = (1 - 2MG/r) dt^2 - (1 - 2MG/r)^(-1) dr^2 - r^2 dOmega^2",
    "r_s = 2MG"
  ],
  "visual_translation": [
    "glowing information horizon",
    "stretched thermal membrane",
    "infalling identity scrambling",
    "sparse Hawking emission",
    "interior readout collapse",
    "deconfinement phase transition"
  ],
  "shader_parameters": [
    "horizonRadius",
    "horizonAreaBudget",
    "stretchedHorizonWidth",
    "complementarityMix",
    "thermalScramble",
    "hawkingGlow",
    "absorptionStrength",
    "redshiftStrength",
    "interiorReadoutLoss",
    "deconfinementThreshold",
    "trappedModeWeight",
    "shellTemperature",
    "photonRingGain",
    "singularitySuppression"
  ],
  "rendering_strategies": [
    "area-controlled shell complexity",
    "dual-radius horizon rendering",
    "thermal scrambling near shell",
    "confidence loss behind horizon",
    "phase-based black hole activation"
  ],
  "failure_modes": [
    "flat black sphere",
    "no stretched-horizon layer",
    "fully readable interior",
    "no thermalization of infalling structure",
    "no phase distinction between AdS and AdS-black-hole scenes"
  ]
}
```

---

# Example scene for this section

## `examples/06_horizon_membrane`

A floating AdS black hole with a razor-thin photon ring and a thick luminous stretched horizon made of drifting thermal glyphs. A humanoid or symbolic object falls inward. As it nears the stretched horizon, its identity dissolves into shell-aligned scanlines and heat cells. Outside observers see only a scrambling membrane with faint Hawking leakage. Inside the horizon, local geometry becomes unreadable except for dim nonlocal precursor residue.

### Scene law

* detail budget tied to horizon area
* stretched horizon brighter than true horizon
* object identity loss increases sharply near shell
* interior confidence collapse after crossing
* shell emits sparse cold-blue or ultraviolet Hawking flecks
* temperature parameter can switch between no-hole AdS and black-hole phase

This would be one of the flagship scenes of the repo.

---

# One distinction the repo should keep absolutely clear

There are three different black-hole visuals people mash together and you should not:

## 1. Causal horizon

The actual geometric threshold.

## 2. Stretched horizon

The operational information-processing membrane.

## 3. Singularity

Deep interior pathology or endpoint.

For most art code, you want to render **2** heavily, respect **1** in logic, and usually suppress or stylize **3** rather than center the whole scene on it.

That keeps the scene holographic instead of turning it into generic sci-fi gravity VFX.

---

# What I would build next in the codebase

* `src/glsl/sdf/blackhole_sdfs.glsl`
* `src/glsl/fields/horizon_scramble.glsl`
* `src/glsl/scenes/scene_horizon_hologram.glsl`

The next theory doc should be:

`docs/07_light_sheets_and_screens.md`

That one will give you:

* Bousso screens
* null congruences
* caustics
* light-sheet projection
* screen-based encoding strategies for shader architecture

Say **next**.

Yes. This is the **null-projection engine** file—the one that stops the repo from thinking holography is “flatten 3D onto 2D” and teaches it the actual deeper law:

**information projects along converging light-like directions**. Not arbitrary directions. Not naive spatial extrusion. Along **light-sheets** with nonpositive expansion.  

# `docs/07_light_sheets_and_screens.md`

```md id="yotmkf"
# 07 — Light-Sheets and Screens

## 7.1 Core idea

A hologram is not fundamentally a spacelike flattening of volume onto surface.
In the covariant formulation, holography projects along null directions.

Given a codimension-2 spatial surface B, there are four orthogonal null directions:
- future ingoing
- future outgoing
- past ingoing
- past outgoing

Only the directions with nonpositive expansion are allowed to generate light-sheets.
The entropy on each such light-sheet is bounded by the area of B:

S[L(B)] <= A(B) / 4

This is the covariant entropy bound.

## 7.2 What a light-sheet is

A light-sheet is a null hypersurface generated by orthogonal light rays whose expansion is nonpositive as one moves away from the initial surface.

Important:
- not every null direction is allowed
- only nonexpanding or contracting directions qualify
- the sheet terminates when the expansion becomes positive, or when a caustic, singularity, or spacetime boundary is reached

## 7.3 Why this matters for visuals

This means holographic encoding is:
- directional
- causal
- convergent
- truncated by geometry

The interior should therefore be built by:
- scanfronts
- inward null rays
- collapsing beam bundles
- caustic stopping surfaces
- truncated projection domains

not by uniform inward filling.

## 7.4 Covariant entropy bound

Let B be a connected codimension-2 surface of area A.
Let L be a hypersurface generated by one of the orthogonal null congruences of B.
If the expansion stays nonpositive away from B on L, then

S[L(B)] <= A / 4

This is the background-independent form of holography.
It applies in general spacetimes and is explicitly not the same thing as saying every spatial volume is bounded by the area of its boundary.

## 7.5 Spacelike holography is not general

A huge warning from the source literature:

holography is not, in general, a spacelike entropy bound.

There are counterexamples in cosmology, inside black holes, and even in weakly gravitating systems.
The covariant formulation avoids these failures because it only counts entropy on allowed light-sheets, not arbitrary spatial interiors.

Repo law:
never treat holography as “delete one spatial dimension.”
Treat it as null projection.

## 7.6 Screens

Now invert the question.

Instead of:
“Given a surface B, what light-sheet does it generate?”

ask:
“Given a null hypersurface, where should the information be projected?”

Follow the generators of a null hypersurface in a direction of nonnegative expansion.
Stop when the expansion becomes negative.
The spatial surface where the projection is terminated is called a screen.

So:
- light-sheet = valid contracting information flow
- screen = where projected information is collected or displayed

## 7.7 Preferred screens

If the expansion vanishes everywhere on the screen, the screen is called preferred.

Preferred screens are special because:
- they can encode information especially efficiently
- they often arise where expansion changes sign
- they are associated with apparent horizons and other balance surfaces
- they are candidates for saturating the holographic bound

Repo translation:
preferred screens are the universe’s best display surfaces.

## 7.8 Screen-hypersurfaces

If spacetime is sliced into a one-parameter family of null hypersurfaces, and each slice projects onto a screen, the collection of those screens forms a screen-hypersurface.

These can be:
- timelike
- null
- spacelike
- or even change causal character along the way

This is useful for animation:
a screen does not need to be one fixed shell.
It can be a moving family of display surfaces.

## 7.9 Apparent horizons and wedges

A practical recipe from the source material:

1. Draw a Penrose diagram.
2. Choose one family of null slices.
3. Identify apparent horizons where expansion vanishes.
4. These divide the spacetime into normal, trapped, and anti-trapped regions.
5. In each region, project along the local direction of negative expansion.
6. Where the projection direction flips, or where the geometry ends, you get screens.

This is gold for scene logic:
apparent horizons become pivot surfaces where projection behavior changes.

## 7.10 Caustics

Caustics matter.

The source literature explicitly points to caustics as one of the places where light-sheets terminate.
That means projection is not allowed to continue forever.
Ray bundles focus, fold, and lose their clean encoding role.

Repo translation:
- caustics should be stopping surfaces
- focused ray bundles should create bright compression membranes
- after a caustic, the clean holographic projection breaks down

## 7.11 Boundary subregions and light-sheet domains

For AdS boundary subregions, there is a beautiful refinement:
if b is a boundary region, one can define future and past light-sheet regions and then take their intersection

L(b) = L+(b) ∩ L-(b)

The nontrivial result is that this equals the causally connected bulk region:

H(b) = C(b) = L(b)

This is powerful for the repo:
the bulk domain assigned to a boundary region can be computed geometrically from light-sheets and causality together.

## 7.12 Slicing subtlety

The union of only future light-sheets or only past light-sheets depends on the time slicing.
That makes those constructions bad as unique holographic domains.
The intersection L+(b) ∩ L-(b), however, is slicing-independent in the cases analyzed.

Repo law:
when assigning a bulk region to a boundary patch, use symmetric past/future logic when possible.

## 7.13 Visual laws to extract

### Law A — Build with converging null beams
Projection should come from contracting light-like flow.

### Law B — Stop at caustics or sign flips
Projection is finite and geometry-limited.

### Law C — Screens are display membranes
Use screens as visible collection surfaces for bulk information.

### Law D — Preferred screens deserve emphasis
When expansion reaches zero, mark those surfaces as special.

### Law E — Boundary domains are wedge-like
A boundary patch should only control the bulk region selected by causal/light-sheet rules.

### Law F — Trapped regions change everything
Inside trapped or anti-trapped sectors, the available light-sheet directions shift.

## 7.14 API vocabulary

- nullExpansion
- congruenceFamily
- lightSheetDirection
- sheetTerminationMode
- causticGain
- screenThreshold
- preferredScreenBias
- apparentHorizonWeight
- trappedRegionMix
- subregionDomainMode
- futurePastIntersect
- screenLayerCount

## 7.15 Failure modes to avoid

- projecting inward equally in all directions
- treating screens as arbitrary decorative planes
- ignoring caustic termination
- assigning the whole bulk to every boundary patch
- confusing spatial slices with valid light-sheets
```

Now the code side.

The core claims in that file are all grounded in Bousso’s covariant entropy bound and screen construction, plus Rosenhaus’s light-sheet treatment for AdS subregions. Bousso defines light-sheets using orthogonal null congruences with nonpositive expansion, states the entropy bound (S \le A/4), explains screens and preferred screens, and gives the Penrose-diagram recipe using apparent horizons and regions of negative expansion. Rosenhaus emphasizes that holography is not generally spacelike, that light-sheets terminate appropriately, and that for suitable AdS boundary regions the bulk domain can be characterized by the slicing-independent intersection (L(b)=L^+(b)\cap L^-(b)), equal to the causally connected region (C(b)).       

---

# Equations to add to `theory/equations.json`

```json id="baw64r"
[
  {
    "id": "covariant_entropy_bound",
    "equation": "S[L(B)] <= A(B) / 4",
    "meaning": "Entropy on any allowed light-sheet of B is bounded by the area of B"
  },
  {
    "id": "holographic_dof_bound",
    "equation": "N_dof <= A / 4",
    "meaning": "Independent degrees of freedom on an allowed light-sheet are bounded by area"
  },
  {
    "id": "subregion_light_sheet_domain",
    "equation": "L(b) = L+(b) ∩ L-(b)",
    "meaning": "Boundary subregion domain from intersected past and future light-sheets"
  },
  {
    "id": "causal_light_sheet_equivalence",
    "equation": "H(b) = C(b) = L(b)",
    "meaning": "In the analyzed AdS cases, the holographic domain equals both the causal domain and the light-sheet domain"
  }
]
```

The first two come straight from Bousso’s covariant entropy conjecture and holographic principle in general spacetimes. The latter two come from Rosenhaus’s analysis of AdS subregion domains and the slicing-independent intersection of past and future light-sheets.  

---

# How I would code this

## 1. Null-congruence selector

Given a seed surface, do not just emit rays randomly.
Emit four candidate congruence families and keep only the valid ones.

### JS sketch

```js id="hv0t2m"
export function classifyNullDirections(surfacePoint, geomState) {
  const dirs = [
    "future_ingoing",
    "future_outgoing",
    "past_ingoing",
    "past_outgoing"
  ];

  return dirs.map((dir) => ({
    dir,
    expansion: estimateExpansion(surfacePoint, dir, geomState),
    allowed: estimateExpansion(surfacePoint, dir, geomState) <= 0
  }));
}
```

That is the actual holographic logic:
**allowed if expansion is nonpositive**. 

---

## 2. Light-sheet growth and termination

A light-sheet is not just “march rays until they hit something.”
It must stop when:

* expansion becomes positive
* a caustic forms
* a singularity or geometry boundary is reached

### JS pseudocode

```js id="5hu4xr"
export function marchLightSheet(seedRay, geom, params) {
  const points = [];
  let ray = seedRay;

  for (let i = 0; i < params.maxSteps; i++) {
    const p = ray.position;
    const theta = estimateExpansionAtRay(ray, geom);
    const caustic = detectCaustic(ray, geom);
    const boundary = detectGeometryBoundary(p, geom);

    if (theta > 0 || caustic || boundary) break;

    points.push(p);
    ray = advanceNullRay(ray, geom, params.stepSize);
  }

  return points;
}
```

This is the single most important difference between a real holographic projection analog and ordinary volumetric scanning.  

---

## 3. Screen finder

Invert the projection flow.
Follow a null hypersurface in the noncontracting direction and stop when the expansion is about to go negative.
That terminal surface is the screen; if expansion vanishes there, treat it as preferred.  

```js id="cp55td"
export function findScreenOnNullSlice(nullSlice, geom, eps = 1e-3) {
  const screenPoints = [];

  for (const generator of nullSlice.generators) {
    let g = generator;

    while (true) {
      const theta = estimateExpansionAtRay(g, geom);

      if (theta < 0) break;

      if (Math.abs(theta) < eps) {
        screenPoints.push({
          position: g.position,
          preferred: true
        });
        break;
      }

      g = advanceNullRay(g, geom, 0.01);
    }
  }

  return screenPoints;
}
```

---

## 4. Apparent-horizon projection wedges

Bousso’s Penrose-diagram recipe is begging to become an engine primitive:
each region gets a preferred projection direction based on which congruence has negative expansion. Apparent horizons are where the direction flips. 

### Data model

```js id="v11qar"
export const regionProjectionRule = {
  NORMAL: { preferredDirections: ["future_ingoing", "past_ingoing"] },
  TRAPPED: { preferredDirections: ["future_ingoing", "future_outgoing"] },
  ANTI_TRAPPED: { preferredDirections: ["past_ingoing", "past_outgoing"] }
};
```

Then every scene can switch its beam logic by region type instead of one global rule.

---

## 5. GLSL light-sheet density field

This gives you the visible scanfront.

```glsl id="heefb3"
float lightSheetDensity(vec3 p, vec3 origin, vec3 dir, float focus, float falloff) {
    vec3 q = p - origin;
    float axial = dot(q, dir);
    float radial = length(q - axial * dir);

    float beam = exp(-radial * focus);
    float range = exp(-max(axial, 0.0) * falloff);

    return beam * range * step(0.0, axial);
}
```

Use multiple beam families and suppress the ones whose estimated expansion is positive.

---

## 6. Caustic membrane rendering

Caustics should not just be bright spots.
They should act like **compression membranes** where the projection terminates or degrades. Bousso and Bigatti–Susskind both treat caustics as genuine structural stopping points for light-sheet constructions.  

```glsl id="2xp4x4"
float causticMembrane(vec3 p, vec3 center, float radius, float sharpness) {
    float d = abs(length(p - center) - radius);
    return exp(-d * sharpness);
}
```

Good uses:

* glowing focal shells
* projection cutoff surfaces
* beam-fan collapse zones
* screen candidates

---

## 7. Preferred-screen emphasis

When `theta ~ 0`, make the renderer treat that surface as extra coherent and information-dense.

```glsl id="ck5f3m"
float preferredScreenWeight(float theta, float width) {
    return exp(-abs(theta) / max(width, 1e-4));
}
```

Drive:

* glyph stability
* edge brightness
* membrane thickness
* symbol legibility

That is the right visual analog of a preferred screen.  

---

## 8. Boundary subregion bulk mask

For boundary-patch control, do not let a patch own the whole bulk.
Use a domain mask inspired by the (L(b)=L^+(b)\cap L^-(b)) logic.  

### JS idea

```js id="sv3hhi"
export function subregionBulkMask(p, futureMask, pastMask) {
  return futureMask(p) * pastMask(p);
}
```

### Interpretation

* in future mask only: not enough
* in past mask only: not enough
* in both: belongs to the stable holographic domain of the patch

That gives you a very clean API for boundary-driven scenes.

---

## 9. Screen-layer stack

A gorgeous art-engine move:
build not one screen, but a family of screens along a null slicing.

```js id="wuk9k8"
export function generateScreenStack(nullSlices, geom) {
  return nullSlices.map((slice) => findScreenOnNullSlice(slice, geom));
}
```

This gives you:

* layered transparent membranes
* flipbook-like projection volumes
* animated “space paints itself” sequences
* cosmology-style nested screens

And that is directly in the spirit of Bousso’s screen-hypersurfaces. 

---

# `theory/concepts.json` addition

```json id="a5o8h3"
{
  "id": "light_sheets_and_screens",
  "name": "Light-Sheets and Screens",
  "physics_meaning": "Holographic information is projected along null congruences with nonpositive expansion; screens collect this information, and preferred screens occur where expansion vanishes.",
  "math_forms": [
    "S[L(B)] <= A(B) / 4",
    "N_dof <= A / 4",
    "L(b) = L+(b) ∩ L-(b)",
    "H(b) = C(b) = L(b)"
  ],
  "visual_translation": [
    "converging null scanfronts",
    "caustic termination membranes",
    "preferred information screens",
    "screen-hypersurface layer stacks",
    "boundary wedge ownership masks"
  ],
  "shader_parameters": [
    "nullExpansion",
    "congruenceFamily",
    "lightSheetDirection",
    "sheetTerminationMode",
    "causticGain",
    "screenThreshold",
    "preferredScreenBias",
    "apparentHorizonWeight",
    "trappedRegionMix",
    "futurePastIntersect",
    "screenLayerCount"
  ],
  "rendering_strategies": [
    "allowed-direction null ray marching",
    "expansion-based termination",
    "screen extraction from null slices",
    "caustic membrane rendering",
    "subregion domain intersection masks"
  ],
  "failure_modes": [
    "uniform inward projection",
    "screens as arbitrary decorative quads",
    "ignoring caustics",
    "one boundary patch controlling all bulk points",
    "confusing spatial extrusion with light-sheet projection"
  ]
}
```

---

# Example scene for this section

## `examples/07_null_projection_cathedral`

A dark warped bulk containing a suspended seed surface. From the seed, only the allowed null directions emit contracting beam-fans. Those beams converge into luminous caustic membranes that act as screens. A stack of preferred screens forms a floating cathedral of transparent glyph sheets. Boundary arcs control only wedge-shaped bulk regions given by future/past intersection masks, so the scene feels causally partitioned instead of globally smeared.

### Scene law

* only nonpositive-expansion directions render full beams
* positive-expansion directions either vanish or become weak guide lines
* caustics terminate projection
* `theta ~ 0` surfaces become readable glyph membranes
* screen stacks create the “bulk as layered projection” look
* boundary subregions own only their admissible wedges

That would be one of the most holographically faithful scenes in the whole repo.

---

# One distinction the repo should keep brutally clear

There are **three different surfaces** here:

## 1. Seed surface `B`

Where the null congruence starts.

## 2. Light-sheet `L(B)`

The contracting null hypersurface generated from `B`.

## 3. Screen

Where projection ends or is collected when you invert the logic.

Do not blur these together. A lot of fake holography does exactly that.

---

# What I would build next in the codebase

* `src/glsl/fields/light_sheet_field.glsl`
* `src/js/nullGeodesicCamera.js`
* `src/glsl/scenes/scene_light_sheet_screen.glsl`

The next theory doc should be:

`docs/08_nonlocality_and_smearing.md`

That one will go even deeper into:

* precursors
* nonlocal boundary operators
* evanescent modes
* trapped null geodesics
* why some deep-bulk structures can only be rendered through global controls

Say **next**.

Hell yes. This is the **“spooky bulk control”** file — where the repo admits that some interior structure is not recoverable from clean local boundary paint, and has to be driven by **distributed kernels, hidden global modes, precursor channels, and coarse-grained readout**. The sources are very explicit about the hierarchy here: in pure global AdS, local smearing can work; in black-hole or barrier backgrounds, trapped null geodesics and evanescent modes break that simple story; and in dynamical situations, **precursors** are the nonlocal operators that can encode bulk events before local one-point data can.

# `docs/08_nonlocality_and_smearing.md`

```md id="59hjfs"
# 08 — Nonlocality and Smearing

## 8.1 Core idea

A bulk point is not generally controlled by one boundary point.
Even in the nicest cases, a local bulk operator is reconstructed by smearing boundary operators over a region of space and time.

In favorable backgrounds:
bulk point = distributed boundary integral

In hostile backgrounds:
- the smearing kernel may fail to exist
- local boundary data may be insufficient
- deep-bulk events may require nonlocal operators
- exact reconstruction may need exponential precision
- only coarse-grained reconstruction may survive

For the repo, this means:
some interior forms should be driven by local kernels,
some by global hidden modes,
and some only by blurred or approximate readout.

## 8.2 Smearing function as the local reconstruction engine

For a free scalar in the large-N limit, one can expand the bulk field in modes,
identify the asymptotic boundary tail with a local operator O,
and derive

Phi(B) = ∫ db' K(B|b') O(b')

where K is the smearing kernel.

This is the cleanest programmable bulk/boundary map:
- B = bulk point
- b' = boundary points
- K = distributed responsibility profile

## 8.3 What the smearing kernel really means

A smearing kernel is not a blur filter in the graphics sense.
It is a statement that the bulk point can be reconstructed continuously from local boundary data spread over a region.

This region can be:
- large in time
- large in angle/position
- background-dependent
- nonunique in representation

Repo translation:
every reconstructible bulk point should have a "responsibility cloud" on the boundary.

## 8.4 Global AdS is the easy kingdom

In pure global AdS, local boundary operators over sufficient time can reconstruct the bulk.
The source material notes that measuring the local operator over the whole sphere and a long enough time interval lets observers distinguish the mode coefficients and reconstruct the field.

That is your clean local-smear mode:
- stable
- exact in principle
- no hidden global rescue needed

## 8.5 Precursors

If AdS/CFT is complete, there should exist highly nonlocal boundary operators — precursors —
that can determine bulk information at one instant of time even when local one-point data cannot.

The source text is very sharp here:
if two wave packets collide in the center of AdS, causality prevents the result from reaching local boundary readout for a time interval of pi/2.
During that interval, the collision outcome is encoded only in the precursors.

Repo translation:
precursors are hidden global channels that know the bulk too early.

## 8.6 Nonlocal operators and subregions

Rosenhaus emphasizes that the CFT definition of local bulk operators involves nonlocal boundary operators called precursors, and that explicit constructions in global AdS use an entire Cauchy surface of the boundary.

That means:
- local-looking bulk events can require globally spread boundary data
- subregion access is delicate
- restricting the boundary region can force the reconstruction to become ambiguous or impossible

## 8.7 Smearing can fail

The smearing-function approach works in pure global AdS but can fail when there are bulk modes whose boundary imprint is arbitrarily small compared with their interior value.

In those cases:
- a bulk field can be huge at B
- its regulated boundary tail can be exponentially tiny
- the mode sum defining K diverges
- local boundary reconstruction breaks

This is not a small technicality.
It is the moment where the repo must switch from "local smear" to "nonlocal/approximate mode."

## 8.8 Trapped null geodesics

The source gives a powerful geometric diagnostic:
for a large class of static backgrounds, if there are trapped null geodesics, then there is no smearing function for some bulk region.

In pure AdS, null geodesics reach the boundary.
In AdS-Schwarzschild and related backgrounds, some do not.
That is enough to kill the clean smearing map for some points.

Repo law:
if light cannot escape to the boundary, local readout should degrade or fail.

## 8.9 Large-l barriers

The same source explains that in AdS black-hole backgrounds, high angular momentum modes can sit behind a centrifugal barrier.
Their large-r imprint decays exponentially with l, even while they remain legitimate bulk modes.

So:
- small deep barriers can corrupt reconstruction far more than naive distance would suggest
- even a small dense star can obstruct smearing for some region
- the boundary may "see" almost nothing of a real bulk structure

## 8.10 Evanescent modes

Evanescent modes are the real demons here.

In pure AdS, the modes relevant for reconstruction are propagating and exact reconstruction works.
In black-hole or inhomogeneous backgrounds, evanescent modes appear.
These grow exponentially in the interior relative to their boundary imprint.

Then:
- exact reconstruction from boundary data requires exponential precision
- without that precision, you cannot reconstruct sharply
- you may still reconstruct a coarse-grained field

## 8.11 Coarse-graining bound

The source makes the STM analogy explicit:
if evanescent modes are present, reconstruction without exponential precision is possible only after averaging parallel to the boundary over a scale sigma, and then only out to depth z < sigma.

That is one of the most usable coding rules in the whole repo.

Repo translation:
resolution scale on boundary controls recoverable depth in bulk.

## 8.12 STM analogy

The source compares this directly to scanning tunneling microscopy.
You can capture evanescent information only by getting close enough and accepting a finite resolution scale.

For the repo:
- deep bulk hidden detail should require near-screen probes
- a global camera should only recover blurred structure
- near-probe shaders can reveal substructure that distant view cannot

## 8.13 Background dependence

The definition of local bulk operators depends on the background metric.
Rosenhaus explicitly warns that the operator reconstruction story is background-sensitive.

Repo law:
do not assume one universal kernel.
Kernel family should depend on scene geometry:
- pure AdS
- wedge/subregion
- horizon background
- dense star / barrier
- black brane atmosphere

## 8.14 What to do when K fails

When the smearing kernel fails, you still have options:
- approximate smearing with cutoffs
- coarse-grained reconstruction
- nonlocal precursor channels
- complexified-boundary tricks
- state-specific reconstruction instead of universal pointwise recovery

Repo translation:
do not throw the scene away.
Downgrade the readout mode.

## 8.15 Visual laws to extract

### Law A — Bulk responsibility is distributed
One point can depend on a large support set on the boundary.

### Law B — Deep structure may be globally encoded
Use hidden long-range channels, not only local patches.

### Law C — Some scenes should be unrecoverably blurry
That is not failure. That is fidelity.

### Law D — Resolution and depth trade off
Finer bulk readout requires finer or closer boundary measurement.

### Law E — Barriers and horizons kill naive locality
If the geometry traps modes, switch rendering logic.

### Law F — Early bulk knowledge belongs to precursor channels
Let some interior events appear before local boundary evidence catches up.

## 8.16 API vocabulary

- smearKernelType
- smearSupportRadius
- timeSupportWidth
- nonlocalMix
- precursorWeight
- kernelFailureMode
- trappedGeodesicBias
- evanescentWeight
- reconstructionSigma
- recoverableDepth
- boundaryResponsibilityCloud
- coarseGrainMode
- globalHiddenModes
- earlyKnowledgeGain

## 8.17 Failure modes to avoid

- every bulk point controlled by one UV coordinate
- exact crisp readout in black-hole atmospheres
- treating precursors as decorative lore instead of a control path
- no blur/depth tradeoff when evanescent modes are active
- using one universal smearing kernel for all geometries
```

The core equations and claims above come directly from the reconstruction chapters in Rosenhaus. He gives the mode-sum smearing formula, explains that it works in pure AdS, and shows it can fail when bulk modes have exponentially tiny boundary tails. He also states the geometric diagnostic that trapped null geodesics imply no smearing function for some bulk region, and that evanescent modes force either exponential precision or coarse-grained recovery with depth bounded by the averaging scale.

---

# Equations to add to `theory/equations.json`

```json id="y6idlc"
[
  {
    "id": "smearing_function",
    "equation": "Phi(B) = ∫ db' K(B|b') O(b')",
    "meaning": "Bulk field reconstructed from local boundary operators via a smearing kernel"
  },
  {
    "id": "mode_sum_kernel",
    "equation": "K(B|b') = ∫ dk Phi_k(B) phi_k*(b') + h.c.",
    "meaning": "Kernel built from bulk modes and their boundary tails"
  },
  {
    "id": "geodesic_potential",
    "equation": "f h r_dot^2 + L^2 f / r^2 = E^2 ; U = L^2 f / r^2",
    "meaning": "Null geodesics move in an effective potential whose trapping diagnoses smearing failure"
  },
  {
    "id": "trapped_mode_criterion",
    "equation": "d/dr (f/r^2) > 0",
    "meaning": "Large-l criterion associated with trapped modes and smearing failure in static spherical backgrounds"
  },
  {
    "id": "evanescent_mode",
    "equation": "J_nu(i kappa z) ~ I_nu(kappa z) ~ exp(kappa z)",
    "meaning": "Evanescent modes grow exponentially into the bulk"
  },
  {
    "id": "coarse_grain_depth_bound",
    "equation": "z < sigma",
    "meaning": "With evanescent modes, coarse-grained reconstruction at boundary scale sigma only resolves bulk out to depth z smaller than sigma"
  }
]
```

The smearing formula and kernel expression appear in the mode-sum derivation of the bulk-boundary map. The null-geodesic potential (U=L^2 f/r^2) is used to diagnose trapped geodesics, and Rosenhaus states that trapped null geodesics imply no smearing function for some bulk region. He also gives the large-(l) trapped-mode criterion (d(f/r^2)/dr>0) and explains that evanescent modes behave like modified Bessel growth and destroy exact sharp reconstruction unless one coarse-grains so that only depths (z<\sigma) are targeted.

---

# How I would code this

## 1. Multi-mode reconstruction system

Do not expose one reconstruction function.
Expose **four** modes:

* `LOCAL_SMEAR`
* `PRECURSOR_ASSISTED`
* `COARSE_GRAINED`
* `FAILED_LOCAL_READOUT`

```js id="6elatq"
export const reconstructionModes = {
  LOCAL_SMEAR: "local_smear",
  PRECURSOR_ASSISTED: "precursor_assisted",
  COARSE_GRAINED: "coarse_grained",
  FAILED_LOCAL_READOUT: "failed_local_readout"
};
```

That matches the actual theory much better than pretending everything is a clean kernel.

---

## 2. Responsibility cloud, not UV point lookup

```js id="q4rm1j"
export function boundaryResponsibilityCloud(bulkPoint, boundarySamples, kernel) {
  let total = 0;
  const weights = boundarySamples.map((b) => {
    const w = kernel(bulkPoint, b);
    total += w;
    return { b, w };
  });

  return weights.map(({ b, w }) => ({
    b,
    w: total > 0 ? w / total : 0
  }));
}
```

This is your actual “smearing support” object.

Use it to drive:

* boundary HUDs
* glow arcs
* bulk ownership maps
* API summaries for prompts

---

## 3. Precursor channel

The source says precursors are highly nonlocal operators and can encode a central bulk collision before local boundary data can show it. That is exactly the repo’s excuse to add a **global hidden mode layer** that can trigger deep events early.

```js id="l8jlzv"
export function precursorField(boundaryState, p, t, params) {
  const lowModes = boundaryState.fourierBands("global_low");
  const graphModes = boundaryState.entanglementModes();
  const temporalLead = Math.sin(params.precursorFreq * t - params.leadBias * p.y);

  return params.precursorWeight * (
    0.45 * lowModes.sample(p) +
    0.35 * graphModes.sample(p) +
    0.20 * temporalLead
  );
}
```

Key behavior:

* precursor contribution can spike before local kernel confidence does
* precursor layer should be broad, ghostly, and nonlocal
* precursor layer should never look like a crisp UV projection

---

## 4. Local smearing kernel with failure test

```js id="dl32jp"
export function localSmear(p, t, boundarySamples, sourceMap, params) {
  let acc = 0;
  let norm = 0;

  for (const s of boundarySamples) {
    const dx = Math.hypot(p.x - s.x, p.z - s.z);
    const dt = Math.abs(t - s.t);
    const w =
      Math.exp(-dx / params.smearSupportRadius) *
      Math.exp(-dt / params.timeSupportWidth);

    acc += w * sourceMap(s.u, s.v, s.t);
    norm += w;
  }

  return norm > 0 ? acc / norm : 0;
}

export function chooseReconstructionMode(geomFlags) {
  if (geomFlags.hasTrappedGeodesics || geomFlags.hasStrongEvanescence) {
    return reconstructionModes.COARSE_GRAINED;
  }
  if (geomFlags.requiresEarlyBulkKnowledge) {
    return reconstructionModes.PRECURSOR_ASSISTED;
  }
  return reconstructionModes.LOCAL_SMEAR;
}
```

This is the place where your geometry flags start mattering.

---

## 5. Trapped-geodesic diagnostic

Rosenhaus gives a very concrete diagnostic in static spherical backgrounds: trapped null geodesics correlate with failure of the smearing function, and in the large-(l) regime one useful criterion is (d(f/r^2)/dr>0) somewhere.

```js id="0qec9y"
export function trappedGeodesicDiagnostic(sampleRadii, fOfR) {
  for (let i = 1; i < sampleRadii.length - 1; i++) {
    const r0 = sampleRadii[i - 1];
    const r1 = sampleRadii[i];
    const r2 = sampleRadii[i + 1];

    const y0 = fOfR(r0) / (r0 * r0);
    const y1 = fOfR(r1) / (r1 * r1);
    const y2 = fOfR(r2) / (r2 * r2);

    const derivative = (y2 - y0) / (r2 - r0);
    if (derivative > 0) return true;
  }
  return false;
}
```

When this flips on:

* local kernel confidence should drop
* hidden modes should rise
* deep bulk should become “seen only globally”

---

## 6. Evanescent-mode coarse graining

The STM analogy is too good not to implement directly: if evanescent modes are active, you can still recover something, but only after coarse-graining over a boundary scale (\sigma), and only to depth (z<\sigma).

```js id="3wt4zw"
export function recoverableDepthFromSigma(sigma) {
  return sigma;
}

export function coarseGrain2D(field2D, sigmaPx) {
  // placeholder: Gaussian blur / mip selection / FFT low-pass
  return gaussianBlur(field2D, sigmaPx);
}
```

Render law:

* if `depth > sigma`, switch to unresolved fog / macrostructure only
* if `depth <= sigma`, allow blurred reconstruction
* make this visible in debug view

---

## 7. GLSL evanescent field

```glsl id="23aiff"
float evanescentGrowth(float depth, float kappa) {
    return exp(kappa * depth);
}

float evanescentSuppressionAtBoundary(float k, float T, float alpha) {
    return exp(-alpha * abs(k) / max(T, 1e-4));
}
```

Use this split:

* boundary imprint tiny
* interior amplitude huge
* visual result = hidden bulk swells that barely flicker on the shell

That’s the right mood for black-hole atmosphere modes.

---

## 8. Dual-view renderer: exact vs recoverable

```js id="v2x7w2"
export function reconstructBulk(p, t, state, params) {
  const local = localSmear(p, t, state.boundarySamples, state.sourceMap, params);
  const precursor = precursorField(state.boundaryState, p, t, params);

  if (state.mode === reconstructionModes.LOCAL_SMEAR) {
    return local;
  }

  if (state.mode === reconstructionModes.PRECURSOR_ASSISTED) {
    return 0.65 * local + 0.35 * precursor;
  }

  if (state.mode === reconstructionModes.COARSE_GRAINED) {
    const depthPenalty = smoothstep(
      params.recoverableDepth,
      params.recoverableDepth * 1.4,
      p.y
    );
    return (1.0 - depthPenalty) * (0.4 * local + 0.6 * precursor);
  }

  return 0.15 * precursor;
}
```

This lets the same scene degrade gracefully instead of snapping from “works” to “broken.”

---

## 9. Boundary HUD for hidden-bulk logic

This repo should absolutely have a debug display with:

* support cloud size
* precursor activity
* trapped-geodesic flag
* evanescent weight
* recoverable depth
* local confidence

That will make scene design ten times easier.

```js id="sctn0i"
export function bulkReadoutDiagnostics(state) {
  return {
    mode: state.mode,
    hasTrappedGeodesics: state.geomFlags.hasTrappedGeodesics,
    evanescentWeight: state.geomFlags.evanescentWeight,
    recoverableDepth: state.params.recoverableDepth,
    precursorWeight: state.params.precursorWeight
  };
}
```

---

# `theory/concepts.json` addition

```json id="svuybs"
{
  "id": "nonlocality_and_smearing",
  "name": "Nonlocality and Smearing",
  "physics_meaning": "Local bulk reconstruction from boundary operators can be expressed through smearing kernels in favorable backgrounds, but trapped null geodesics, barriers, and evanescent modes can destroy exact local readout and force nonlocal or coarse-grained reconstruction.",
  "math_forms": [
    "Phi(B) = ∫ db' K(B|b') O(b')",
    "K(B|b') = ∫ dk Phi_k(B) phi_k*(b') + h.c.",
    "U = L^2 f / r^2",
    "d/dr (f/r^2) > 0",
    "J_nu(i kappa z) ~ I_nu(kappa z) ~ exp(kappa z)",
    "z < sigma"
  ],
  "visual_translation": [
    "responsibility clouds on the boundary",
    "precursor ghost channels",
    "deep bulk hidden behind tiny shell imprint",
    "coarse-grained recoverable zones",
    "STM-like near-probe reveal"
  ],
  "shader_parameters": [
    "smearKernelType",
    "smearSupportRadius",
    "timeSupportWidth",
    "nonlocalMix",
    "precursorWeight",
    "kernelFailureMode",
    "trappedGeodesicBias",
    "evanescentWeight",
    "reconstructionSigma",
    "recoverableDepth",
    "boundaryResponsibilityCloud",
    "coarseGrainMode",
    "globalHiddenModes",
    "earlyKnowledgeGain"
  ],
  "rendering_strategies": [
    "mode-switched reconstruction",
    "precursor-assisted bulk events",
    "trapped-geodesic diagnostics",
    "evanescent-mode coarse graining",
    "support-cloud debug overlays"
  ],
  "failure_modes": [
    "one UV point drives one bulk point",
    "no geometry-dependent kernel switching",
    "perfect crisp readout in barrier backgrounds",
    "precursors treated as cosmetic instead of structural",
    "no depth-resolution tradeoff"
  ]
}
```

---

# Example scene for this section

## `examples/08_precursor_chamber`

A dark AdS chamber with a boundary ring of dim local operator glyphs. Deep in the bulk, an event begins: two hidden structures collide and generate a luminous crystalline knot. At first, the knot is visible only through a broad, low-frequency precursor aura distributed over the entire boundary. Local boundary cells show almost nothing. Later, responsibility clouds form, the local smearing kernel stabilizes, and the knot sharpens. If a black-hole atmosphere or dense barrier is introduced, evanescent modes activate: the knot can only be recovered as a blurred macroscopic blob unless the “probe” moves close enough or the reconstruction sigma increases.

### Scene law

* early phase: precursor only
* mid phase: precursor + local smearing interfere
* hostile geometry: local sharpness collapses
* coarse-grain slider directly controls recoverable depth
* debug HUD shows support cloud, trapped-geodesic flag, and evanescent weight

This would be one of the most conceptually badass scenes in the repo.

---

# One distinction the repo should keep razor sharp

There are **three different reasons** local-looking boundary data can fail you:

## 1. Causality delay

The event has happened in the bulk but the local boundary operator has not had time to know yet.
That is the precursor regime.

## 2. Geometric trapping

Null geodesics do not escape cleanly to the boundary.
That is the trapped-geodesic / no-smearing regime.

## 3. Evanescent amplification

The boundary imprint is exponentially tiny compared with interior amplitude.
That is the coarse-graining / STM regime.

If you keep those separate, the repo gets much smarter.

---

# What I would build next in the codebase

* `src/glsl/fields/operator_smear.glsl`
* `src/glsl/fields/precursor_field.glsl`
* `src/js/boundaryResponsibility.js`
* `src/js/reconstructionModes.js`

The next theory doc should be:

`docs/09_cosmology_and_holographic_matter.md`

That one will open up:

* cosmological holographic bounds
* future horizons
* holographic dark energy / matter vibes
* expanding-screen aesthetics
* universe-scale scene logic

Say **next**.


Perfect. Now we move from black holes and AdS chambers into **cosmic-scale holography**: expanding universes, future horizons, de Sitter screens, and holographic matter as a programmable field. This is where the repo learns how to render **an evolving boundary budget** instead of one fixed shell.

# `docs/09_cosmology_and_holographic_matter.md`

```md id="i0t9z8"
# 09 — Cosmology and Holographic Matter

## 9.1 Core idea

In cosmology, holography becomes more subtle than in static AdS.
The universe expands, horizons move, and the allowed holographic screens can be:
- spacelike
- null
- timelike
- or a changing family of embedded screens

This means the repo must support:
- time-dependent screen area
- expanding or contracting information budgets
- future-horizon-driven fields
- cosmological light-sheet truncation
- dynamic activation and deactivation of degrees of freedom

## 9.2 FRW / FLRW as the base cosmology

A Friedmann–Robertson–Walker cosmology can be written as

ds^2 = a^2(η) [ -dη^2 + dχ^2 + f^2(χ) dΩ^2 ]

with
f(χ) = sinh χ, χ, sin χ

for open, flat, and closed universes.

This is the base metric for cosmological holography:
geometry is no longer static,
so screen placement and allowed light-sheets depend on the scale factor.

## 9.3 Why light-sheets matter even more in cosmology

The cosmology sources are explicit:
a spatial region can easily contain entropy that exceeds its area if you count all of it.
The bound works only when you count entropy passing through the appropriate light-sheet, which may be truncated by the big bang or other spacetime boundaries.

Repo law:
cosmological holography must be rendered as
time-limited null capture,
not naive volume encoding.

## 9.4 de Sitter space as the cosmic holography laboratory

A flat slicing of de Sitter can be written as

ds^2 = -dt^2 + e^(2Ht)(dr^2 + r^2 dΩ^2)

with
H = sqrt(Λ/3)

de Sitter is crucial because it gives two major screen ideas:

1. Global null projection:
the full spacetime can be projected using past and future infinity as optimal screens.

2. Observer-horizon projection:
for one observer, the event horizon E is a preferred null screen of constant area

A(E) = 4π H^(-2)

that encodes the observable half of de Sitter space.

## 9.5 Cosmological screens are not always boundary screens

Bousso’s construction shows that cosmological spacetimes may have embedded screens, not just asymptotic boundaries.
Some screens are spacelike, some null, some timelike.
Their area may vary with time.

This is one of the most important repo-level upgrades:
your holographic display surface may move through the scene and change size.

## 9.6 Varying active degrees of freedom

Bousso explicitly argues that in general cosmological situations one should not expect a conventional theory with a fixed number of degrees of freedom.
Instead, the effective number of active degrees of freedom can vary with the screen area.

Repo translation:
screen area should directly modulate:
- visible complexity
- symbol count
- sample count
- entanglement channel count
- ray budget
- procedural layer count

Cosmic scenes should breathe their information capacity.

## 9.7 Holographic matter from the UV/IR bound

The cosmological holographic-matter notes derive a classic bound:
if a region of size L has UV cutoff Λ,
one should require the total vacuum energy not to exceed the mass of a black hole of the same size.

This leads to

ρ ~ M_p^2 L^(-2)

and the standard holographic matter / dark-energy form

ρ_HM = 3 C^2 M_p^2 / L^2

with C of order one.

This is the central field law for cosmic holographic rendering:
energy density is set by an IR length scale.

## 9.8 Future event horizon as the IR scale

A key move in holographic dark energy is to choose

L_f = a(t) ∫_t^∞ dt' / a(t')

as the characteristic length scale.

Then the dark-energy density becomes controlled by the future event horizon rather than the instantaneous Hubble radius.

This is visually extraordinary:
the present field depends on a future-defined screen.

Repo translation:
the scene can be shaped by a horizon that is not merely local,
but teleological / anticipatory in structure.

## 9.9 Why the future horizon matters

The notes emphasize that taking the particle horizon or Hubble scale as L does not correctly produce a cosmological-constant-like equation of state.
A de Sitter solution appears naturally when the future horizon is used, with the special choice corresponding to a de Sitter phase.

Repo law:
if you want genuinely dark-energy-like cosmic expansion aesthetics,
future-horizon logic is the preferred mode.

## 9.10 Holographic dark energy dynamics

In a universe with matter plus holographic dark energy:

3 M_p^2 H^2 = ρ_m + ρ_de

and the fractional dark-energy density obeys

dΩ_de / d ln a = Ω_de (1 - Ω_de) ( 1 + 2 sqrt(Ω_de) / C )

while the equation of state is

w = -1/3 - (2/3) sqrt(Ω_de) / C

This gives the repo an evolving cosmic shader parameter:
the relation between expansion, horizon length, and dark-energy dominance changes over time.

## 9.11 Entanglement-entropy route to holographic matter

The notes also derive holographic matter from horizon entanglement entropy.
If

S_ent ~ β N_dof L_h^2 / l^2

and one uses the Gibbons–Hawking temperature

T = 1 / (2π L_h)

then the associated energy density again gives

ρ_de = 3 M_p^2 C^2 / L_h^2

This is incredibly usable for art:
cosmic energy density can be rendered as horizon entanglement pressure.

## 9.12 Entropic-force route

A separate route uses Verlinde-style entropic force ideas:
the future event horizon carries degrees of freedom and temperature,
leading again to an induced holographic matter component of order

ρ_HM ~ 3 C^2 M_p^2 / L_h^2

Repo translation:
cosmic expansion fields can be treated as horizon-pressure or horizon-tension effects,
not just scalar fog.

## 9.13 Action-principle route and dark radiation

The action-principle derivation introduces a cutoff variable L and a Lagrange multiplier λ.
This yields

L = ∫_t^∞ dt' / a(t')

and an energy density

ρ_HM = M_p^2 [ C / (a^2 L^2) + λ / (2 a^4) ]

The λ(a=0) piece evolves like radiation and is naturally interpreted as dark radiation.

This is amazing for the repo:
you get two visual channels:
- future-horizon energy
- primordial dark-radiation residue

## 9.14 Inflation and holographic matter

The cosmology notes say holographic matter can act either as dark energy or in inflationary scenarios, but not both in the same unconstrained way.
If it contributes during early inflation, it modifies the primordial spectrum and can affect low-l CMB structure.

Repo translation:
use holographic matter during inflation to create:
- suppressed large-angle modes
- horizon-scale cutoff bands
- primordial IR scars
- huge early screen pressure

## 9.15 Visual laws to extract

### Law A — Cosmic information budget changes with time
Let complexity track screen area and horizon size.

### Law B — Future horizons can control present form
Allow anticipatory or teleological field logic.

### Law C — Expanding screens should carry the image
Do not freeze the screen in place.

### Law D — Dark energy is a horizon field
Render it as low-frequency, horizon-linked pressure or glow.

### Law E — Primordial residue matters
A dark-radiation-like channel can carry early-universe memory.

### Law F — de Sitter should feel finite to an observer and infinite globally
Use observer horizon and global screens as distinct rendering modes.

## 9.16 API vocabulary

- scaleFactor
- hubbleRate
- cosmologicalHorizon
- futureEventHorizon
- screenAreaBudget
- activeDof
- holographicMatterDensity
- darkRadiationMix
- deSitterObserverMode
- globalInfinityMode
- teleologicalMix
- cosmicExpansionGain
- inflationCutoff
- cmbSuppressionBias

## 9.17 Failure modes to avoid

- fixed information budget in an expanding universe
- treating cosmological holography as static AdS with stars
- using local-only horizon logic when future horizon is the point
- no distinction between observer patch and global de Sitter
- no primordial residue / dark-radiation channel
```

The bones of that file come directly from the uploaded cosmology sources. The FLRW/FRW metric and the need to use light-sheets rather than naive spacelike regions are stated in the cosmology discussions by Bigatti–Susskind and Bousso. They explain that in expanding universes the relevant light-sheet can be truncated, so only entropy passing through it is bounded by area.   

For de Sitter, Bousso shows that global de Sitter can be projected onto past and future infinity as optimal screen-hypersurfaces, while the observable half for a single observer can be encoded on the event horizon (E), a preferred null screen of constant area (4\pi H^{-2}).  

The holographic-matter paper explicitly gives the future event horizon (L_f=a(t)\int_t^\infty dt'/a(t')), the holographic matter density (\rho=3C^2M_p^2/L^2), the action-principle form with a dark-radiation term, and the dynamical equation and equation of state for holographic dark energy.     

Bousso also stresses that in general holographic screen theories one should not expect a fixed number of active degrees of freedom, which is exactly the right conceptual move for dynamic cosmological scenes.  

---

# Equations to add to `theory/equations.json`

```json id="eeb4ov"
[
  {
    "id": "frw_metric",
    "equation": "ds^2 = a^2(η)[-dη^2 + dχ^2 + f^2(χ)dΩ^2]",
    "meaning": "FRW / FLRW cosmology in conformal time"
  },
  {
    "id": "de_sitter_flat_slicing",
    "equation": "ds^2 = -dt^2 + e^(2Ht)(dr^2 + r^2 dΩ^2)",
    "meaning": "Flat slicing of de Sitter space"
  },
  {
    "id": "de_sitter_horizon_area",
    "equation": "A_E = 4π H^(-2)",
    "meaning": "Observer event-horizon area in de Sitter space"
  },
  {
    "id": "holographic_matter_density",
    "equation": "ρ_HM = 3 C^2 M_p^2 / L^2",
    "meaning": "Basic holographic matter / dark-energy density law"
  },
  {
    "id": "future_event_horizon",
    "equation": "L_f = a(t) ∫_t^∞ dt' / a(t')",
    "meaning": "Future event horizon used as the IR scale in holographic dark energy"
  },
  {
    "id": "friedmann_hde",
    "equation": "3 M_p^2 H^2 = ρ_m + ρ_de",
    "meaning": "Matter plus holographic dark energy Friedmann equation"
  },
  {
    "id": "omega_hde_flow",
    "equation": "dΩ_de/d ln a = Ω_de (1-Ω_de)(1 + 2 sqrt(Ω_de)/C)",
    "meaning": "Evolution of the holographic dark-energy fraction"
  },
  {
    "id": "hde_equation_of_state",
    "equation": "w = -1/3 - (2/3) sqrt(Ω_de)/C",
    "meaning": "Equation of state for holographic dark energy"
  },
  {
    "id": "entanglement_hde",
    "equation": "S_ent ~ β N_dof L_h^2 / l^2 ; T = 1/(2πL_h) ; ρ_de = 3 M_p^2 C^2 / L_h^2",
    "meaning": "Entanglement-entropy route to holographic dark energy"
  },
  {
    "id": "action_principle_hm",
    "equation": "ρ_HM = M_p^2 [ C/(a^2L^2) + λ/(2a^4) ]",
    "meaning": "Action-principle holographic matter with a dark-radiation-like term"
  }
]
```

These formulas are all directly supported by the uploaded cosmology material: the de Sitter geometry and observer-horizon area from Bousso’s screen construction, and the holographic matter / dark-energy equations from the cosmological-application notes.       

---

# How I would code this

## 1. Cosmology state as a first-class scene driver

```js id="k70f9x"
export class CosmologyState {
  constructor({
    a = 1.0,
    H = 1.0,
    omegaDe = 0.7,
    C = 0.8,
    lambdaDarkRadiation = 0.0
  }) {
    this.a = a;
    this.H = H;
    this.omegaDe = omegaDe;
    this.C = C;
    this.lambdaDarkRadiation = lambdaDarkRadiation;
  }
}
```

This should sit above individual shaders. Cosmology is not a post effect. It is a scene law.

---

## 2. Expanding information budget

Tie active complexity directly to screen area.

```js id="lk5ly0"
export function deSitterObserverHorizonArea(H) {
  return 4.0 * Math.PI / Math.max(H * H, 1e-6);
}

export function activeDofFromScreenArea(area, density = 1.0) {
  return density * area;
}
```

Use that to drive:

* ray budget
* glyph count
* lattice resolution
* number of active entanglement bridges
* shader octave count

That is the correct Bousso-style cosmological move: active degrees of freedom need not be fixed.  

---

## 3. Future-event-horizon estimator

For real numerics you would integrate over a future expansion history. For the art engine, make it a controllable forecast operator.

```js id="8s5s63"
export function estimateFutureEventHorizon(a, history, i0) {
  let sum = 0.0;
  for (let i = i0; i < history.length - 1; i++) {
    const dt = history[i + 1].t - history[i].t;
    sum += dt / Math.max(history[i].a, 1e-6);
  }
  return a * sum;
}
```

This is one of the coolest controls in the whole repo:
present appearance depends on a future horizon forecast.

---

## 4. Holographic matter density field

```js id="70mwdc"
export function holographicMatterDensity(Mp, C, L) {
  return 3.0 * C * C * Mp * Mp / Math.max(L * L, 1e-6);
}
```

Render consequences:

* larger future horizon → softer lower-density pressure
* smaller horizon → denser cosmic pressure field
* scenes can transition from loose auroral expansion to compressed de Sitter shell logic

---

## 5. Dark-radiation residue channel

From the action-principle route, the (\lambda/(2a^4)) term is perfect for a primordial-memory layer. 

```js id="6mupxb"
export function darkRadiationDensity(Mp, lambda0, a) {
  return Mp * Mp * lambda0 / Math.max(2.0 * Math.pow(a, 4.0), 1e-6);
}
```

Visual use:

* ancient blue-violet grain
* primordial shell scars
* residual early-universe scanlines
* deep background “afterglow dust”

---

## 6. Equation-of-state-driven color / motion bias

```js id="ml6h22"
export function holographicW(omegaDe, C) {
  return -1.0 / 3.0 - (2.0 / 3.0) * Math.sqrt(Math.max(omegaDe, 0.0)) / Math.max(C, 1e-6);
}
```

Then map:

* `w ≈ -1` → smooth de Sitter wash, soft horizons, low-frequency expansion fog
* less negative `w` → more matter-like texture, stronger particulate structure
* extreme values → unstable cosmic boundary behavior

That gives the renderer a physics-derived mood slider instead of arbitrary vibes. 

---

## 7. GLSL cosmic pressure field

```glsl id="99wm2n"
float cosmicPressureField(vec3 p, float rhoHM, float a, float teleologicalMix) {
    float r = length(p);
    float horizonGlow = exp(-r * 0.4 / max(a, 1e-3));
    float lowFreq = 0.5 + 0.5 * cos(0.7 * r - 0.5 * a);
    return rhoHM * mix(lowFreq, horizonGlow, teleologicalMix);
}
```

This should feel like the universe being inflated by a boundary condition, not filled with random fog.

---

## 8. Observer patch vs global de Sitter mode

These are different scenes and should have different control logic.

```js id="hko2nu"
export const cosmologyModes = {
  DESITTER_OBSERVER_PATCH: "desitter_observer_patch",
  DESITTER_GLOBAL_SCREENS: "desitter_global_screens",
  FLRW_EMBEDDED_SCREENS: "flrw_embedded_screens",
  HDE_UNIVERSE: "hde_universe",
  INFLATIONARY_HM: "inflationary_hm"
};
```

### `DESITTER_OBSERVER_PATCH`

Use the observer horizon as the main preferred screen.

### `DESITTER_GLOBAL_SCREENS`

Use past and future infinity as paired global screens.

### `FLRW_EMBEDDED_SCREENS`

Use moving embedded screen layers.

### `HDE_UNIVERSE`

Drive density by (L_f) and (\Omega_{de}).

### `INFLATIONARY_HM`

Add primordial cutoff scars and dark-radiation residue.

Those distinctions are straight out of the de Sitter and cosmology screen logic in Bousso.   

---

## 9. Expanding screen stack

```js id="cy9q5w"
export function expandingScreenStack(t, count, baseRadius, growthRate) {
  const screens = [];
  for (let i = 0; i < count; i++) {
    screens.push({
      radius: baseRadius * Math.exp(growthRate * t) * (1.0 + 0.12 * i),
      age: i
    });
  }
  return screens;
}
```

Use these as:

* evolving cosmic display membranes
* e-folding shells
* epoch markers
* horizon-history layers

This is how you make cosmology visible.

---

## 10. Inflationary low-(l) suppression aesthetic

The inflation section says holographic matter can modify the early-universe spectrum and help with low-(l) suppression logic.  

You do not need full Boltzmann numerics for art.
You need a controlled “primordial cutoff” layer.

```js id="0ryzk5"
export function primordialSuppression(k, kCut, softness = 0.2) {
  const x = (k - kCut) / Math.max(softness, 1e-6);
  return 1.0 / (1.0 + Math.exp(-x));
}
```

Use it to:

* mute the largest-scale modes
* carve giant low-frequency voids
* create cosmic missing-harmonic aesthetics

---

# `theory/concepts.json` addition

```json id="g67v8u"
{
  "id": "cosmology_and_holographic_matter",
  "name": "Cosmology and Holographic Matter",
  "physics_meaning": "In cosmological settings, holographic screens can be time-dependent and embedded, with information budgets tied to horizon area and holographic matter density set by an IR length scale such as the future event horizon.",
  "math_forms": [
    "ds^2 = a^2(η)[-dη^2 + dχ^2 + f^2(χ)dΩ^2]",
    "ds^2 = -dt^2 + e^(2Ht)(dr^2 + r^2 dΩ^2)",
    "A_E = 4πH^(-2)",
    "ρ_HM = 3 C^2 M_p^2 / L^2",
    "L_f = a(t) ∫_t^∞ dt' / a(t')",
    "dΩ_de/d ln a = Ω_de (1-Ω_de)(1 + 2 sqrt(Ω_de)/C)",
    "w = -1/3 - (2/3) sqrt(Ω_de)/C",
    "ρ_HM = M_p^2 [ C/(a^2L^2) + λ/(2a^4) ]"
  ],
  "visual_translation": [
    "expanding screen membranes",
    "future-horizon-driven pressure fields",
    "observer-patch de Sitter domes",
    "global past/future screen pairing",
    "primordial dark-radiation afterglow",
    "low-l suppression voids"
  ],
  "shader_parameters": [
    "scaleFactor",
    "hubbleRate",
    "cosmologicalHorizon",
    "futureEventHorizon",
    "screenAreaBudget",
    "activeDof",
    "holographicMatterDensity",
    "darkRadiationMix",
    "deSitterObserverMode",
    "globalInfinityMode",
    "teleologicalMix",
    "cosmicExpansionGain",
    "inflationCutoff",
    "cmbSuppressionBias"
  ],
  "rendering_strategies": [
    "time-dependent screen budgets",
    "future-horizon field forecasting",
    "observer/global de Sitter mode switching",
    "dark-radiation residue layers",
    "primordial cutoff shaping"
  ],
  "failure_modes": [
    "fixed complexity in expanding cosmology",
    "static-shell treatment of FLRW",
    "no distinction between observer horizon and global de Sitter screens",
    "no horizon-based density law",
    "no primordial memory channel"
  ]
}
```

---

# Example scene for this section

## `examples/09_future_horizon_universe`

A cosmological chamber with nested translucent screens expanding through dark space. In observer mode, a finite de Sitter dome encloses the visible universe with area (4\pi H^{-2}), and complexity breathes with horizon size. In global mode, past and future infinity appear as immense opposing luminous walls, each encoding half the spacetime. A low-frequency holographic matter field fills the bulk, not as random fog but as future-horizon pressure. A faint dark-radiation channel leaves ancient ultraviolet scars, while a primordial cutoff suppresses the largest-scale harmonics and creates impossible cosmic silence in the deepest modes.

### Scene law

* screen area changes complexity budget
* future-horizon estimate changes present density
* observer patch and global de Sitter feel fundamentally different
* primordial residue is always quieter, older, colder, and more spectral
* inflationary mode introduces giant missing harmonics instead of simple noise

This would be one of the most original scenes in the repo.

---

# One distinction that will save the repo from fake cosmology

Keep these three separate:

## 1. Local Hubble scale

A present-time local expansion measure.

## 2. Future event horizon

A genuinely future-defined IR scale.

## 3. Observer event horizon in de Sitter

A finite preferred screen for one observer’s patch.

They are related, but they are not interchangeable. The cosmology notes specifically highlight why simply taking the Hubble scale as the characteristic length does not reproduce the desired dark-energy behavior, while the future event horizon does.  

---

# What I would build next in the codebase

* `src/js/cosmologyState.js`
* `src/glsl/fields/cosmic_pressure.glsl`
* `src/glsl/scenes/scene_future_horizon_universe.glsl`

The next theory doc should be:

`docs/10_shader_translation_guide.md`

That one will turn the whole repo into a usable coding manual:

* how to map each theory concept into SDFs
* raymarch step rules
* field layering
* camera logic
* post-processing
* debug passes
* how to combine JS + GLSL + maybe compute or node graphs cleanly

Say **next**.

Here’s the actual file.

# `docs/10_shader_translation_guide.md`

````md
# 10 — Shader Translation Guide

## 10.1 What this file is

This is the implementation bridge.
It turns the repo’s physics concepts into render architecture.

It answers:

- What should live in JavaScript?
- What should live in GLSL?
- What should be an SDF, a scalar field, a mask, or a post-process?
- When do we use local reconstruction, nonlocal reconstruction, or coarse-grained reconstruction?
- How do we debug whether a scene is actually “holographic” instead of just pretty?

The source theory gives a very clear backbone for this guide:

- near-boundary structure carries source data, and UV cutoffs in the boundary correspond to bulk cutoffs near the AdS boundary :contentReference[oaicite:0]{index=0} :contentReference[oaicite:1]{index=1}
- near-boundary fields admit asymptotic expansions with leading source data and subleading response data, with counterterms used to extract finite observables :contentReference[oaicite:2]{index=2} :contentReference[oaicite:3]{index=3}
- RT surfaces geometrize entanglement in static settings through minimal-area bulk surfaces :contentReference[oaicite:4]{index=4}
- screens and preferred screens arise from null projection, with preferred screens sitting where expansion vanishes :contentReference[oaicite:5]{index=5} :contentReference[oaicite:6]{index=6}
- local bulk reconstruction can fail in backgrounds with trapped null geodesics or evanescent modes, forcing either nonlocal or coarse-grained strategies :contentReference[oaicite:7]{index=7} :contentReference[oaicite:8]{index=8}

## 10.2 The golden architectural split

Use four layers.

### Layer A — scene topology
The coarse object structure:
- SDF bodies
- boundaries
- shells
- horizons
- membranes
- wedge volumes
- screens

### Layer B — holographic fields
The invisible logic:
- boundary source fields
- response fields
- entanglement weights
- null-projection density
- renormalization terms
- precursor channels
- cosmological pressure

### Layer C — transport / reconstruction
How information moves:
- local smearing
- nonlocal precursor injection
- light-sheet growth
- coarse-graining
- horizon scrambling
- RG filtering

### Layer D — image formation
How the above becomes visible:
- raymarch shading
- glow
- spectral lensing
- post-process diffusion
- chromatic anomaly bands
- debug overlays

This separation is essential because the physics distinguishes source, response, geometry, and readout. Near-boundary analysis determines divergent/source structure, but full deep-bulk correlation data generally needs more than asymptotics alone. :contentReference[oaicite:9]{index=9} :contentReference[oaicite:10]{index=10}

## 10.3 Repo implementation map

```text
src/
  js/
    sceneGraph.js
    holographyParams.js
    radialFlows.js
    boundaryEncoders.js
    entanglementGraphs.js
    reconstructionModes.js
    cosmologyState.js
    debugViews.js

  glsl/
    common.glsl
    sdf/
      primitives.glsl
      ads_spaces.glsl
      blackhole_sdfs.glsl
      membrane_sdfs.glsl
      screen_sdfs.glsl
    fields/
      source_field.glsl
      response_field.glsl
      entropy_field.glsl
      uv_ir_field.glsl
      light_sheet_field.glsl
      entanglement_field.glsl
      horizon_scramble.glsl
      operator_smear.glsl
      precursor_field.glsl
      renorm_field.glsl
      cosmic_pressure.glsl
    render/
      raymarch.glsl
      normals.glsl
      shading.glsl
      lighting.glsl
      post.glsl
      debug.glsl
    scenes/
      scene_surface_volume.glsl
      scene_ads_tunnel.glsl
      scene_rt_surface.glsl
      scene_horizon_hologram.glsl
      scene_light_sheet_screen.glsl
      scene_precursor_chamber.glsl
      scene_future_horizon_universe.glsl
````

## 10.4 What belongs in JavaScript

JavaScript should hold:

* state
* topology graph data
* long-timescale evolution
* boundary textures / operator maps
* entanglement graphs
* cosmology parameters
* mode switching
* debug panels
* CPU-side precomputation

Do in JS:

* build the boundary sample set
* forecast future-horizon values
* update relation graphs
* choose reconstruction mode
* manage temporal history buffers
* precompute support clouds
* generate screen stacks
* pass uniforms and textures to GLSL

### Example JS scene state

```js
export const sceneState = {
  mode: "ads_rt_blackhole_hybrid",
  cameraMode: "null_follow",
  reconstructionMode: "precursor_assisted",
  params: {
    boundaryDepth: 0.03,
    warpIntensity: 1.2,
    smearSupportRadius: 0.18,
    precursorWeight: 0.35,
    horizonRadius: 0.9,
    stretchedHorizonWidth: 0.06,
    entanglementWeight: 0.72,
    bridgeThreshold: 0.42,
    cutoffRho: 0.025,
    anomalyMix: 0.15
  },
  debug: {
    showBoundarySupport: false,
    showRTSurface: false,
    showScreens: false,
    showCounterterm: false,
    showRecoverableDepth: false
  }
};
```

## 10.5 What belongs in GLSL

GLSL should hold:

* fast local field evaluation
* SDF evaluation
* march loop
* normals
* shell weighting
* glow accumulation
* short-range kernels
* depth-dependent frequency scaling
* caustic/screen emphasis
* post effects

Do in GLSL:

* evaluate warped coordinates
* compute shell and membrane weights
* evaluate source/response fields locally
* integrate short boundary sample kernels
* apply depth-aware ray steps
* accumulate emission
* output debug render targets

Rule of thumb:
if the quantity must be sampled per-fragment or per-step, it belongs in GLSL.

## 10.6 Core data types

Every scene element should be one of these.

### SDF object

Use for:

* bodies
* shells
* horizons
* membranes
* screens
* wedge boundaries

### Scalar field

Use for:

* entropy density
* response amplitude
* coarse-grained visibility
* renormalized finite part
* cosmological pressure
* reconstruction confidence

### Vector field

Use for:

* null congruence direction
* entanglement flow
* radial RG flow
* shell tangent drift
* beam convergence direction

### Graph / relation object

Use for:

* entanglement connectivity
* precursor coupling
* operator-support clouds
* screen-family hierarchy

### Texture / operator map

Use for:

* explicit boundary inscriptions
* source maps
* glyph atlases
* historical buffers
* response caches

## 10.7 The one true render pipeline

A holographic scene should usually render in this order:

### Pass 1 — build boundary state

Generate or update:

* source maps
* operator glyphs
* history buffers
* entanglement graph textures
* cosmology horizon values

### Pass 2 — choose geometry logic

Choose:

* pure AdS
* black hole
* screen stack
* RT membrane
* cosmic screen mode
* hybrid scene mode

### Pass 3 — choose reconstruction mode

Choose one:

* local smear
* precursor assisted
* coarse grained
* failed local readout

This matters because the theory does not support one universal local readout map in all backgrounds. Trapped null geodesics and evanescent modes are real obstructions.  

### Pass 4 — raymarch topology

Raymarch SDFs in warped coordinates.

### Pass 5 — evaluate holographic fields

At each step compute:

* source contribution
* response contribution
* null-sheet density
* entanglement bridge weight
* horizon scrambling
* renormalization layers

### Pass 6 — renormalize visible quantity

Subtract counterterm-like shell artifacts and keep finite response where appropriate. The point is not to “drop infinities by vibe,” but to separate near-boundary divergent structure from meaningful finite structure.  

### Pass 7 — shade and post-process

Add:

* spectral glow
* anomaly bands
* photon-ring accents
* caustic bloom
* debug overlays

## 10.8 Coordinate systems you actually want

Use several coordinate views.

### Euclidean scene coordinates

For easy editing and interaction.

### AdS-inspired warped coordinates

For depth/scale logic.
Near-boundary cutoffs correspond to UV cutoffs, so the boundary side of the scene should be treated as a high-frequency zone with explicit cutoff control. 

### Null coordinates

For light-sheet and screen scenes.

### Boundary UV coordinates

For source/operator textures.

### Relation coordinates

For graph-driven entanglement or precursor control.

Do not force one coordinate system to do every job.

## 10.9 Standard shader skeleton

```glsl
struct Hit {
    float d;
    int materialId;
};

struct FieldData {
    float source;
    float response;
    float entropy;
    float nullSheet;
    float entanglement;
    float scramble;
    float counterterm;
    float finitePart;
};

Hit sceneSDF(vec3 p);
FieldData evalFields(vec3 p, float t);

vec3 render(vec3 ro, vec3 rd, float t) {
    float dist = 0.0;
    vec3 color = vec3(0.0);

    for (int i = 0; i < MAX_STEPS; i++) {
        vec3 p = ro + rd * dist;
        Hit hit = sceneSDF(p);
        FieldData f = evalFields(p, t);

        color += accumulateEmission(p, f, hit);

        if (hit.d < SURF_EPS) {
            color += shadeSurface(p, rd, hit, f);
            break;
        }

        dist += stepRule(p, hit.d, f);
        if (dist > MAX_DIST) break;
    }

    return postColor(color);
}
```

## 10.10 Step rules are physical style

Step size should not be constant.

### Near boundary

Use smaller steps.
The geometry and field structure are more singular and source-heavy there. Near-boundary analysis is exactly where the important divergent/source structure lives. 

### Near horizon

Use smaller steps.
Scrambling shells, photon rings, and redshift features are thin.

### In unresolved evanescent zones

Increase blur, not precision.
The theory says exact sharp reconstruction may require exponential precision, so do not pretend by oversampling indefinitely. Use coarse-graining instead. 

### Sample rule

```glsl
float stepRule(vec3 p, float sdfD, FieldData f) {
    float zScale = clamp(adsDepth(p) * 0.2, 0.03, 0.25);
    float shellPenalty = mix(1.0, 0.35, f.scramble);
    float nullPenalty = mix(1.0, 0.5, f.nullSheet);
    return sdfD * zScale * shellPenalty * nullPenalty;
}
```

## 10.11 Standard field decomposition

Every meaningful scene field should decompose as:

```text
raw field
= source
+ response
+ nonlocal correction
+ anomaly/log residue
```

This mirrors the asymptotic source/response logic and anomaly structure of holographic renormalization. The leading coefficient is the source, subleading coefficients are recursively determined response terms up to a state-dependent slot, and log terms encode anomaly structure.  

### Recommended GLSL struct

```glsl
struct Decomp {
    float source;
    float response;
    float precursor;
    float anomaly;
};
```

## 10.12 Boundary-to-bulk injection pattern

Do not let one UV point own one interior point.

Use:

* local support cloud
* time window
* depth attenuation
* optional precursor correction

```glsl
float localSmear(vec3 p, float t) {
    float acc = 0.0;
    float norm = 0.0;
    for (int i = 0; i < BOUNDARY_SAMPLES; i++) {
        vec3 b = boundaryPos(i);
        float dt = abs(t - boundaryTime(i));
        float dx = length(p.xz - b.xz);
        float w = exp(-dx * SMEAR_K) * exp(-dt * SMEAR_T);
        float src = sourceTexture(boundaryUV(i)).r;
        acc += w * src;
        norm += w;
    }
    return acc / max(norm, 1e-4);
}
```

That is the implementation echo of the smearing-function viewpoint. But the repo must still allow this to fail in hostile geometries. 

## 10.13 Reconstruction mode switch

```js
export function selectReconstructionMode(flags) {
  if (flags.hasTrappedGeodesics || flags.hasStrongEvanescence) {
    return "coarse_grained";
  }
  if (flags.requiresEarlyKnowledge) {
    return "precursor_assisted";
  }
  return "local_smear";
}
```

This is not just engineering convenience.
It is theoretically honest.

## 10.14 How to render entanglement

RT is for static settings and computes entropy through minimal-area surfaces in the bulk. So your entanglement renderer should treat membranes as **structural surfaces**, not decorative wires. 

Implementation pattern:

* JS stores relation graph
* JS decides connected vs disconnected phase
* GLSL renders candidate membrane / bridge field
* scene picks lower-cost phase

### Phase logic

```js
function bridgePhase(weight, threshold) {
  return weight > threshold ? "connected" : "disconnected";
}
```

### Geometry logic

Use:

* saddle membrane
* soap-film bridge
* wedge overlap
* tear field on disconnect

Do not default to cylinders or bezier noodles.

## 10.15 How to render screens and light-sheets

Screens come from null projection.
Preferred screens occur where expansion vanishes; screen-hypersurfaces are families of such surfaces.  

Implementation pattern:

* JS picks null slice family
* JS estimates allowed directions
* GLSL emits beam families
* GLSL terminates at caustics or screen conditions
* preferred-screen regions get higher coherence/brightness

### Screen field

```glsl
float preferredScreenWeight(float theta) {
    return exp(-abs(theta) * 40.0);
}
```

### Light-sheet field

```glsl
float lightSheetDensity(vec3 p, vec3 origin, vec3 dir) {
    vec3 q = p - origin;
    float axial = dot(q, dir);
    float radial = length(q - axial * dir);
    return exp(-radial * 8.0) * exp(-max(axial, 0.0) * 0.9) * step(0.0, axial);
}
```

## 10.16 How to render horizons

Black-hole entropy scales with horizon area, and the stretched horizon is the visible operational membrane where outside-observer dynamics live. The horizon is also a minimal surface in the black-hole case, which is part of why it talks cleanly to RT-style rendering.  

Implementation pattern:

* JS computes area budget from horizon radius
* GLSL renders true horizon logic + stretched horizon shell
* infalling objects lose identity into shell scrambling
* interior readout confidence drops

### Shell rule

```glsl
float stretchedHorizonShell(vec3 p, float rs, float width) {
    return exp(-abs(length(p) - rs * (1.0 + width)) * 60.0);
}
```

## 10.17 How to render renormalization

Near-boundary asymptotic data should be handled explicitly.
Fefferman–Graham form and asymptotic field expansions tell you that the boundary zone is where source and divergence structure live; counterterms give a consistent way to remove local divergences and retain finite response.  

Implementation pattern:

* evaluate raw shell field
* evaluate local counterterm
* subtract to get finite part
* preserve anomaly/glitch residual

### Counterterm pass

```glsl
float countertermLocal(float shellValue, float gradMag, float rho) {
    return 0.7 * shellValue / max(rho, 1e-3)
         + 0.15 * gradMag
         + 0.08 * log(1.0 / max(rho, 1e-3));
}
```

## 10.18 How to render evanescent / unresolved zones

When evanescent modes matter, exact reconstruction requires exponential precision; coarse-graining over a boundary scale can recover information only to a comparable depth scale. 

Implementation pattern:

* compute recoverable depth from sigma
* if depth exceeds sigma, suppress sharp structure
* replace with blurred macro-amplitude
* optionally expose “probe mode” to recover more

### Rule

```js
function recoverableDepth(sigma) {
  return sigma;
}
```

This is one of the most important anti-fake rules in the repo.

## 10.19 Camera logic

You want more than one camera mode.

### Observer-centric

Use for:

* stretched-horizon scenes
* de Sitter observer patch
* screen-facing portraits

### Null-follow

Use for:

* light-sheet scenes
* causal cone scenes
* converging beam cathedrals

### RG descent

Move mostly along the holographic depth axis so motion feels like changing scale, not just translating through room coordinates. The UV/IR guidance from the theory makes this the natural camera for AdS-inspired scenes. 

### Entanglement orbit

Orbit around wedge overlaps and minimal-surface bridges.

## 10.20 Post-processing rules

Post should encode physics layers, not just make things “cool.”

### Bloom

Use for:

* shells
* screens
* caustics
* preferred surfaces

### Chromatic shift

Use for:

* anomaly residues
* horizon redshift
* deep-bulk precursor fog

### Blur

Use for:

* unresolved evanescent regions
* deconfined thermal shell haze
* coarse-grained recovery mode

### Temporal echo

Use for:

* precursor channels
* boundary memory
* delayed Hawking-like emission

## 10.21 Debug views you absolutely want

The repo should ship with these debug passes.

### Boundary source

See the operator/source map directly.

### Response only

See finite interior response without shell blow-up.

### Counterterm

See what gets subtracted.

### Screen map

See preferred screens and null slices.

### RT surface

See minimal/bridge candidates.

### Reconstruction confidence

See local readability vs precursor-only zones.

### Recoverable depth

See where evanescent/coarse-grained constraints cut off detail.

### Horizon area budget

See shell complexity budget.

These are not optional.
Without them the repo will drift into fake mysticism.

## 10.22 Standard scene assembly recipes

### Recipe A — surface-encoded figure

* SDF humanoid or mask
* shell source texture
* inward smear field
* hollow interior
* renorm pass
* shell bloom

### Recipe B — AdS tunnel

* warped coordinates
* hyperbolic grid
* UV-rich boundary
* low-frequency deep bulk
* RG-descent camera

### Recipe C — RT bridge

* two source regions
* relation graph
* wedge overlap
* connected/disconnected phase test
* membrane SDF
* tear post-process

### Recipe D — horizon membrane

* true horizon radius
* stretched horizon shell
* identity-loss melt
* photon-ring accent
* precursor-only interior residue

### Recipe E — screen cathedral

* seed surface
* allowed null directions
* beam fans
* caustic termination
* preferred-screen stack
* transparent membrane shading

### Recipe F — cosmological pressure hall

* expanding screen stack
* future horizon estimate
* holographic matter pressure field
* dark-radiation residue
* observer/global mode switch

## 10.23 Performance rules

Do not waste samples where theory says resolution is fake.

### Good places to spend budget

* shells
* screen surfaces
* near-boundary UV zone
* RT membranes
* photon rings
* caustics

### Good places to save budget

* unresolved deep-bulk evanescent zones
* fully thermalized horizon interiors
* coarse-grained cosmic pressure fog
* precursor-only shadow regions

This is one of the few cases where physics intuition aligns beautifully with GPU optimization.

## 10.24 Parameter naming rules

Keep the parameter names semantically clean.

Use:

* `boundaryDepth`
* `cutoffRho`
* `smearSupportRadius`
* `precursorWeight`
* `recoverableDepth`
* `screenThreshold`
* `horizonRadius`
* `stretchedHorizonWidth`
* `entanglementWeight`
* `bridgeThreshold`
* `futureEventHorizon`
* `darkRadiationMix`

Avoid vague names like:

* `magicGlow`
* `spaceWeirdness`
* `mysticFactor`

## 10.25 A full scene contract

Every scene file should declare:

```js
export const sceneContract = {
  topology: ["sdf_shell", "screen_stack", "membrane"],
  fields: ["source", "response", "null_sheet", "precursor"],
  reconstruction: "precursor_assisted",
  renormalization: true,
  cameraModes: ["observer", "null_follow"],
  debugViews: [
    "source",
    "response",
    "screen_map",
    "reconstruction_confidence"
  ]
};
```

## 10.26 The central anti-fake test

Ask these questions.

1. Is bulk structure caused by a boundary/source/screen law?
2. Does depth change scale or recoverability?
3. Are light-sheet and screen scenes directional and terminating?
4. Are entanglement bridges driven by relation, not ornament?
5. Are black-hole interiors losing readability?
6. Is near-boundary structure separated into source/divergence/finite response?
7. Are hostile geometries allowed to fail clean local reconstruction?

If not, the scene is probably “holography flavored,” not holography translated.

## 10.27 Minimal starter stack

If you want the first working version fast:

* JS:

  * source map
  * scene params
  * reconstruction mode
  * relation graph
* GLSL:

  * one shell SDF
  * one AdS warp
  * one local smear field
  * one precursor field
  * one membrane field
  * one renorm subtraction
  * one debug pass

That is enough to build:

* a surface-encoded figure
* an AdS tunnel
* an RT bridge
* a horizon membrane
* a screen stack scene

````

---

# Code stubs I’d add immediately

## `src/js/reconstructionModes.js`

```js
export const reconstructionModes = {
  LOCAL_SMEAR: "local_smear",
  PRECURSOR_ASSISTED: "precursor_assisted",
  COARSE_GRAINED: "coarse_grained",
  FAILED_LOCAL_READOUT: "failed_local_readout"
};

export function chooseReconstructionMode(flags) {
  if (flags.hasTrappedGeodesics || flags.hasStrongEvanescence) {
    return reconstructionModes.COARSE_GRAINED;
  }
  if (flags.requiresEarlyKnowledge) {
    return reconstructionModes.PRECURSOR_ASSISTED;
  }
  return reconstructionModes.LOCAL_SMEAR;
}
````

## `src/glsl/fields/source_response.glsl`

```glsl
struct Decomp {
    float source;
    float response;
    float precursor;
    float anomaly;
};

Decomp evalDecomp(vec3 p, float t) {
    Decomp d;
    d.source = localSmear(p, t);
    d.response = responseField(p, t);
    d.precursor = precursorField(p, t);
    d.anomaly = anomalyField(p, t);
    return d;
}

float finiteVisibleField(Decomp d, float rho) {
    float raw = d.source + d.response + 0.35 * d.precursor + 0.15 * d.anomaly;
    float ct  = countertermLocal(d.source, abs(d.response - d.source), rho);
    return raw - ct;
}
```

## `src/js/debugViews.js`

```js
export const debugViews = {
  NONE: "none",
  SOURCE: "source",
  RESPONSE: "response",
  COUNTERTERM: "counterterm",
  SCREENS: "screens",
  RT: "rt_surface",
  CONFIDENCE: "reconstruction_confidence",
  RECOVERABLE_DEPTH: "recoverable_depth",
  HORIZON_BUDGET: "horizon_budget"
};
```

---

This guide is grounded by the actual theory spine you uploaded: UV/IR cutoff logic and bulk depth scaling , near-boundary asymptotic expansions and counterterms  , RT minimal surfaces in static holographic settings , preferred screens and screen-hypersurfaces from null projection  , and the hard limit that trapped null geodesics and evanescent modes can kill exact local reconstruction or force coarse-graining  .

The next file I’d write is `docs/11_visual_motif_library.md`, which would turn all this into a catalog of reusable looks:
AdS cathedrals, screen skins, horizon glyphfire, RT bridges, precursor ghosts, cosmological pressure domes.

Here’s the motif catalog file.

# `docs/11_visual_motif_library.md`

````md
# 11 — Visual Motif Library

## 11.1 What this file is

This is the reusable lookbook for the repo.

Not “style” in the empty moodboard sense.
These motifs are scene archetypes tied to actual holographic structures:
- area-law shells
- AdS scale gradients
- bulk-from-boundary emergence
- RT membranes
- preferred screens
- stretched horizons
- precursor ghosts
- evanescent blur
- future-horizon cosmology

The goal is simple:
take one motif,
plug it into JS + GLSL + raymarching,
and get a scene that still feels mathematically governed.

The source spine for this motif library is clear:
surface-first encoding and radial resolution gradients are already the right visual translation of the holographic principle and AdS scale logic. Entanglement naturally becomes filaments, tunnels, and minimal membranes. Preferred screens arise where null expansion vanishes. The de Sitter observer horizon is a preferred screen of constant area. And stretched horizons plus light-sheets provide the right logic for shell, screen, and horizon aesthetics. :contentReference[oaicite:0]{index=0} :contentReference[oaicite:1]{index=1} :contentReference[oaicite:2]{index=2} :contentReference[oaicite:3]{index=3} :contentReference[oaicite:4]{index=4}

---

## 11.2 How to use the library

Each motif has:

- **Core principle**
- **Visual DNA**
- **Scene role**
- **Shader strategy**
- **Parameter cluster**
- **Prompt tokens**
- **Failure mode**

You should be able to combine 2–4 motifs per scene without collapsing into nonsense.

Rule:
one motif usually controls **geometry**, one controls **information flow**, one controls **illumination**, and one controls **failure/degradation**.

---

## 11.3 Motif 01 — Surface Glyphshell

### Core principle
Information lives on the boundary, not in the volume.

### Visual DNA
- glowing shell
- hollow or refractive interior
- encoded glyph skin
- pulsation on boundary
- shell cracks, maps, logic scars

### Scene role
Best as:
- character body
- planet-like object
- mask
- chamber wall
- seed object for null projection

### Shader strategy
- shell-weighted emission
- UV-driven glyph atlas
- transparent interior
- weak interior refractive haze
- optional local smear into bulk

### Parameter cluster
```json
{
  "shellGlow": 1.4,
  "glyphDensity": 18.0,
  "interiorOpacity": 0.15,
  "surfacePulse": 0.35,
  "logicCrackGain": 0.22
}
````

### Prompt tokens

* `surface-encoded`
* `glyphshell`
* `hollow interior`
* `logic skin`
* `boundary pulse`

### Failure mode

Making the interior equally detailed as the shell.

This motif is directly supported by the visual-translation rules in your holographic art source, which explicitly says to prioritize surface detail, make interiors hollow/refractive, and let boundaries glow with encoded logic.  

---

## 11.4 Motif 02 — AdS Edge Saturation

### Core principle

Outer / near-boundary regions carry finer resolution than deeper bulk regions.

### Visual DNA

* ultradetailed edges
* recursive outer ornament
* softened deep center
* depth-gradient detail loss
* concentric or radial recursion

### Scene role

Best as:

* tunnels
* cathedral halls
* infinite zoom spaces
* boundary-ring chambers
* camera-depth worlds

### Shader strategy

* coordinate warp with depth
* frequency redshift by depth
* boundary cutoff
* finer noise bands near UV edge
* smoother interior macro fields

### Parameter cluster

```json
{
  "boundaryDepth": 0.03,
  "warpIntensity": 1.15,
  "uvIrMix": 0.8,
  "boundaryDetailGain": 1.8,
  "deepBulkSoftness": 0.55
}
```

### Prompt tokens

* `radial scale gradient`
* `edge-detailed`
* `recursive tunnel`
* `uv-ir depth`
* `boundary saturation`

### Failure mode

Uniform frequency everywhere.

This comes straight from the repo’s AdS logic and is echoed in the uploaded visual-translation rules as “radial resolution scaling”: outer regions highly detailed, deeper regions smoother and less resolved. 

---

## 11.5 Motif 03 — Light-Sheet Hologram

### Core principle

Information projects along converging null directions.

### Visual DNA

* beam fans
* scanfronts
* inward radial slices
* converging light
* partial reconstruction by beam overlap

### Scene role

Best as:

* scanning objects
* self-assembling bulk forms
* projection scenes
* ritualized build-up sequences
* camera-facing reveal effects

### Shader strategy

* allowed null-direction beam families
* termination at caustics
* overlap-driven interior intensity
* asymmetric future/past variants

### Parameter cluster

```json
{
  "beamFocus": 7.5,
  "sheetFalloff": 1.1,
  "causticGain": 1.25,
  "terminationSharpness": 0.7,
  "nullDirectionality": 0.92
}
```

### Prompt tokens

* `light-sheet`
* `converging beams`
* `null scanfront`
* `caustic projection`
* `bulk reconstructed from light`

### Failure mode

Flat inward projection with no directionality or termination.

The covariant holographic setup explicitly defines light-sheets from null congruences with nonpositive expansion, and both Bousso and Bigatti–Susskind emphasize that holographic entropy is most naturally defined on light-like surfaces rather than naive spacelike volumes.   

---

## 11.6 Motif 04 — Preferred Screen Membrane

### Core principle

Some surfaces are privileged information displays because null expansion vanishes there.

### Visual DNA

* transparent membrane sheet
* balanced glow
* sharply legible symbol layer
* calm surface amid chaotic beams
* screen as “where the world chooses to appear”

### Scene role

Best as:

* display plane
* sacred membrane
* event interface
* projection stop surface
* observer-centric image layer

### Shader strategy

* screen field from `theta ~ 0`
* high coherence at preferred screen
* layered screen stack option
* light caustic accumulation onto membrane

### Parameter cluster

```json
{
  "screenThreshold": 0.03,
  "preferredScreenBias": 1.6,
  "screenOpacity": 0.22,
  "glyphStability": 0.9,
  "membraneBloom": 1.15
}
```

### Prompt tokens

* `preferred screen`
* `projection membrane`
* `information canvas`
* `theta-zero surface`
* `caustic shell`

### Failure mode

Using arbitrary glowing planes unrelated to projection logic.

Bousso defines screens by inverting the projection construction and calls a screen “preferred” when the expansion vanishes on it; the source also presents these as especially efficient encoders of global information.  

---

## 11.7 Motif 05 — Entanglement Filaments

### Core principle

Correlation becomes visible connective tissue.

### Visual DNA

* threads
* braids
* luminous cords
* shimmering lattice ties
* thickness = relation strength

### Scene role

Best as:

* portraits
* paired figures
* relation maps
* emotional scene architecture
* bridge pre-phase before full membrane

### Shader strategy

* relation graph to spline/field conversion
* line thickness from entanglement weight
* color drift from mutual information
* flutter/jitter from unstable coupling

### Parameter cluster

```json
{
  "threadDensity": 36,
  "threadThickness": 0.018,
  "entanglementWeight": 0.7,
  "colorShiftGain": 0.4,
  "flutter": 0.08
}
```

### Prompt tokens

* `entanglement filaments`
* `rainbow lattice`
* `correlation threads`
* `light braids`
* `informational connective tissue`

### Failure mode

Using random webs with no relation metric.

Your holographic visual notes explicitly translate entanglement into strands, filaments, and woven geometry, with thickness and brightness carrying relation strength.  

---

## 11.8 Motif 06 — RT Soap-Film Bridge

### Core principle

Shared information is encoded by a minimal-like connecting membrane.

### Visual DNA

* soap-film saddle
* curved membrane bridge
* floating minimal surface
* tense, elegant geometry
* sudden connect/disconnect phase changes

### Scene role

Best as:

* shared chamber between two entities
* portal-like bridge
* static relation geometry
* centerpiece of an entanglement scene

### Shader strategy

* graph decides bridge phase
* connected vs disconnected membrane candidates
* tension-controlled sheet SDF
* tear field on collapse

### Parameter cluster

```json
{
  "bridgeThreshold": 0.45,
  "rtTension": 0.72,
  "membraneGlow": 1.28,
  "disconnectSharpness": 0.35,
  "sharedInteriorWeight": 0.6
}
```

### Prompt tokens

* `minimal surface`
* `soap-film bridge`
* `entanglement membrane`
* `shared chamber`
* `bulk bridge`

### Failure mode

Rendering the bridge as a tube instead of a membrane.

The RT formula ties entanglement entropy to the area of a minimal bulk surface, and your visual translation source already renders this as glowing soap-film surfaces spanning boundary anchors.  

---

## 11.9 Motif 07 — Entanglement Tear

### Core principle

Loss of shared information tears geometry.

### Visual DNA

* ripped membrane
* distortion gaps
* shadow shear
* broken cords
* spatial discontinuity

### Scene role

Best as:

* scene failure state
* dramatic transition
* heartbreak / severance / decoherence scene
* geometry degradation effect

### Shader strategy

* noise-modulated cut field
* bridge thickness collapse
* local darkening as missing data, not occlusion
* broken filament fragments

### Parameter cluster

```json
{
  "cutSeverity": 0.2,
  "tearNoiseScale": 3.2,
  "missingDataDarkness": 0.75,
  "bridgeFailureGain": 1.1,
  "shadowShear": 0.3
}
```

### Prompt tokens

* `entanglement tear`
* `ripped light fabric`
* `distortion gap`
* `severed information threads`
* `geometry degradation`

### Failure mode

Just fading opacity instead of structurally breaking geometry.

The visual notes explicitly translate disentanglement into broken cords, distortion gaps, and ripped light-fabric. 

---

## 11.10 Motif 08 — Horizon Glyphfire

### Core principle

The horizon is an active information membrane, not a black outline.

### Visual DNA

* thermal shell
* drifting heat glyphs
* shell-aligned turbulence
* sparse emission
* thin ring accents

### Scene role

Best as:

* black-hole scenes
* deconfined thermal scenes
* collapse events
* shell-heavy cosmic icons

### Shader strategy

* true horizon + stretched horizon split
* thermal scrambling at shell
* sparse Hawking-like specks
* photon-ring accent
* interior readout suppression

### Parameter cluster

```json
{
  "horizonRadius": 0.9,
  "stretchedHorizonWidth": 0.06,
  "thermalScramble": 0.85,
  "hawkingGlow": 0.18,
  "photonRingGain": 0.45
}
```

### Prompt tokens

* `horizon glyphfire`
* `thermal membrane`
* `stretched horizon`
* `photon ring`
* `scrambling shell`

### Failure mode

Making the black hole a matte black sphere.

Bigatti–Susskind describe stretched horizons on light-sheets and screen mapping, and the black-hole entropy/horizon-minimal-surface connection makes the horizon the right information membrane to emphasize.  

---

## 11.11 Motif 09 — Infall Melt

### Core principle

Object identity thermalizes near the stretched horizon.

### Visual DNA

* identity smear
* shell-aligned streaking
* detail melting into heat cells
* redshifted color collapse
* universalized local state

### Scene role

Best as:

* object entering black hole
* sacrifice / assimilation scene
* information-processing metaphor
* transition into shell logic

### Shader strategy

* identity-loss factor by distance to horizon
* normals dissolve
* object-specific colors replaced by shell palette
* lingering nonlocal residue inside

### Parameter cluster

```json
{
  "identityLossSharpness": 0.8,
  "shellAssimilation": 0.9,
  "redshiftStrength": 0.4,
  "detailDecay": 0.65,
  "interiorResidue": 0.12
}
```

### Prompt tokens

* `infall melt`
* `identity thermalization`
* `shell assimilation`
* `redshift dissolve`
* `horizon consumption`

### Failure mode

Letting objects cross the shell unchanged.

---

## 11.12 Motif 10 — Caustic Cathedral

### Core principle

Focused null bundles create compression membranes and luminous architecture.

### Visual DNA

* focal shells
* beam-formed vaults
* cathedral ribs
* compressed light curtains
* sacred optics

### Scene role

Best as:

* large environmental spaces
* screen cathedrals
* boss-arena type chambers
* sacred geometry halls

### Shader strategy

* multiple converging beam families
* caustic membrane SDFs
* screen stack layering
* transparent interlocking shells

### Parameter cluster

```json
{
  "beamCount": 8,
  "causticRadius": 1.4,
  "compressionBloom": 1.35,
  "screenLayerCount": 5,
  "vaultContrast": 0.42
}
```

### Prompt tokens

* `caustic cathedral`
* `focused light vault`
* `compression membrane`
* `null-geometry hall`
* `projection basilica`

### Failure mode

Treating caustics as random bright spots instead of structural membranes.

The screen/light-sheet material explicitly associates projection with converging null congruences and termination surfaces, while your visual notes translate screens into glowing caustic geometries and layered depth slices.  

---

## 11.13 Motif 11 — Precursor Ghost

### Core principle

Deep bulk events can be encoded nonlocally before local readout catches up.

### Visual DNA

* low-frequency aura
* early ghost geometry
* broad global shimmer
* hidden mode halo
* too-early bulk knowledge

### Scene role

Best as:

* prophecy scenes
* collision-before-readout effects
* eerie anticipation
* latent-event chambers

### Shader strategy

* global hidden modes
* precursor field mixed before local support cloud stabilizes
* broad soft illumination
* delayed sharpening

### Parameter cluster

```json
{
  "precursorWeight": 0.38,
  "earlyKnowledgeGain": 0.55,
  "ghostSpread": 0.7,
  "localDelay": 0.4,
  "globalModeDrift": 0.2
}
```

### Prompt tokens

* `precursor ghost`
* `nonlocal aura`
* `too-early geometry`
* `global hidden modes`
* `latent bulk event`

### Failure mode

Making the ghost look like ordinary fog instead of distributed encoded anticipation.

---

## 11.14 Motif 12 — Evanescent Blur Chamber

### Core principle

Some bulk features have exponentially tiny boundary imprint and can only be recovered coarsely.

### Visual DNA

* blurred hidden structures
* soft but intense interior swells
* impossible near-depth sharpness
* STM-like reveal when approached
* macroform without crisp readout

### Scene role

Best as:

* black-hole atmospheres
* dense barrier interiors
* hidden chambers
* unresolved deep-bulk zones

### Shader strategy

* recoverable depth cutoff from sigma
* blurred reconstruction beyond threshold
* optional probe mode for local reveal
* suppressed boundary imprint

### Parameter cluster

```json
{
  "reconstructionSigma": 0.35,
  "recoverableDepth": 0.35,
  "evanescentWeight": 0.7,
  "probeRevealGain": 1.2,
  "boundarySuppression": 0.8
}
```

### Prompt tokens

* `evanescent blur`
* `hidden bulk swell`
* `coarse-grained chamber`
* `STM reveal`
* `unrecoverable crispness`

### Failure mode

Rendering all deep structure sharply regardless of geometry.

---

## 11.15 Motif 13 — Responsibility Cloud

### Core principle

A bulk point is owned by a distributed support region on the boundary.

### Visual DNA

* arcs on shell
* distributed activation patches
* weighted constellations on boundary
* support halos
* visible causation map

### Scene role

Best as:

* debug pass
* UI overlay
* explanatory visualization
* educational mode
* operator scenes

### Shader strategy

* compute boundary support weights
* draw responsibility arcs / masks
* pulse only responsible source cells
* optional temporal trails

### Parameter cluster

```json
{
  "supportRadius": 0.22,
  "responsibilityGain": 1.0,
  "arcGlow": 0.9,
  "supportTrail": 0.16,
  "temporalWindow": 0.4
}
```

### Prompt tokens

* `responsibility cloud`
* `support arcs`
* `boundary ownership map`
* `causal shell overlay`
* `distributed source control`

### Failure mode

Single UV point controls single bulk point.

---

## 11.16 Motif 14 — Renormalized Boundary Fire

### Core principle

The boundary can be violently divergent, but finite response emerges after local subtraction.

### Visual DNA

* overbright shell fire
* shell singularity bands
* subtraction scars
* finite calm interior
* anomaly rings that remain

### Scene role

Best as:

* edge of AdS scenes
* shell-heavy portraits
* explanation/debug scenes
* transition between chaos and form

### Shader strategy

* raw shell field
* local counterterm subtraction
* finite response pass
* preserved anomaly chroma residue

### Parameter cluster

```json
{
  "shellDivergenceGain": 1.3,
  "countertermStrength": 1.0,
  "finitePartGain": 1.15,
  "anomalyMix": 0.16,
  "logTermGlow": 0.22
}
```

### Prompt tokens

* `renormalized boundary fire`
* `shell divergence`
* `counterterm scars`
* `finite interior response`
* `anomaly halo`

### Failure mode

Either clipping away the shell entirely or treating all shell brightness as meaningful structure.

---

## 11.17 Motif 15 — Observer Patch Dome

### Core principle

For one observer in de Sitter, the event horizon is a preferred finite screen.

### Visual DNA

* enclosing dome
* finite visible universe
* calm horizon boundary
* observer-centric cosmic membrane
* hidden beyond-dome silence

### Scene role

Best as:

* cosmological scenes
* VR-like observer worlds
* consciousness-as-boundary scenes
* finite infinity spaces

### Shader strategy

* spherical or distorted horizon dome
* area-budget-driven complexity
* beyond-dome suppression
* soft thermal or de Sitter bath

### Parameter cluster

```json
{
  "hubbleRate": 0.85,
  "observerHorizonGain": 1.0,
  "screenAreaBudget": 1.0,
  "teleologicalMix": 0.25,
  "beyondPatchSuppression": 0.8
}
```

### Prompt tokens

* `observer patch`
* `de Sitter dome`
* `finite cosmic horizon`
* `preferred null screen`
* `visible universe membrane`

### Failure mode

Treating global de Sitter and observer de Sitter as the same scene.

Bousso explicitly identifies the observer event horizon in de Sitter as a preferred null screen of constant area (4\pi H^{-2}) encoding the observable region.   

---

## 11.18 Motif 16 — Twin Infinity Walls

### Core principle

Global de Sitter can be encoded using paired past and future infinity screens.

### Visual DNA

* two immense opposed walls
* bidirectional projection
* time-sandwiched cosmos
* vast spacelike luminous boundaries
* global rather than observer-centric cosmology

### Scene role

Best as:

* large cosmic halls
* metaphysical universe scenes
* temporal-axis environments
* cosmological murals

### Shader strategy

* two giant asymptotic screen planes/curved walls
* different color signatures for past vs future
* central evolving bulk
* opposing null-projection logic

### Parameter cluster

```json
{
  "pastWallGlow": 0.9,
  "futureWallGlow": 1.1,
  "temporalGradient": 0.5,
  "globalInfinityMode": 1.0,
  "screenSeparation": 8.0
}
```

### Prompt tokens

* `past infinity wall`
* `future infinity wall`
* `global de Sitter screens`
* `temporal cosmos hall`
* `dual asymptotic membranes`

### Failure mode

Collapsing global and observer cosmology into one generic skybox.

Bousso’s global de Sitter construction uses past and future infinity as optimal screen-hypersurfaces for the full spacetime, while the observer horizon is a different, finite preferred screen for the observable patch.  

---

## 11.19 Motif 17 — Future-Horizon Pressure Sea

### Core principle

Present cosmic field strength can be driven by the future event horizon.

### Visual DNA

* low-frequency pressure fog
* anticipatory expansion field
* horizon-linked cosmic wash
* nonlocal large-scale calm
* teleological mood

### Scene role

Best as:

* cosmic environments
* de Sitter-like pressure scenes
* background field for universe-scale chambers
* “dark energy” visual channel

### Shader strategy

* future-horizon estimate in JS
* density field from `rho ~ 1/L^2`
* low-frequency pressure gradients
* very slow, immense motion

### Parameter cluster

```json
{
  "futureEventHorizon": 3.8,
  "holographicMatterDensity": 0.22,
  "teleologicalMix": 0.7,
  "cosmicExpansionGain": 0.55,
  "lowFreqPressure": 0.9
}
```

### Prompt tokens

* `future-horizon pressure`
* `dark energy field`
* `teleological glow`
* `anticipatory cosmos`
* `horizon-linked expansion`

### Failure mode

Treating cosmic expansion as local random fog.

---

## 11.20 Motif 18 — Dark Radiation Residue

### Core principle

A primordial radiation-like residue can survive as a faint early-universe memory channel.

### Visual DNA

* antique ultraviolet grain
* primordial scanlines
* ancient shell noise
* cold blue-violet afterglow
* faint cosmic ash

### Scene role

Best as:

* background layer
* inflation scenes
* cosmology transitions
* subtle memory texture

### Shader strategy

* `a^-4`-like decay channel
* low-amplitude additive residue
* larger-scale coherence than ordinary noise
* time-depth-linked fading

### Parameter cluster

```json
{
  "darkRadiationMix": 0.14,
  "primordialGrain": 0.18,
  "uvAsh": 0.3,
  "epochFade": 0.6,
  "backgroundColdness": 0.5
}
```

### Prompt tokens

* `dark radiation residue`
* `primordial ash`
* `ancient ultraviolet grain`
* `cosmic memory scanlines`
* `early-universe afterglow`

### Failure mode

Making primordial residue louder than the main cosmic field.

---

## 11.21 Motif 19 — Low-l Silence

### Core principle

The largest cosmological modes can be selectively suppressed.

### Visual DNA

* missing giant harmonics
* cosmic silence bands
* large void-like calm
* incomplete spherical music
* absent macrostructure

### Scene role

Best as:

* inflationary scenes
* subtle cosmology unease
* CMB-inspired environments
* huge calm backdrops

### Shader strategy

* suppress lowest spherical / radial modes
* leave mid-scale structure alive
* make the absence visible as elegant emptiness

### Parameter cluster

```json
{
  "inflationCutoff": 0.22,
  "cmbSuppressionBias": 0.5,
  "macroVoidGain": 0.35,
  "harmonicMute": 0.7,
  "midBandSurvival": 0.8
}
```

### Prompt tokens

* `missing harmonics`
* `cosmic silence`
* `low-l suppression`
* `primordial cutoff`
* `void resonance`

### Failure mode

Flattening the whole scene instead of selectively muting the largest modes.

---

## 11.22 Motif 20 — Observer-as-Boundary Interface

### Core principle

The observer can be rendered as a literal boundary condition.

### Visual DNA

* gaze-reactive screens
* mirror-shell feedback
* POV-solidifying geometry
* eyes blooming only when seen
* refractive attention field

### Scene role

Best as:

* interactive scenes
* installation-style pieces
* first-person camera work
* consciousness-heavy portraits

### Shader strategy

* camera vector drives visibility
* screen/pupil activation where gaze intersects
* recursive reflection feedback
* perception-limited emergence

### Parameter cluster

```json
{
  "observerCentricity": 0.9,
  "gazeSolidification": 0.7,
  "reflectionFeedback": 0.55,
  "povWarp": 0.3,
  "attentionBloom": 1.1
}
```

### Prompt tokens

* `observer boundary`
* `gaze-reactive screen`
* `feedback mirror`
* `perception topology`
* `attention field`

### Failure mode

Treating observer-reactivity as a gimmick instead of the actual boundary condition.

The uploaded visual-translation material explicitly includes “observer = boundary” and gaze-reactive emergence as a cognitive-symbolic extension of holographic art logic. 

---

## 11.23 Motif bundles that work especially well

### Bundle A — Classic holographic portrait

* Surface Glyphshell
* Entanglement Filaments
* Renormalized Boundary Fire

### Bundle B — AdS ritual chamber

* AdS Edge Saturation
* Preferred Screen Membrane
* Caustic Cathedral

### Bundle C — Deep-bulk mystery

* Precursor Ghost
* Evanescent Blur Chamber
* Responsibility Cloud

### Bundle D — Black-hole liturgy

* Horizon Glyphfire
* Infall Melt
* Entanglement Tear

### Bundle E — Cosmic holography

* Observer Patch Dome
* Future-Horizon Pressure Sea
* Dark Radiation Residue

### Bundle F — Global cosmology mural

* Twin Infinity Walls
* Low-l Silence
* Preferred Screen Membrane

---

## 11.24 Motif mixing rules

### Mix by scale

* shell motif at small scale
* screen motif at medium scale
* cosmology motif at large scale

### Mix by function

* one motif for encoding
* one motif for relation
* one motif for failure
* one motif for atmosphere

### Mix by causal status

* present-visible motif
* hidden/nonlocal motif
* future-horizon or screen motif

### Do not mix three failure motifs at once

Example bad combo:

* Entanglement Tear
* Evanescent Blur Chamber
* Infall Melt
* Low-l Silence

That can erase too much legibility.

---

## 11.25 Quick preset table

| Preset             | Motifs                                                              | Use case                |
| ------------------ | ------------------------------------------------------------------- | ----------------------- |
| `glyph_saint`      | Surface Glyphshell + Renormalized Boundary Fire                     | Holographic portraits   |
| `ads_basilica`     | AdS Edge Saturation + Caustic Cathedral + Preferred Screen Membrane | Cathedral/tunnel scenes |
| `entropy_lovers`   | Entanglement Filaments + RT Soap-Film Bridge                        | Paired figures          |
| `black_sun`        | Horizon Glyphfire + Infall Melt                                     | Black-hole scenes       |
| `precursor_oracle` | Precursor Ghost + Responsibility Cloud                              | Latent-event scenes     |
| `de_sitter_eye`    | Observer Patch Dome + Observer-as-Boundary Interface                | Interactive cosmic POV  |
| `future_tide`      | Future-Horizon Pressure Sea + Dark Radiation Residue                | Cosmic ambient fields   |

---

## 11.26 Anti-cheapening rules

A motif becomes fake when it loses the law that produced it.

Examples:

* threads without relation weights
* membranes without minimal/tension logic
* screens without null-projection logic
* horizons without shell thermalization
* cosmology without changing information budget
* blur without recoverability logic
* observer-reactivity without actual view dependence

That is the main thing this repo should protect against.

---

## 11.27 Minimal machine-readable schema

```json
[
  {
    "id": "surface_glyphshell",
    "category": "encoding",
    "core_principle": "boundary stores information",
    "scene_role": ["portrait", "object", "seed_surface"],
    "shader_modules": ["shell_sdf", "glyph_uv", "source_field"],
    "params": ["shellGlow", "glyphDensity", "interiorOpacity"],
    "prompt_tokens": ["glyphshell", "surface-encoded", "hollow interior"],
    "failure_mode": "interior more detailed than shell"
  },
  {
    "id": "rt_soap_film_bridge",
    "category": "relation",
    "core_principle": "shared information becomes membrane",
    "scene_role": ["bridge", "shared_chamber", "pair_scene"],
    "shader_modules": ["membrane_sdf", "entanglement_field"],
    "params": ["bridgeThreshold", "rtTension", "membraneGlow"],
    "prompt_tokens": ["minimal surface", "soap-film bridge", "entanglement membrane"],
    "failure_mode": "bridge rendered as tube"
  }
]
```

---

## 11.28 The actual point of this file

This library is here so that when you build scenes you can say:

* “I want an observer-patch de Sitter portrait with glyphshell skin and precursor ghosts.”
* “I want a black-hole chamber with screen-cathedral ribs and an entanglement tear.”
* “I want a future-horizon cosmic sea with low-l silence and dark-radiation residue.”

And those words map to actual code structure instead of just vibes.

```

The motifs above are anchored to the uploaded theory and visual-translation docs: surface-first encoding and radial scale gradients for shell and AdS motifs, entanglement threads and minimal membranes for relation motifs, screens and preferred screens for null-projection motifs, de Sitter observer and global screens for cosmological motifs, and stretched-horizon/light-sheet logic for horizon scenes. :contentReference[oaicite:28]{index=28} :contentReference[oaicite:29]{index=29} :contentReference[oaicite:30]{index=30} :contentReference[oaicite:31]{index=31} :contentReference[oaicite:32]{index=32} :contentReference[oaicite:33]{index=33}

Next logical file is `docs/12_glossary.md`, and after that I’d start generating actual repo assets like `theory/concepts.json`, `theory/visual_mappings.json`, and the first GLSL scene files from these motif presets.
```
Yep — here’s the missing tail from that response:

The motifs above are anchored to the uploaded theory and visual-translation docs: surface-first encoding and radial scale gradients for shell and AdS motifs, entanglement threads and minimal membranes for relation motifs, screens and preferred screens for null-projection motifs, de Sitter observer and global screens for cosmological motifs, and stretched-horizon/light-sheet logic for horizon scenes.

Next logical file is `docs/12_glossary.md`, and after that I’d start generating actual repo assets like `theory/concepts.json`, `theory/visual_mappings.json`, and the first GLSL scene files from these motif presets.

If you want, I’ll go straight into `docs/12_glossary.md` next.

Absolutely — here’s the next file.

# `docs/12_glossary.md`

```md id="mvj7pq"
# 12 — Glossary

## 12.1 How to read this glossary

This glossary is not written like a pure physics textbook.
Each term has three layers:

- **Physics meaning** — what it means in holography/math
- **Repo meaning** — how we use it in this project
- **Code meaning** — what it tends to become in JS / GLSL / raymarching

The aim is to make every important word in the repo executable.

---

## AdS (Anti-de Sitter space)

**Physics meaning**  
A spacetime with negative cosmological constant, used as the canonical bulk geometry in AdS/CFT.

**Repo meaning**  
The default warped bulk space where radial depth behaves like scale.

**Code meaning**  
Usually a depth warp, metric-inspired scaling field, or coordinate remap.

**Related**  
UV/IR, boundary, bulk, Fefferman–Graham, holographic renormalization

---

## AdS/CFT

**Physics meaning**  
A holographic duality relating gravity in AdS to a conformal field theory on its boundary.

**Repo meaning**  
The master blueprint for “boundary data generates bulk structure.”

**Code meaning**  
The architecture rule: source maps on a lower-dimensional surface control higher-dimensional render behavior.

---

## anomaly

**Physics meaning**  
A symmetry-related leftover that survives renormalization, often appearing through logarithmic terms in near-boundary expansions.

**Repo meaning**  
A residual visual trace that should remain after subtracting local divergences.

**Code meaning**  
Thin chromatic scars, interference bands, or log-sensitive shell glows.

---

## apparent horizon

**Physics meaning**  
A surface where at least one orthogonal null expansion vanishes. In many constructions it separates normal, trapped, and anti-trapped regions.

**Repo meaning**  
A pivot surface where projection behavior changes.

**Code meaning**  
A mask or surface where beam direction, screen logic, or readout confidence flips.

---

## area law

**Physics meaning**  
The idea that entropy in gravitational systems scales with area rather than volume, especially in black-hole entropy.

**Repo meaning**  
Complexity budget should often scale with visible or effective surface area.

**Code meaning**  
Detail budget, symbol count, or sample density driven by shell area instead of object volume.

---

## asymptotic expansion

**Physics meaning**  
A near-boundary series expansion of bulk fields or metrics, with leading source terms and subleading response terms.

**Repo meaning**  
The rule that the boundary tells you how the deep bulk should begin to unfold.

**Code meaning**  
A layered field like `source + rho*response + rho^2*... + log(rho)*anomaly`.

The near-boundary Fefferman–Graham setup, source/response split, and log-term anomaly structure are core parts of holographic renormalization, while AdS/CFT itself provides the bulk/boundary framework that makes this split meaningful. :contentReference[oaicite:0]{index=0} :contentReference[oaicite:1]{index=1}

---

## beam fan

**Physics meaning**  
Not a formal term, but a useful visual name for a family of null rays or light-sheet generators.

**Repo meaning**  
A visible projection family used to reconstruct bulk form from a surface.

**Code meaning**  
A set of directional density lobes or converging ray bundles.

---

## Bekenstein–Hawking entropy

**Physics meaning**  
The entropy of a black hole, proportional to one quarter of the horizon area in Planck units.

**Repo meaning**  
The reason horizons should be treated as information membranes.

**Code meaning**  
Horizon area sets shell complexity, glyph count, photon-ring richness, or thermal texture budget.

---

## bit thread

**Physics meaning**  
A flow-based reformulation of holographic entanglement where flux lines represent entanglement structure.

**Repo meaning**  
A good mental model for entanglement filaments and relation flows.

**Code meaning**  
Directed strand fields, flux curves, or graph-to-line render systems.

---

## black-hole complementarity

**Physics meaning**  
A principle proposed to preserve quantum mechanics in black-hole physics while giving up naive observer-independent locality across the horizon.

**Repo meaning**  
Outside and infalling viewpoints should not be rendered as one naive unified visual story.

**Code meaning**  
Observer-mode switch, horizon-shell logic, and different readout rules inside vs outside.

---

## boundary

**Physics meaning**  
The lower-dimensional surface or asymptotic region on which dual data lives.

**Repo meaning**  
The control surface for bulk generation.

**Code meaning**  
A UV map, shell surface, screen, horizon skin, or explicit operator texture domain.

---

## boundary operator

**Physics meaning**  
An operator in the boundary theory dual to a bulk field or excitation.

**Repo meaning**  
An encoded source unit on the shell or screen.

**Code meaning**  
A source texture, symbolic activation patch, graph node, or time-varying control cell.

---

## bulk

**Physics meaning**  
The higher-dimensional spacetime region dual to the boundary theory.

**Repo meaning**  
The interior generated from boundary logic.

**Code meaning**  
The 3D or 4D field domain your shaders raymarch through.

The boundary/bulk relation, the extrapolate dictionary, and local-vs-nonlocal reconstruction all come from the holographic duality framework and its reconstruction machinery. :contentReference[oaicite:2]{index=2} :contentReference[oaicite:3]{index=3}

---

## bulk reconstruction

**Physics meaning**  
The attempt to recover bulk operators or fields from boundary data.

**Repo meaning**  
The process that turns shell data into interior form.

**Code meaning**  
A smearing kernel, nonlocal support cloud, precursor path, or coarse-grained recovery function.

---

## bulk-to-boundary propagator

**Physics meaning**  
A kernel used to connect bulk fields to boundary source behavior, especially in Euclidean/source contexts.

**Repo meaning**  
A source-injection map, not always the same as a Lorentzian smearing map.

**Code meaning**  
A source transfer operator, usually distinct from the reconstruction kernel used for state data.

---

## caustic

**Physics meaning**  
A focusing region where nearby null generators intersect or become singular as a congruence.

**Repo meaning**  
A projection termination membrane or compression zone.

**Code meaning**  
A bright shell, focal band, or stopping surface where beams collapse.

---

## causal wedge / causally connected region

**Physics meaning**  
The bulk region causally accessible from a given boundary region.

**Repo meaning**  
The admissible bulk territory owned by a boundary patch.

**Code meaning**  
A domain mask limiting which points a boundary segment can validly influence.

---

## coarse graining

**Physics meaning**  
A lower-resolution description that smooths over fine detail.

**Repo meaning**  
The correct fallback when sharp reconstruction is impossible.

**Code meaning**  
Blur, mip selection, low-pass filtering, reduced harmonic recovery, or reduced sample depth.

---

## congruence

**Physics meaning**  
A family of curves, often geodesics; in this repo, usually null geodesics.

**Repo meaning**  
A projection family.

**Code meaning**  
A set of beam directions or ray generators.

---

## conformal boundary

**Physics meaning**  
The boundary obtained after conformal compactification, often where the boundary theory is defined.

**Repo meaning**  
The ideal outer encoding surface of AdS-like scenes.

**Code meaning**  
The `z -> 0` side of the scene or the outer cutoff shell of an AdS warp.

---

## counterterm

**Physics meaning**  
A local covariant subtraction term added at a cutoff surface to remove divergences and define finite observables.

**Repo meaning**  
A structured subtraction layer, not a random fade or clip.

**Code meaning**  
A local shell function depending on cutoff, field value, and derivatives.

---

## covariant entropy bound

**Physics meaning**  
The statement that entropy on an allowed light-sheet is bounded by one quarter of the area of the generating surface.

**Repo meaning**  
The reason projection must be null, directional, and expansion-aware.

**Code meaning**  
A rule that only certain beam families are valid, and that their information budget is area-controlled.

The covariant bound uses null congruences with nonpositive expansion, and Bousso’s screen construction builds preferred screens by projecting along directions of nonnegative expansion until the sign flips. :contentReference[oaicite:4]{index=4} :contentReference[oaicite:5]{index=5}

---

## de Sitter space

**Physics meaning**  
A spacetime with positive cosmological constant, central to cosmology and horizon thermodynamics.

**Repo meaning**  
The main cosmological holography lab.

**Code meaning**  
Either an observer-patch dome scene or a global paired-screen cosmology scene.

---

## detail budget

**Physics meaning**  
Not a formal physics term.

**Repo meaning**  
How much visible complexity a surface or region is allowed to carry.

**Code meaning**  
Noise octaves, symbol count, ray steps, line density, or microstructure frequency.

---

## dual theory

**Physics meaning**  
A lower-dimensional nongravitational theory equivalent to a gravitational bulk description, such as the CFT in AdS/CFT.

**Repo meaning**  
The readable surface-side description of a bulk scene.

**Code meaning**  
The API/control/schema layer that defines source-side logic.

---

## entanglement entropy

**Physics meaning**  
The entropy associated with tracing out part of a quantum system.

**Repo meaning**  
The master relation measure that becomes geometry in holographic scenes.

**Code meaning**  
A graph weight, bridge cost, membrane area proxy, or wedge-overlap strength.

---

## entanglement wedge

**Physics meaning**  
The bulk region naturally associated with a boundary subregion and its extremal surface.

**Repo meaning**  
The interior chamber owned by a boundary patch.

**Code meaning**  
A wedge field, region mask, or interior ownership volume.

---

## evanescent mode

**Physics meaning**  
A mode whose amplitude is exponentially suppressed at the boundary while growing toward the interior.

**Repo meaning**  
A hidden-bulk mode that destroys naive sharp readout.

**Code meaning**  
A field that forces coarse-grained reconstruction, blurred recovery, or probe-only reveal.

Rosenhaus’s reconstruction discussions treat evanescent modes as the reason sharp local reconstruction can require exponential precision, motivating coarse-grained recovery instead. :contentReference[oaicite:6]{index=6}

---

## event horizon

**Physics meaning**  
The causal boundary beyond which signals cannot escape to a given observer.

**Repo meaning**  
A hidden-region threshold and a screen candidate.

**Code meaning**  
A geometric radius or surface where readout confidence collapses.

---

## expansion scalar (theta)

**Physics meaning**  
A measure of whether a congruence is focusing or diverging.

**Repo meaning**  
The validity test for light-sheets and the detector for preferred screens.

**Code meaning**  
A signed field that gates beam families and highlights theta-zero surfaces.

---

## Fefferman–Graham coordinates

**Physics meaning**  
A standard near-boundary coordinate system for asymptotically AdS spacetimes.

**Repo meaning**  
The coordinate language of source/response extraction.

**Code meaning**  
A cutoff-coordinate system using `rho` or similar depth to organize asymptotic terms.

---

## finite part

**Physics meaning**  
The renormalized remainder after divergent terms and counterterms are handled.

**Repo meaning**  
The actually meaningful visible response.

**Code meaning**  
The quantity you usually want to shade after subtracting local shell artifacts.

---

## future event horizon

**Physics meaning**  
A horizon length defined by integrating the future expansion history of the universe.

**Repo meaning**  
The preferred IR scale for holographic dark-energy scenes.

**Code meaning**  
A forecast-driven cosmic control parameter that sets low-frequency pressure fields.

The cosmology notes explicitly use the future event horizon as the IR scale in holographic dark energy, leading to a density of order \(3C^2M_p^2/L^2\), and Bousso shows that de Sitter also has preferred screen structures tied to horizons and infinity. :contentReference[oaicite:7]{index=7} :contentReference[oaicite:8]{index=8}

---

## global screen

**Physics meaning**  
A screen used to encode an entire spacetime or a very large part of it.

**Repo meaning**  
A universe-scale display surface.

**Code meaning**  
A huge wall, infinity surface, or evolving screen-hypersurface stack.

---

## holographic dark energy / holographic matter

**Physics meaning**  
A cosmological energy-density model of order \(3C^2M_p^2/L^2\), often tied to an IR horizon scale.

**Repo meaning**  
A cosmic pressure field linked to screen size or future horizon size.

**Code meaning**  
A low-frequency density field controlling expansive cosmic mood.

---

## holographic principle

**Physics meaning**  
The idea that the information content of a region is bounded by surface area rather than volume, with covariant realization on light-sheets.

**Repo meaning**  
The base rule of the whole project.

**Code meaning**  
Boundary-driven interiors, area-driven detail budgets, and null-projection logic.

---

## holographic renormalization

**Physics meaning**  
The near-boundary regularization and subtraction procedure used to define finite observables in holographic setups.

**Repo meaning**  
How the repo keeps boundary blow-up meaningful instead of messy.

**Code meaning**  
Cutoff control, asymptotic decomposition, counterterm subtraction, and anomaly preservation.

---

## horizon area budget

**Physics meaning**  
A repo phrase derived from the black-hole area law.

**Repo meaning**  
The complexity budget assigned to a horizon shell by its area.

**Code meaning**  
A function of radius used to drive shell detail and emission structure.

---

## light-ray / null geodesic

**Physics meaning**  
A null curve in spacetime.

**Repo meaning**  
The path along which valid holographic projection happens.

**Code meaning**  
A directional ray family or beam trajectory.

---

## light-sheet

**Physics meaning**  
A null hypersurface generated from a surface along an orthogonal congruence with nonpositive expansion.

**Repo meaning**  
The valid projection manifold for holographic information flow.

**Code meaning**  
A converging beam density field with termination rules.

---

## membrane

**Physics meaning**  
A general geometric surface. In this repo it often means an RT-like bridge, screen, or horizon skin.

**Repo meaning**  
A visible information-bearing surface.

**Code meaning**  
A thin SDF shell or tensioned sheet field.

---

## minimal surface

**Physics meaning**  
A surface of extremal/minimal area, central to the Ryu–Takayanagi prescription in static cases.

**Repo meaning**  
The geometric form of shared information.

**Code meaning**  
A bridge membrane, saddle field, or tension-minimized sheet.

Headrick’s notes motivate RT by the black-hole horizon and then state the general area formula for entanglement in static holographic settings. :contentReference[oaicite:9]{index=9} :contentReference[oaicite:10]{index=10}

---

## mutual information

**Physics meaning**  
A measure of shared information between two subsystems.

**Repo meaning**  
A threshold-control quantity for deciding whether bridges or shared chambers should appear.

**Code meaning**  
A graph weight or phase variable controlling connected vs disconnected geometry.

---

## null projection

**Physics meaning**  
Projection of information along null hypersurfaces.

**Repo meaning**  
The correct holographic projection logic.

**Code meaning**  
Beam-march or screen-construction logic based on null directions.

---

## observer patch

**Physics meaning**  
The causally accessible region for a given observer, especially in de Sitter.

**Repo meaning**  
The finite visible universe mode.

**Code meaning**  
A dome-like horizon-bounded scene with beyond-horizon suppression.

---

## operator support cloud

**Physics meaning**  
Not a standard formal term; a repo phrase for the distributed boundary support controlling a bulk point.

**Repo meaning**  
A visualization of smearing support.

**Code meaning**  
A weighted set of boundary samples or a debug overlay.

---

## photon ring

**Physics meaning**  
A thin optical ring associated with strongly lensed null trajectories near a compact object.

**Repo meaning**  
A horizon-adjacent visual accent.

**Code meaning**  
A thin bright band around a black-hole shell.

---

## precursor

**Physics meaning**  
A highly nonlocal boundary operator encoding bulk information before local boundary observables can cleanly reveal it.

**Repo meaning**  
The hidden global channel for early or unreachable bulk knowledge.

**Code meaning**  
A global low-frequency field, graph mode, or distributed latent control path.

---

## preferred screen

**Physics meaning**  
A screen on which the expansion vanishes.

**Repo meaning**  
A specially efficient display membrane.

**Code meaning**  
A theta-zero emphasis surface with extra coherence, brightness, or legibility.

---

## radial depth

**Physics meaning**  
In AdS-like settings, the extra coordinate organizing scale.

**Repo meaning**  
Depth is not just distance; it is descriptive scale.

**Code meaning**  
A coordinate controlling frequency, warp strength, blur, and RG behavior.

Bousso’s preferred-screen logic, de Sitter/FRW screen examples, and AdS boundary/cutoff intuition all feed this repo notion that geometry uses special surfaces and special depth, not plain Euclidean distance. :contentReference[oaicite:11]{index=11} :contentReference[oaicite:12]{index=12} :contentReference[oaicite:13]{index=13}

---

## recoverable depth

**Physics meaning**  
A repo phrase inspired by evanescent-mode limits: how deep sharp reconstruction can go at a given resolution.

**Repo meaning**  
The maximum depth where your current readout can still be trusted.

**Code meaning**  
A scalar threshold tied to coarse-graining scale.

---

## reconstruction confidence

**Physics meaning**  
Not a standard formal term.

**Repo meaning**  
A numerical estimate of how valid local bulk readout is.

**Code meaning**  
A mask combining trapped-geodesic, horizon, support, and evanescence tests.

---

## Rényi entropy

**Physics meaning**  
A family of entropies generalizing von Neumann entropy.

**Repo meaning**  
Useful when you want multiple relation scales rather than one single entanglement number.

**Code meaning**  
Alternative graph-weight channels or multi-order relation metrics.

---

## response field

**Physics meaning**  
The subleading or state-dependent bulk reaction to a source.

**Repo meaning**  
What the interior does because of the boundary, not what the boundary explicitly writes.

**Code meaning**  
A derived field layered on top of source data.

---

## RG flow / renormalization-group flow

**Physics meaning**  
Scale evolution of an effective description.

**Repo meaning**  
The reason deeper bulk becomes coarser or more collective.

**Code meaning**  
Depth-dependent filtering, structure merging, or frequency suppression.

---

## Ryu–Takayanagi formula (RT)

**Physics meaning**  
For static holographic states, entanglement entropy of a boundary region equals bulk minimal-surface area divided by \(4G_N\).

**Repo meaning**  
Connectivity becomes membrane geometry.

**Code meaning**  
A bridge-phase engine driven by relation weights and minimal/tension logic.

---

## screen

**Physics meaning**  
A surface obtained by projecting along a null hypersurface in the noncontracting direction and stopping before expansion turns negative.

**Repo meaning**  
A place where the universe “chooses to display itself.”

**Code meaning**  
A projection membrane, display surface, or layered screen stack element.

---

## screen-hypersurface

**Physics meaning**  
A one-parameter family of screens.

**Repo meaning**  
A moving or layered display geometry across spacetime.

**Code meaning**  
An animated stack of screens or membrane sequence.

---

## smearing function / smearing kernel

**Physics meaning**  
A kernel expressing a bulk operator as a distributed integral over boundary operators in favorable settings.

**Repo meaning**  
The main local reconstruction engine.

**Code meaning**  
A weighted sum over boundary samples across space and time.

Bousso’s screen language, RT’s geometry, and Rosenhaus’s reconstruction program all reinforce the repo split between local kernels, preferred surfaces, and nonlocal rescue channels when local readout fails. :contentReference[oaicite:14]{index=14} :contentReference[oaicite:15]{index=15}

---

## source field

**Physics meaning**  
The leading boundary datum acting as a source for a dual operator.

**Repo meaning**  
What you intentionally write on the shell or screen.

**Code meaning**  
A boundary texture, glyph map, operator channel, or explicit control texture.

---

## stretched horizon

**Physics meaning**  
A timelike surface just outside the true event horizon, used as the operational membrane in complementarity-style descriptions.

**Repo meaning**  
The visible horizon skin.

**Code meaning**  
A shell offset from the mathematical horizon where scrambling and glow are rendered.

The cosmology review explicitly describes the stretched horizon as a timelike surface just outside the mathematical lightlike horizon and discusses mapping horizon data onto screens via light-sheets. :contentReference[oaicite:16]{index=16}

---

## theta-zero surface

**Physics meaning**  
A surface where the null expansion vanishes.

**Repo meaning**  
Usually a preferred screen candidate.

**Code meaning**  
A highlight mask for stable membranes or balanced encoding zones.

---

## trapped null geodesic

**Physics meaning**  
A null geodesic that does not escape cleanly to the boundary.

**Repo meaning**  
A warning sign that local reconstruction may fail.

**Code meaning**  
A geometry flag that lowers reconstruction confidence and increases nonlocal/coarse-grained behavior.

---

## UV/IR relation

**Physics meaning**  
The correspondence between ultraviolet structure in the boundary theory and infrared/near-boundary cutoff structure in the bulk.

**Repo meaning**  
Outer depth is high detail; deeper bulk is lower-resolution collective structure.

**Code meaning**  
Depth-based frequency scaling and boundary cutoff logic.

---

## volume illusion

**Physics meaning**  
Not a standard term.

**Repo meaning**  
The sense that a large interior is being reconstructed from a lower-dimensional source.

**Code meaning**  
Hollow interiors, beam-built bodies, or shell-extruded field volumes.

---

## von Neumann entropy

**Physics meaning**  
The entropy \(S = -\mathrm{tr}(\rho \log \rho)\) of a quantum state.

**Repo meaning**  
The baseline notion of entropy beneath entanglement discussions.

**Code meaning**  
Usually an abstract metric rather than a directly rendered field.

---

## wedge

**Physics meaning**  
A region selected by causal or entanglement structure, such as a causal wedge or entanglement wedge.

**Repo meaning**  
A bulk ownership zone.

**Code meaning**  
A region mask, cone, or volume field tied to a boundary patch.

---

## z cutoff / rho cutoff

**Physics meaning**  
A bulk cutoff surface near the AdS boundary used to regulate divergences.

**Repo meaning**  
The place where source-rich shell behavior is sampled and renormalized.

**Code meaning**  
`boundaryDepth`, `cutoffRho`, or `zMin`.
```

A lot of the glossary’s backbone comes straight from Bousso’s definitions of screens, preferred screens, and projection; from the cosmology notes on stretched horizons, future event horizons, and holographic matter density; and from the RT/entanglement notes on minimal surfaces and entropy geometry.    

Now that the doc spine is in place, the next useful move is to start generating the actual repo assets:

* `theory/concepts.json`
* `theory/equations.json`
* `theory/visual_mappings.json`
* first shader scene templates

Good. Now we switch from docs into **machine-readable repo assets**.

These files turn the theory stack into something your app can actually load: concepts, mappings, presets, and module routing. The structure below keeps the important distinctions intact — source vs response, RT membranes vs decorative links, screens vs shells, and local vs nonlocal reconstruction modes.    

---

# `theory/concepts.json`

```json id="l5x4tg"
[
  {
    "id": "holographic_principle",
    "name": "Holographic Principle",
    "physics_meaning": "Information content scales with boundary area rather than bulk volume.",
    "math_forms": [
      "S_BH = A / (4 G_N ħ)",
      "S[L(B)] <= A(B) / 4",
      "N_dof <= A / 4"
    ],
    "render_laws": [
      "surface controls interior",
      "area controls detail budget",
      "volume can be hollow",
      "projection should favor shells and scanfronts"
    ],
    "parameters": [
      "boundaryDensity",
      "areaBudget",
      "projectionSharpness",
      "entropyGlow",
      "interiorOpacity"
    ],
    "modules": [
      "shell_sdf",
      "source_field",
      "boundary_data_field"
    ],
    "motifs": [
      "surface_glyphshell",
      "renormalized_boundary_fire"
    ],
    "failure_modes": [
      "uniform detail everywhere",
      "interior more detailed than boundary",
      "purely decorative shell"
    ]
  },
  {
    "id": "ads_geometry_and_scale",
    "name": "AdS Geometry and Scale",
    "physics_meaning": "Radial bulk depth organizes scale; near-boundary zones encode UV detail and deeper bulk encodes coarser structure.",
    "math_forms": [
      "ds^2 = (L^2 / z^2)(dz^2 + dx_mu dx^mu)",
      "(t,x,z) -> (lambda t, lambda x, lambda z)",
      "ds^2 = d rho^2/(4 rho^2) + (1/rho) g_ij(x,rho) dx^i dx^j"
    ],
    "render_laws": [
      "radial depth = scale",
      "boundary zones carry finer frequencies",
      "deep bulk softens into collective fields"
    ],
    "parameters": [
      "boundaryDepth",
      "warpIntensity",
      "uvIrMix",
      "boundaryDetailGain",
      "deepBulkSoftness"
    ],
    "modules": [
      "ads_spaces",
      "uv_ir_field",
      "radial_flows"
    ],
    "motifs": [
      "ads_edge_saturation"
    ],
    "failure_modes": [
      "uniform frequency across depth",
      "no boundary cutoff logic",
      "plain Euclidean tunnel"
    ]
  },
  {
    "id": "bulk_boundary_dictionary",
    "name": "Bulk Boundary Dictionary",
    "physics_meaning": "Bulk fields are encoded by boundary operators; finite-depth reconstruction may use smearing kernels or nonlocal structures.",
    "math_forms": [
      "lim_{z->0} z^{-Delta} Phi(x,t,z) = O(x,t)",
      "Phi(B) = ∫ db' K(B|b') O(b') + ...",
      "Z_bulk[phi_(0)] = <exp(-∫ phi_(0) O)>"
    ],
    "render_laws": [
      "bulk points depend on distributed boundary support",
      "source and response must be separated",
      "global channels can encode deep events"
    ],
    "parameters": [
      "smearRadius",
      "smearTimeWindow",
      "responseGain",
      "causalLag",
      "nonlocalMix"
    ],
    "modules": [
      "operator_smear",
      "source_field",
      "response_field",
      "boundary_encoders"
    ],
    "motifs": [
      "responsibility_cloud"
    ],
    "failure_modes": [
      "one UV point controls one bulk point",
      "source and response collapsed together"
    ]
  },
  {
    "id": "entanglement_geometry",
    "name": "Entanglement Geometry",
    "physics_meaning": "Entanglement entropy is represented by bulk minimal surfaces in static holographic settings.",
    "math_forms": [
      "S(A) = Area(gamma_A) / (4 G_N)",
      "I(A:B) = S(A) + S(B) - S(A union B)",
      "I(A:BC) >= I(A:B) + I(A:C)"
    ],
    "render_laws": [
      "relation creates bridges",
      "shared information creates membranes",
      "disconnects tear geometry",
      "bridge phase can change abruptly"
    ],
    "parameters": [
      "entanglementWeight",
      "bridgeThreshold",
      "rtTension",
      "sharedInteriorWeight",
      "cutSeverity"
    ],
    "modules": [
      "entanglement_field",
      "membrane_sdfs",
      "entanglement_graphs"
    ],
    "motifs": [
      "entanglement_filaments",
      "rt_soap_film_bridge",
      "entanglement_tear"
    ],
    "failure_modes": [
      "random webbing",
      "tube bridges instead of membranes",
      "no connected/disconnected phase logic"
    ]
  },
  {
    "id": "holographic_renormalization",
    "name": "Holographic Renormalization",
    "physics_meaning": "Near-boundary divergences are regulated by a cutoff and removed with local covariant counterterms, leaving finite source/response data and anomalies.",
    "math_forms": [
      "F(x,rho)=rho^m[f_(0)+f_(2)rho+...+rho^n(f_(2n)+log(rho)f~_(2n))+...]",
      "ds^2 = d rho^2/(4 rho^2) + (1/rho) g_ij(x,rho) dx^i dx^j"
    ],
    "render_laws": [
      "boundary blow-up is structured",
      "subtract local shell artifacts",
      "preserve finite response",
      "keep anomaly residue visible"
    ],
    "parameters": [
      "cutoffRho",
      "countertermStrength",
      "shellDivergenceGain",
      "finitePartGain",
      "anomalyMix"
    ],
    "modules": [
      "renorm_field",
      "post_renorm_debug"
    ],
    "motifs": [
      "renormalized_boundary_fire"
    ],
    "failure_modes": [
      "hard-clipping the boundary",
      "dropping divergences by hand",
      "discarding anomaly structure"
    ]
  },
  {
    "id": "black_holes_and_horizons",
    "name": "Black Holes and Horizons",
    "physics_meaning": "Horizon area sets black-hole entropy; the stretched horizon acts as an operational information membrane and local readout degrades near and behind it.",
    "math_forms": [
      "S_BH = A_hor / (4 G_N)",
      "r_s = 2MG",
      "S_total = S_matter + S_BH"
    ],
    "render_laws": [
      "horizon area controls shell complexity",
      "true horizon and stretched horizon should be separated",
      "infalling identity thermalizes near shell",
      "interior readout collapses"
    ],
    "parameters": [
      "horizonRadius",
      "stretchedHorizonWidth",
      "thermalScramble",
      "hawkingGlow",
      "interiorReadoutLoss"
    ],
    "modules": [
      "blackhole_sdfs",
      "horizon_scramble",
      "photon_ring"
    ],
    "motifs": [
      "horizon_glyphfire",
      "infall_melt"
    ],
    "failure_modes": [
      "matte black sphere",
      "fully readable interior",
      "no shell thermalization"
    ]
  },
  {
    "id": "light_sheets_and_screens",
    "name": "Light-Sheets and Screens",
    "physics_meaning": "Allowed holographic projection occurs along null congruences with nonpositive expansion; screens collect projected information and preferred screens occur at zero expansion.",
    "math_forms": [
      "S[L(B)] <= A(B) / 4",
      "L(b) = L+(b) ∩ L-(b)",
      "H(b) = C(b) = L(b)"
    ],
    "render_laws": [
      "projection should be directional and convergent",
      "beam families terminate at caustics or sign flips",
      "screens are display membranes",
      "theta-zero surfaces deserve emphasis"
    ],
    "parameters": [
      "nullExpansion",
      "sheetTerminationMode",
      "causticGain",
      "screenThreshold",
      "preferredScreenBias"
    ],
    "modules": [
      "light_sheet_field",
      "screen_sdfs",
      "null_geodesic_camera"
    ],
    "motifs": [
      "light_sheet_hologram",
      "preferred_screen_membrane",
      "caustic_cathedral"
    ],
    "failure_modes": [
      "uniform inward projection",
      "arbitrary decorative planes",
      "ignoring caustics"
    ]
  },
  {
    "id": "nonlocality_and_smearing",
    "name": "Nonlocality and Smearing",
    "physics_meaning": "Local smearing works in favorable backgrounds, but trapped null geodesics, barriers, and evanescent modes can force nonlocal or coarse-grained reconstruction.",
    "math_forms": [
      "Phi(B) = ∫ db' K(B|b') O(b')",
      "d/dr (f/r^2) > 0",
      "z < sigma"
    ],
    "render_laws": [
      "support clouds are distributed",
      "precursors can know too early",
      "some deep zones should stay blurry",
      "resolution and recoverable depth trade off"
    ],
    "parameters": [
      "precursorWeight",
      "kernelFailureMode",
      "trappedGeodesicBias",
      "evanescentWeight",
      "reconstructionSigma"
    ],
    "modules": [
      "operator_smear",
      "precursor_field",
      "reconstruction_modes"
    ],
    "motifs": [
      "precursor_ghost",
      "evanescent_blur_chamber",
      "responsibility_cloud"
    ],
    "failure_modes": [
      "perfect crisp readout in hostile geometry",
      "no nonlocal rescue channel",
      "no depth-resolution tradeoff"
    ]
  },
  {
    "id": "cosmology_and_holographic_matter",
    "name": "Cosmology and Holographic Matter",
    "physics_meaning": "Cosmological holography uses time-dependent and embedded screens; holographic matter density is tied to an IR scale such as the future event horizon.",
    "math_forms": [
      "ds^2 = a^2(η)[-dη^2 + dχ^2 + f^2(χ)dΩ^2]",
      "A_E = 4πH^(-2)",
      "ρ_HM = 3 C^2 M_p^2 / L^2",
      "L_f = a(t) ∫_t^∞ dt' / a(t')"
    ],
    "render_laws": [
      "screen area changes active complexity",
      "future horizons can control present fields",
      "observer patch and global cosmology should be distinct",
      "dark-radiation residue can carry early-universe memory"
    ],
    "parameters": [
      "scaleFactor",
      "hubbleRate",
      "futureEventHorizon",
      "screenAreaBudget",
      "darkRadiationMix"
    ],
    "modules": [
      "cosmology_state",
      "cosmic_pressure",
      "screen_stack"
    ],
    "motifs": [
      "observer_patch_dome",
      "twin_infinity_walls",
      "future_horizon_pressure_sea",
      "dark_radiation_residue",
      "low_l_silence"
    ],
    "failure_modes": [
      "fixed information budget in expanding scenes",
      "no distinction between observer and global de Sitter",
      "random fog instead of horizon-linked field"
    ]
  }
]
```

---

# `theory/visual_mappings.json`

```json id="79y22t"
[
  {
    "id": "surface_glyphshell",
    "concepts": ["holographic_principle"],
    "category": "encoding",
    "shader_modules": ["shell_sdf", "glyph_uv", "source_field"],
    "scene_roles": ["portrait", "object", "seed_surface"],
    "preset_params": {
      "shellGlow": 1.4,
      "glyphDensity": 18.0,
      "interiorOpacity": 0.15
    },
    "prompt_tokens": ["surface-encoded", "glyphshell", "hollow interior"]
  },
  {
    "id": "ads_edge_saturation",
    "concepts": ["ads_geometry_and_scale"],
    "category": "scale",
    "shader_modules": ["ads_spaces", "uv_ir_field"],
    "scene_roles": ["tunnel", "cathedral", "zoom_space"],
    "preset_params": {
      "boundaryDepth": 0.03,
      "warpIntensity": 1.15,
      "uvIrMix": 0.8
    },
    "prompt_tokens": ["uv-ir depth", "recursive tunnel", "boundary saturation"]
  },
  {
    "id": "light_sheet_hologram",
    "concepts": ["light_sheets_and_screens"],
    "category": "projection",
    "shader_modules": ["light_sheet_field", "screen_sdfs"],
    "scene_roles": ["scanner", "projection_scene", "reveal_sequence"],
    "preset_params": {
      "beamFocus": 7.5,
      "sheetFalloff": 1.1,
      "causticGain": 1.25
    },
    "prompt_tokens": ["light-sheet", "converging beams", "null scanfront"]
  },
  {
    "id": "preferred_screen_membrane",
    "concepts": ["light_sheets_and_screens"],
    "category": "display_surface",
    "shader_modules": ["screen_sdfs", "light_sheet_field"],
    "scene_roles": ["display_plane", "membrane", "screen_stack"],
    "preset_params": {
      "screenThreshold": 0.03,
      "preferredScreenBias": 1.6,
      "screenOpacity": 0.22
    },
    "prompt_tokens": ["preferred screen", "projection membrane", "theta-zero surface"]
  },
  {
    "id": "entanglement_filaments",
    "concepts": ["entanglement_geometry"],
    "category": "relation",
    "shader_modules": ["entanglement_field", "entanglement_graphs"],
    "scene_roles": ["pair_scene", "relation_map", "portrait_overlay"],
    "preset_params": {
      "threadDensity": 36,
      "threadThickness": 0.018,
      "entanglementWeight": 0.7
    },
    "prompt_tokens": ["entanglement filaments", "correlation threads", "light braids"]
  },
  {
    "id": "rt_soap_film_bridge",
    "concepts": ["entanglement_geometry"],
    "category": "relation_geometry",
    "shader_modules": ["membrane_sdfs", "entanglement_field"],
    "scene_roles": ["bridge", "shared_chamber", "pair_scene"],
    "preset_params": {
      "bridgeThreshold": 0.45,
      "rtTension": 0.72,
      "membraneGlow": 1.28
    },
    "prompt_tokens": ["minimal surface", "soap-film bridge", "entanglement membrane"]
  },
  {
    "id": "entanglement_tear",
    "concepts": ["entanglement_geometry"],
    "category": "failure",
    "shader_modules": ["tear_field", "entanglement_field"],
    "scene_roles": ["break_event", "disconnection", "scene_transition"],
    "preset_params": {
      "cutSeverity": 0.2,
      "tearNoiseScale": 3.2,
      "missingDataDarkness": 0.75
    },
    "prompt_tokens": ["entanglement tear", "ripped light fabric", "severed threads"]
  },
  {
    "id": "horizon_glyphfire",
    "concepts": ["black_holes_and_horizons"],
    "category": "thermal_shell",
    "shader_modules": ["blackhole_sdfs", "horizon_scramble", "photon_ring"],
    "scene_roles": ["black_hole", "thermal_icon", "collapse_scene"],
    "preset_params": {
      "horizonRadius": 0.9,
      "stretchedHorizonWidth": 0.06,
      "thermalScramble": 0.85
    },
    "prompt_tokens": ["horizon glyphfire", "stretched horizon", "thermal membrane"]
  },
  {
    "id": "infall_melt",
    "concepts": ["black_holes_and_horizons"],
    "category": "transition",
    "shader_modules": ["horizon_scramble", "identity_loss"],
    "scene_roles": ["infall_event", "assimilation", "collapse_transition"],
    "preset_params": {
      "identityLossSharpness": 0.8,
      "shellAssimilation": 0.9,
      "redshiftStrength": 0.4
    },
    "prompt_tokens": ["infall melt", "identity thermalization", "shell assimilation"]
  },
  {
    "id": "precursor_ghost",
    "concepts": ["nonlocality_and_smearing"],
    "category": "nonlocal",
    "shader_modules": ["precursor_field", "global_hidden_modes"],
    "scene_roles": ["prophecy_scene", "early_event", "latent_chamber"],
    "preset_params": {
      "precursorWeight": 0.38,
      "earlyKnowledgeGain": 0.55,
      "ghostSpread": 0.7
    },
    "prompt_tokens": ["precursor ghost", "nonlocal aura", "too-early geometry"]
  },
  {
    "id": "evanescent_blur_chamber",
    "concepts": ["nonlocality_and_smearing"],
    "category": "recoverability_limit",
    "shader_modules": ["operator_smear", "coarse_grain_pass"],
    "scene_roles": ["hidden_chamber", "barrier_scene", "probe_mode"],
    "preset_params": {
      "reconstructionSigma": 0.35,
      "recoverableDepth": 0.35,
      "evanescentWeight": 0.7
    },
    "prompt_tokens": ["evanescent blur", "hidden bulk swell", "STM reveal"]
  },
  {
    "id": "responsibility_cloud",
    "concepts": ["bulk_boundary_dictionary", "nonlocality_and_smearing"],
    "category": "debug_ui",
    "shader_modules": ["support_cloud_overlay", "boundary_encoders"],
    "scene_roles": ["debug", "operator_scene", "educational_overlay"],
    "preset_params": {
      "supportRadius": 0.22,
      "responsibilityGain": 1.0,
      "arcGlow": 0.9
    },
    "prompt_tokens": ["responsibility cloud", "support arcs", "boundary ownership map"]
  },
  {
    "id": "renormalized_boundary_fire",
    "concepts": ["holographic_principle", "holographic_renormalization"],
    "category": "boundary_response",
    "shader_modules": ["renorm_field", "post_renorm_debug"],
    "scene_roles": ["boundary_scene", "portrait_edge", "debug_pass"],
    "preset_params": {
      "shellDivergenceGain": 1.3,
      "countertermStrength": 1.0,
      "anomalyMix": 0.16
    },
    "prompt_tokens": ["renormalized boundary fire", "counterterm scars", "finite response"]
  },
  {
    "id": "observer_patch_dome",
    "concepts": ["cosmology_and_holographic_matter"],
    "category": "cosmology",
    "shader_modules": ["cosmology_state", "screen_stack", "observer_patch"],
    "scene_roles": ["observer_cosmos", "finite_universe", "cosmic_portrait"],
    "preset_params": {
      "hubbleRate": 0.85,
      "observerHorizonGain": 1.0,
      "screenAreaBudget": 1.0
    },
    "prompt_tokens": ["observer patch", "de Sitter dome", "finite cosmic horizon"]
  },
  {
    "id": "twin_infinity_walls",
    "concepts": ["cosmology_and_holographic_matter"],
    "category": "global_cosmology",
    "shader_modules": ["global_screens", "screen_stack"],
    "scene_roles": ["cosmic_hall", "temporal_mural", "universe_scene"],
    "preset_params": {
      "pastWallGlow": 0.9,
      "futureWallGlow": 1.1,
      "screenSeparation": 8.0
    },
    "prompt_tokens": ["past infinity wall", "future infinity wall", "global de Sitter screens"]
  },
  {
    "id": "future_horizon_pressure_sea",
    "concepts": ["cosmology_and_holographic_matter"],
    "category": "cosmic_field",
    "shader_modules": ["cosmic_pressure", "future_horizon_forecast"],
    "scene_roles": ["cosmic_background", "dark_energy_field", "ambient_universe"],
    "preset_params": {
      "futureEventHorizon": 3.8,
      "holographicMatterDensity": 0.22,
      "teleologicalMix": 0.7
    },
    "prompt_tokens": ["future-horizon pressure", "dark energy field", "teleological glow"]
  },
  {
    "id": "dark_radiation_residue",
    "concepts": ["cosmology_and_holographic_matter"],
    "category": "primordial_residue",
    "shader_modules": ["cosmic_pressure", "dark_radiation_layer"],
    "scene_roles": ["background_layer", "inflation_scene", "cosmic_memory"],
    "preset_params": {
      "darkRadiationMix": 0.14,
      "primordialGrain": 0.18,
      "epochFade": 0.6
    },
    "prompt_tokens": ["dark radiation residue", "primordial ash", "early-universe afterglow"]
  },
  {
    "id": "low_l_silence",
    "concepts": ["cosmology_and_holographic_matter"],
    "category": "primordial_suppression",
    "shader_modules": ["harmonic_cutoff", "cosmic_pressure"],
    "scene_roles": ["inflation_scene", "cmb_ambient", "macro_void"],
    "preset_params": {
      "inflationCutoff": 0.22,
      "cmbSuppressionBias": 0.5,
      "macroVoidGain": 0.35
    },
    "prompt_tokens": ["missing harmonics", "cosmic silence", "low-l suppression"]
  }
]
```

---

# `src/js/loadTheory.js`

```js id="ndwwgm"
export async function loadJson(path) {
  const res = await fetch(path);
  if (!res.ok) throw new Error(`Failed to load ${path}: ${res.status}`);
  return res.json();
}

export async function loadTheoryAssets() {
  const [concepts, visualMappings] = await Promise.all([
    loadJson("/theory/concepts.json"),
    loadJson("/theory/visual_mappings.json")
  ]);

  const conceptMap = new Map(concepts.map((c) => [c.id, c]));
  const motifMap = new Map(visualMappings.map((m) => [m.id, m]));

  return { concepts, visualMappings, conceptMap, motifMap };
}

export function buildScenePreset(motifIds, assets) {
  const modules = new Set();
  const params = {};
  const promptTokens = new Set();
  const conceptIds = new Set();

  for (const motifId of motifIds) {
    const motif = assets.motifMap.get(motifId);
    if (!motif) continue;

    motif.shader_modules.forEach((m) => modules.add(m));
    motif.concepts.forEach((c) => conceptIds.add(c));
    Object.assign(params, motif.preset_params);
    motif.prompt_tokens.forEach((t) => promptTokens.add(t));
  }

  return {
    motifs: motifIds,
    concepts: [...conceptIds],
    modules: [...modules],
    params,
    promptTokens: [...promptTokens]
  };
}
```

---

# `src/js/presets.js`

```js id="6a2k39"
export const scenePresets = {
  glyphSaint: [
    "surface_glyphshell",
    "renormalized_boundary_fire"
  ],
  adsBasilica: [
    "ads_edge_saturation",
    "preferred_screen_membrane",
    "caustic_cathedral"
  ],
  entropyLovers: [
    "entanglement_filaments",
    "rt_soap_film_bridge"
  ],
  blackSun: [
    "horizon_glyphfire",
    "infall_melt"
  ],
  precursorOracle: [
    "precursor_ghost",
    "responsibility_cloud"
  ],
  deSitterEye: [
    "observer_patch_dome",
    "future_horizon_pressure_sea"
  ],
  futureTide: [
    "future_horizon_pressure_sea",
    "dark_radiation_residue",
    "low_l_silence"
  ]
};
```

---

# `src/js/exampleBuildScene.js`

```js id="6hi7dj"
import { loadTheoryAssets, buildScenePreset } from "./loadTheory.js";
import { scenePresets } from "./presets.js";

export async function buildExampleScene(name = "blackSun") {
  const assets = await loadTheoryAssets();
  const motifIds = scenePresets[name];
  if (!motifIds) throw new Error(`Unknown preset: ${name}`);

  const preset = buildScenePreset(motifIds, assets);

  return {
    name,
    ...preset,
    reconstructionMode: preset.concepts.includes("nonlocality_and_smearing")
      ? "precursor_assisted"
      : "local_smear",
    debugViews: [
      "source",
      "response",
      "reconstruction_confidence"
    ]
  };
}
```

---

# What I’d do next

The next strongest move is to start the first real shader scene templates:

* `src/glsl/scenes/scene_surface_volume.glsl`
* `src/glsl/scenes/scene_rt_surface.glsl`
* `src/glsl/scenes/scene_horizon_hologram.glsl`

I’d start with `scene_horizon_hologram.glsl`, because it pulls together:

* shell SDF
* AdS-ish depth logic
* stretched horizon
* scrambling
* precursor residue
* strong visual payoff fast

Say **next** and I’ll write that file.

Good. Here’s the first real scene template.

This one is built to express the horizon logic we already defined:

* entropy budget tied to horizon area
* true horizon separated from stretched horizon
* shell-local thermal scrambling
* interior readout collapse
* faint Hawking leakage
* nonlocal precursor residue inside the hidden region

That matches the black-hole/horizon logic from the source spine: black-hole entropy scales with horizon area, the stretched horizon is the operational membrane, and local reconstruction degrades in barrier/horizon backgrounds.   

# `src/glsl/scenes/scene_horizon_hologram.glsl`

```glsl id="4pjprw"
// scene_horizon_hologram.glsl
//
// Purpose:
// A flagship holographic black-hole scene template:
// - true horizon vs stretched horizon
// - shell-local glyphfire / thermal scrambling
// - photon-ring accent
// - infalling identity melt
// - precursor residue in hidden region
//
// Assumes a generic raymarch pipeline with uniforms and helper fns.
// Keep this file modular: include from your main fragment shader.

#ifndef PI
#define PI 3.14159265359
#endif

// -----------------------------------------------------------------------------
// Uniforms
// -----------------------------------------------------------------------------

uniform float uTime;
uniform vec2  uResolution;

uniform vec3  uCamPos;
uniform mat3  uCamBasis;

// Core horizon controls
uniform float uHorizonRadius;          // true horizon radius rs
uniform float uStretchedWidth;         // stretched-horizon offset as fraction
uniform float uThermalScramble;        // shell scrambling strength
uniform float uHawkingGlow;            // sparse outgoing emission
uniform float uPhotonRingGain;         // photon-ring accent
uniform float uInteriorReadoutLoss;    // interior readability suppression
uniform float uRedshiftStrength;       // shell spectral compression
uniform float uShellGlyphDensity;      // shell glyph cell frequency
uniform float uPrecursorWeight;        // hidden nonlocal residue
uniform float uBoundaryDepth;          // optional AdS-ish boundary logic mix
uniform float uWarpIntensity;          // optional warp intensity

// Optional infall object controls
uniform vec3  uInfallPos;
uniform float uInfallRadius;
uniform float uInfallEnable;           // 0 or 1

// Optional debug view
uniform int   uDebugMode;
// 0 = beauty
// 1 = true horizon
// 2 = stretched horizon
// 3 = scramble field
// 4 = readout confidence
// 5 = precursor field
// 6 = photon ring

// -----------------------------------------------------------------------------
// Basic helpers
// -----------------------------------------------------------------------------

float saturate(float x) { return clamp(x, 0.0, 1.0); }

float sdSphere(vec3 p, float r) {
    return length(p) - r;
}

float hash21(vec2 p) {
    p = fract(p * vec2(123.34, 456.21));
    p += dot(p, p + 45.32);
    return fract(p.x * p.y);
}

float noise3(vec3 p) {
    vec3 i = floor(p);
    vec3 f = fract(p);
    f = f * f * (3.0 - 2.0 * f);

    float n000 = hash21(i.xy + i.z);
    float n100 = hash21((i.xy + vec2(1.0, 0.0)) + i.z);
    float n010 = hash21((i.xy + vec2(0.0, 1.0)) + i.z);
    float n110 = hash21((i.xy + vec2(1.0, 1.0)) + i.z);

    float n001 = hash21(i.xy + (i.z + 1.0));
    float n101 = hash21((i.xy + vec2(1.0, 0.0)) + (i.z + 1.0));
    float n011 = hash21((i.xy + vec2(0.0, 1.0)) + (i.z + 1.0));
    float n111 = hash21((i.xy + vec2(1.0, 1.0)) + (i.z + 1.0));

    float nx00 = mix(n000, n100, f.x);
    float nx10 = mix(n010, n110, f.x);
    float nx01 = mix(n001, n101, f.x);
    float nx11 = mix(n011, n111, f.x);

    float nxy0 = mix(nx00, nx10, f.y);
    float nxy1 = mix(nx01, nx11, f.y);

    return mix(nxy0, nxy1, f.z);
}

float fbm(vec3 p) {
    float a = 0.5;
    float s = 0.0;
    for (int i = 0; i < 4; i++) {
        s += a * noise3(p);
        p *= 2.03;
        a *= 0.5;
    }
    return s;
}

vec2 sphereUV(vec3 p) {
    vec3 n = normalize(p);
    float u = atan(n.z, n.x) / (2.0 * PI) + 0.5;
    float v = acos(clamp(n.y, -1.0, 1.0)) / PI;
    return vec2(u, v);
}

// -----------------------------------------------------------------------------
// Optional AdS-ish warp for mild holographic depth flavor
// -----------------------------------------------------------------------------

float adsDepth(vec3 p) {
    return max(0.03, p.y + 1.25);
}

vec3 adsWarp(vec3 p) {
    float z = adsDepth(p);
    float s = mix(1.0, 1.0 / z, uWarpIntensity);
    return vec3(p.x * s, p.y, p.z * s);
}

// -----------------------------------------------------------------------------
// Horizon layers
// -----------------------------------------------------------------------------

float trueHorizonField(vec3 p) {
    float d = abs(sdSphere(p, uHorizonRadius));
    return exp(-d * 140.0);
}

float stretchedHorizonField(vec3 p) {
    float rs = uHorizonRadius * (1.0 + uStretchedWidth);
    float d = abs(sdSphere(p, rs));
    return exp(-d * 85.0);
}

float photonRingField(vec3 p) {
    float r = length(p);
    float ringR = 1.5 * uHorizonRadius;
    return exp(-abs(r - ringR) * 22.0) * uPhotonRingGain;
}

float readoutConfidence(vec3 p) {
    float r = length(p);
    float conf = smoothstep(
        uHorizonRadius * 1.15,
        uHorizonRadius * 1.35,
        r
    );
    return mix(conf, conf * conf, uInteriorReadoutLoss);
}

// -----------------------------------------------------------------------------
// Shell glyphfire / thermal scrambling
// -----------------------------------------------------------------------------

float shellGlyphCells(vec3 p) {
    vec2 uv = sphereUV(p);
    vec2 g = uv * vec2(uShellGlyphDensity, uShellGlyphDensity * 0.65);

    vec2 cell = floor(g);
    vec2 f = fract(g) - 0.5;

    float id = hash21(cell);
    float gate = step(0.62, id);

    float lineA = smoothstep(0.12, 0.0, abs(f.x));
    float lineB = smoothstep(0.12, 0.0, abs(f.y));
    float ring  = smoothstep(0.22, 0.18, abs(length(f) - 0.2));

    float glyph = max(max(lineA, lineB * step(0.5, fract(id * 7.0))), ring);
    return gate * glyph;
}

float thermalScrambleField(vec3 p) {
    float sh = stretchedHorizonField(p);
    vec3 q = normalize(p) * 4.0 + vec3(0.0, 0.0, uTime * 0.4);
    float n = fbm(q * 2.1 + vec3(1.3, -0.5, 0.8));
    float cells = shellGlyphCells(p);
    float swirl = 0.5 + 0.5 * sin(18.0 * sphereUV(p).x + 7.0 * sphereUV(p).y + 1.8 * uTime);

    float thermal = mix(n, max(cells, swirl), 0.45);
    return sh * thermal * uThermalScramble;
}

// -----------------------------------------------------------------------------
// Infall object and identity melt
// -----------------------------------------------------------------------------

float infallObjectSDF(vec3 p) {
    return sdSphere(p - uInfallPos, uInfallRadius);
}

float infallIdentityLoss(vec3 p) {
    float rs = uHorizonRadius * (1.0 + uStretchedWidth);
    float d = abs(length(p) - rs);
    float x = 1.0 - clamp(d / max(0.25 * rs, 1e-3), 0.0, 1.0);
    return x * x;
}

// -----------------------------------------------------------------------------
// Precursor residue: hidden, global, low-frequency interior field
// -----------------------------------------------------------------------------

float precursorField(vec3 p) {
    float r = length(p);
    float inside = 1.0 - smoothstep(uHorizonRadius * 0.95, uHorizonRadius * 1.25, r);

    float lowA = 0.5 + 0.5 * sin(1.7 * p.x + 1.1 * p.y - 0.8 * p.z + 0.6 * uTime);
    float lowB = 0.5 + 0.5 * sin(0.9 * r - 0.9 * uTime + 2.0 * atan(p.z, p.x));
    float cloud = fbm(p * 0.9 + vec3(0.0, 0.0, uTime * 0.08));

    return inside * (0.4 * lowA + 0.3 * lowB + 0.3 * cloud) * uPrecursorWeight;
}

// -----------------------------------------------------------------------------
// Redshift-ish spectral shaping
// -----------------------------------------------------------------------------

vec3 applyRedshift(vec3 c, vec3 p) {
    float r = length(p);
    float g = sqrt(max(1e-4, 1.0 - uHorizonRadius / max(r, uHorizonRadius + 1e-4)));
    float mixAmt = (1.0 - g) * uRedshiftStrength;

    vec3 redshifted = vec3(
        c.r + 0.15 * c.g,
        0.72 * c.g,
        0.45 * c.b
    );

    return mix(c, redshifted, clamp(mixAmt, 0.0, 1.0));
}

// -----------------------------------------------------------------------------
// Scene SDF
// -----------------------------------------------------------------------------

struct Hit {
    float d;
    int materialId;
};

Hit sceneSDF(vec3 p) {
    p = adsWarp(p);

    Hit h;
    h.d = 1e6;
    h.materialId = 0;

    float horizonOuter = sdSphere(p, uHorizonRadius * (1.0 + uStretchedWidth));
    if (horizonOuter < h.d) {
        h.d = horizonOuter;
        h.materialId = 1;
    }

    if (uInfallEnable > 0.5) {
        float infall = infallObjectSDF(p);
        if (infall < h.d) {
            h.d = infall;
            h.materialId = 2;
        }
    }

    return h;
}

vec3 estimateNormal(vec3 p) {
    const vec2 e = vec2(0.001, 0.0);
    return normalize(vec3(
        sceneSDF(p + e.xyy).d - sceneSDF(p - e.xyy).d,
        sceneSDF(p + e.yxy).d - sceneSDF(p - e.yxy).d,
        sceneSDF(p + e.yyx).d - sceneSDF(p - e.yyx).d
    ));
}

// -----------------------------------------------------------------------------
// Field evaluation
// -----------------------------------------------------------------------------

struct FieldData {
    float horizonTrue;
    float horizonStretched;
    float photonRing;
    float scramble;
    float precursor;
    float readout;
    float infallLoss;
};

FieldData evalFields(vec3 p) {
    FieldData f;
    f.horizonTrue      = trueHorizonField(p);
    f.horizonStretched = stretchedHorizonField(p);
    f.photonRing       = photonRingField(p);
    f.scramble         = thermalScrambleField(p);
    f.precursor        = precursorField(p);
    f.readout          = readoutConfidence(p);
    f.infallLoss       = (uInfallEnable > 0.5) ? infallIdentityLoss(p) : 0.0;
    return f;
}

// -----------------------------------------------------------------------------
// Emission / shading
// -----------------------------------------------------------------------------

vec3 horizonPalette(float x) {
    vec3 cold = vec3(0.05, 0.16, 0.34);
    vec3 hot  = vec3(0.40, 0.85, 1.35);
    vec3 ash  = vec3(0.95, 0.78, 0.40);
    return mix(cold, hot, smoothstep(0.15, 0.7, x)) + 0.15 * ash * pow(x, 4.0);
}

vec3 accumulateEmission(vec3 p, FieldData f) {
    vec3 col = vec3(0.0);

    // Stretched horizon glyphfire
    vec3 shellCol = horizonPalette(f.scramble);
    shellCol = applyRedshift(shellCol, p);
    col += shellCol * (0.9 * f.horizonStretched + 0.55 * f.scramble);

    // True horizon inner razor line
    col += vec3(0.25, 0.65, 1.2) * f.horizonTrue * 0.4;

    // Photon ring
    col += vec3(0.6, 0.8, 1.1) * f.photonRing * 0.55;

    // Sparse Hawking flecks
    float flick = 0.5 + 0.5 * cos(7.0 * uTime + 20.0 * length(p));
    float sparse = pow(flick, 7.0);
    col += vec3(0.12, 0.22, 0.36) * uHawkingGlow * sparse * f.horizonStretched;

    // Hidden precursor residue inside
    col += vec3(0.18, 0.10, 0.32) * f.precursor * (1.0 - f.readout) * 0.8;

    return col;
}

vec3 shadeSurface(vec3 p, vec3 rd, Hit hit, FieldData f) {
    vec3 n = estimateNormal(p);
    vec3 l = normalize(vec3(0.5, 0.8, -0.7));
    float ndl = 0.5 + 0.5 * dot(n, l);
    float rim = pow(1.0 - max(0.0, dot(n, -rd)), 2.2);

    if (hit.materialId == 1) {
        vec3 base = horizonPalette(f.scramble + 0.2 * rim);
        base = applyRedshift(base, p);

        vec3 surf = base * (0.35 + 0.65 * ndl);
        surf += vec3(0.15, 0.45, 0.95) * rim * 0.6;
        surf += vec3(0.1, 0.2, 0.35) * f.photonRing * 0.4;
        return surf;
    }

    if (hit.materialId == 2) {
        float melt = f.infallLoss;
        vec3 clean = vec3(0.85, 0.78, 0.95) * (0.3 + 0.7 * ndl);
        vec3 melted = mix(
            vec3(0.2, 0.4, 0.9),
            horizonPalette(f.scramble + 0.2),
            0.7
        );
        return mix(clean, melted, melt);
    }

    return vec3(0.0);
}

// -----------------------------------------------------------------------------
// Raymarch
// -----------------------------------------------------------------------------

float stepRule(vec3 p, float sdfD, FieldData f) {
    float base = clamp(0.12 * adsDepth(p), 0.02, 0.25);

    float shellPenalty = mix(1.0, 0.35, saturate(f.horizonStretched + f.scramble));
    float hiddenPenalty = mix(1.0, 0.6, 1.0 - f.readout);

    return max(0.002, sdfD * base * shellPenalty * hiddenPenalty);
}

vec3 background(vec3 rd) {
    float v = 0.5 + 0.5 * rd.y;
    vec3 bg = mix(vec3(0.01, 0.015, 0.03), vec3(0.03, 0.05, 0.08), v);
    float stars = pow(hash21(floor(rd.xy * 140.0)), 48.0);
    return bg + 0.2 * stars;
}

vec3 debugColor(FieldData f) {
    if (uDebugMode == 1) return vec3(f.horizonTrue);
    if (uDebugMode == 2) return vec3(f.horizonStretched);
    if (uDebugMode == 3) return vec3(f.scramble);
    if (uDebugMode == 4) return vec3(f.readout);
    if (uDebugMode == 5) return vec3(f.precursor);
    if (uDebugMode == 6) return vec3(f.photonRing);
    return vec3(0.0);
}

vec3 render(vec3 ro, vec3 rd) {
    float t = 0.0;
    vec3 color = vec3(0.0);

    for (int i = 0; i < 160; i++) {
        vec3 p = ro + rd * t;
        Hit h = sceneSDF(p);
        FieldData f = evalFields(p);

        if (uDebugMode != 0) {
            color += 0.045 * debugColor(f);
        } else {
            color += 0.04 * accumulateEmission(p, f);
        }

        if (h.d < 0.0015) {
            if (uDebugMode == 0) {
                color += shadeSurface(p, rd, h, f);
            }
            break;
        }

        t += stepRule(p, h.d, f);
        if (t > 16.0) {
            color += background(rd);
            break;
        }
    }

    return color;
}

// -----------------------------------------------------------------------------
// Entry
// -----------------------------------------------------------------------------

vec3 getRayDir(vec2 fragCoord) {
    vec2 uv = (2.0 * fragCoord - uResolution) / min(uResolution.x, uResolution.y);
    return normalize(uCamBasis * normalize(vec3(uv, 1.8)));
}

void mainImage(out vec4 fragColor, in vec2 fragCoord) {
    vec3 ro = uCamPos;
    vec3 rd = getRayDir(fragCoord);

    vec3 col = render(ro, rd);

    // Simple filmic-ish tone + gamma
    col = col / (1.0 + col);
    col = pow(col, vec3(0.4545));

    fragColor = vec4(col, 1.0);
}
```

---

# Minimal JS hookup

## `src/js/scene_horizon_hologram.js`

```js id="um2jtn"
export function getHorizonHologramUniforms(time, resolution) {
  return {
    uTime: time,
    uResolution: resolution,

    uCamPos: [0.0, 0.2, -4.2],
    uCamBasis: [
      1, 0, 0,
      0, 1, 0,
      0, 0, 1
    ],

    uHorizonRadius: 0.95,
    uStretchedWidth: 0.065,
    uThermalScramble: 0.95,
    uHawkingGlow: 0.22,
    uPhotonRingGain: 0.55,
    uInteriorReadoutLoss: 0.9,
    uRedshiftStrength: 0.6,
    uShellGlyphDensity: 22.0,
    uPrecursorWeight: 0.28,
    uBoundaryDepth: 0.03,
    uWarpIntensity: 0.15,

    uInfallPos: [1.25 - 0.2 * time, 0.15, 0.0],
    uInfallRadius: 0.26,
    uInfallEnable: 1.0,

    uDebugMode: 0
  };
}
```

---

# How to read this scene

## What is physically translated here

The shell is doing the real work:

* the **true horizon** is the logic boundary
* the **stretched horizon** is the visible membrane
* shell-local texture becomes **thermal scrambling / glyphfire**
* the inside is not “empty art space,” it has only **low-confidence nonlocal residue**

That matches the black-hole complementarity / stretched-horizon style picture much better than “sphere with glow.”  

## What is artistically faked on purpose

This is **not** a GR-accurate lensing solver.
The photon ring and redshift are stylized approximations to get the right render behavior fast.

## What you should tune first

Start with:

* `uHorizonRadius`
* `uStretchedWidth`
* `uThermalScramble`
* `uShellGlyphDensity`
* `uPrecursorWeight`
* `uPhotonRingGain`

Those six parameters basically set the whole vibe.

---

# Good next variants

## Variant A — “Black Sun”

Turn off infall, push shell glyph density up, lower Hawking glow, stronger photon ring.

## Variant B — “Sacrifice”

Keep infall enabled, animate `uInfallPos` across the shell, raise `uThermalScramble`, lower `uPrecursorWeight`.

## Variant C — “Interior Oracle”

Lower shell brightness, increase precursor, lower readout confidence, make interior feel like hidden nonlocal thought.

---

# Best next file

The cleanest follow-up is:

`src/glsl/scenes/scene_rt_surface.glsl`

because that gives you the entanglement counterpart to this horizon scene:

* two anchors
* membrane bridge
* connected/disconnected phase
* tear logic
* wedge glow

Say **next** and I’ll write that one.
Perfect. Here’s the entanglement counterpart.

This scene is built around the static RT logic: a pair of boundary anchors, a membrane-like bridge that appears when shared information crosses a threshold, and a tear field that takes over when the connected phase loses. In the source spine, entanglement entropy in static holographic settings is computed by a bulk minimal surface, and mutual information controls when connected vs disconnected configurations are favored.  

# `src/glsl/scenes/scene_rt_surface.glsl`

```glsl id="tns7sv"
// scene_rt_surface.glsl
//
// Static holographic entanglement scene template:
// - two boundary/source anchors
// - wedge fields
// - connected/disconnected bridge phase
// - RT-style membrane bridge
// - tear logic when relation weakens
//
// Stylized, not a literal extremal-surface solver.

#ifndef PI
#define PI 3.14159265359
#endif

// -----------------------------------------------------------------------------
// Uniforms
// -----------------------------------------------------------------------------

uniform float uTime;
uniform vec2  uResolution;

uniform vec3  uCamPos;
uniform mat3  uCamBasis;

// Anchor regions
uniform vec3  uAnchorA;
uniform vec3  uAnchorB;
uniform float uAnchorRadius;

// Entanglement / RT controls
uniform float uEntanglementWeight;   // 0..1-ish
uniform float uBridgeThreshold;      // transition threshold
uniform float uRTTension;            // bridge thinning / saddle tension
uniform float uSharedInteriorWeight; // wedge overlap glow
uniform float uDisconnectSharpness;  // how sharply bridge fails
uniform float uMembraneGlow;         // membrane brightness
uniform float uThreadGain;           // filament brightness
uniform float uCutSeverity;          // tear severity

// Atmosphere
uniform float uBoundaryDepth;
uniform float uWarpIntensity;
uniform float uScreenBloom;

// Debug
uniform int uDebugMode;
// 0 beauty
// 1 wedge A
// 2 wedge B
// 3 overlap
// 4 bridge phase
// 5 tear field
// 6 filament field

// -----------------------------------------------------------------------------
// Helpers
// -----------------------------------------------------------------------------

float saturate(float x) { return clamp(x, 0.0, 1.0); }

float sdSphere(vec3 p, float r) {
    return length(p) - r;
}

float smin(float a, float b, float k) {
    float h = saturate(0.5 + 0.5 * (b - a) / k);
    return mix(b, a, h) - k * h * (1.0 - h);
}

float hash21(vec2 p) {
    p = fract(p * vec2(123.34, 456.21));
    p += dot(p, p + 45.32);
    return fract(p.x * p.y);
}

float noise3(vec3 p) {
    vec3 i = floor(p);
    vec3 f = fract(p);
    f = f * f * (3.0 - 2.0 * f);

    float n000 = hash21(i.xy + i.z);
    float n100 = hash21((i.xy + vec2(1.0, 0.0)) + i.z);
    float n010 = hash21((i.xy + vec2(0.0, 1.0)) + i.z);
    float n110 = hash21((i.xy + vec2(1.0, 1.0)) + i.z);

    float n001 = hash21(i.xy + (i.z + 1.0));
    float n101 = hash21((i.xy + vec2(1.0, 0.0)) + (i.z + 1.0));
    float n011 = hash21((i.xy + vec2(0.0, 1.0)) + (i.z + 1.0));
    float n111 = hash21((i.xy + vec2(1.0, 1.0)) + (i.z + 1.0));

    float nx00 = mix(n000, n100, f.x);
    float nx10 = mix(n010, n110, f.x);
    float nx01 = mix(n001, n101, f.x);
    float nx11 = mix(n011, n111, f.x);

    float nxy0 = mix(nx00, nx10, f.y);
    float nxy1 = mix(nx01, nx11, f.y);

    return mix(nxy0, nxy1, f.z);
}

float fbm(vec3 p) {
    float a = 0.5;
    float s = 0.0;
    for (int i = 0; i < 4; i++) {
        s += a * noise3(p);
        p *= 2.02;
        a *= 0.5;
    }
    return s;
}

// -----------------------------------------------------------------------------
// Mild AdS-ish warp
// -----------------------------------------------------------------------------

float adsDepth(vec3 p) {
    return max(0.03, p.y + 1.4);
}

vec3 adsWarp(vec3 p) {
    float z = adsDepth(p);
    float s = mix(1.0, 1.0 / z, uWarpIntensity);
    return vec3(p.x * s, p.y, p.z * s);
}

// -----------------------------------------------------------------------------
// Wedge fields
// -----------------------------------------------------------------------------

float wedgeField(vec3 p, vec3 anchor, vec3 inwardDir, float depth, float spread) {
    vec3 q = p - anchor;
    float axial = dot(q, inwardDir);
    float lateral = length(q - axial * inwardDir);

    float core = exp(-abs(axial - 0.55 * depth) * 1.4);
    float fan  = exp(-lateral * spread);

    return fan * core * step(0.0, axial) * step(axial, depth);
}

vec3 inwardDirForAnchor(vec3 a) {
    // inward toward scene center, with slight upward bias
    vec3 dir = normalize(-a + vec3(0.0, 0.35, 0.0));
    return dir;
}

// -----------------------------------------------------------------------------
// Bridge phase
// -----------------------------------------------------------------------------

float bridgeStrength() {
    float x = (uEntanglementWeight - uBridgeThreshold) / max(0.001, uDisconnectSharpness);
    return 1.0 / (1.0 + exp(-x));
}

// -----------------------------------------------------------------------------
// Membrane bridge
// -----------------------------------------------------------------------------

float membraneBridgeSDF(vec3 p, vec3 a, vec3 b, float phase, float tension) {
    vec3 ab = b - a;
    float len = max(length(ab), 1e-4);
    vec3 dir = ab / len;

    vec3 mid = 0.5 * (a + b);
    vec3 rel = p - mid;

    float axial = dot(rel, dir);
    vec3 perp = rel - axial * dir;
    float lateral = length(perp);

    float spanMask = smoothstep(0.65 * len, 0.35 * len, abs(axial));

    // saddle / soap-film-ish thickness
    float baseThickness = mix(0.18, 0.035, tension);
    float pinch = 1.0 - 0.55 * cos(PI * axial / len);
    float thickness = mix(baseThickness, baseThickness * 0.55, phase) * pinch;

    // slight membrane undulation
    float und = 0.015 * sin(8.0 * axial / len + 1.5 * uTime);
    float d = lateral - (thickness + und);

    // gate by phase and span
    d += (1.0 - phase) * 0.5;
    d += (1.0 - spanMask) * 0.15;

    return d;
}

// -----------------------------------------------------------------------------
// Filaments
// -----------------------------------------------------------------------------

float filamentField(vec3 p, vec3 a, vec3 b, float phase) {
    vec3 ab = b - a;
    float len = max(length(ab), 1e-4);
    vec3 dir = normalize(ab);

    vec3 rel = p - a;
    float axial = clamp(dot(rel, dir), 0.0, len);
    vec3 proj = a + dir * axial;

    float d = length(p - proj);

    float strands = 0.5 + 0.5 * sin(18.0 * axial / len + 12.0 * atan(p.z - proj.z, p.y - proj.y));
    float envelope = exp(-d * 18.0);

    return phase * envelope * strands;
}

// -----------------------------------------------------------------------------
// Tear field
// -----------------------------------------------------------------------------

float tearField(vec3 p, vec3 a, vec3 b, float phase) {
    vec3 mid = 0.5 * (a + b);
    vec3 ab = normalize(b - a);
    vec3 rel = p - mid;

    float axial = dot(rel, ab);
    float side = length(rel - axial * ab);

    float n = fbm(p * 2.6 + vec3(0.0, 0.0, uTime * 0.1));
    float instability = (1.0 - phase) * uCutSeverity;

    float seam = exp(-side * 10.0) * smoothstep(0.7, 0.2, abs(axial));
    return seam * smoothstep(0.45 - instability, 0.55 + instability, n);
}

// -----------------------------------------------------------------------------
// Source anchors / shell-ish nodes
// -----------------------------------------------------------------------------

float anchorGlow(vec3 p, vec3 anchor) {
    float d = abs(sdSphere(p - anchor, uAnchorRadius));
    return exp(-d * 35.0);
}

float anchorGlyph(vec3 p, vec3 anchor) {
    vec3 q = normalize(p - anchor);
    float u = atan(q.z, q.x) / (2.0 * PI) + 0.5;
    float v = acos(clamp(q.y, -1.0, 1.0)) / PI;

    vec2 g = vec2(u, v) * vec2(16.0, 10.0);
    vec2 f = fract(g) - 0.5;
    vec2 cell = floor(g);

    float id = hash21(cell);
    float lineA = smoothstep(0.14, 0.0, abs(f.x));
    float lineB = smoothstep(0.14, 0.0, abs(f.y));
    float ring  = smoothstep(0.25, 0.2, abs(length(f) - 0.23));

    float glyph = max(lineA * step(0.4, id), max(lineB * step(0.7, fract(id * 9.0)), ring));
    return anchorGlow(p, anchor) * glyph;
}

// -----------------------------------------------------------------------------
// Scene SDF
// -----------------------------------------------------------------------------

struct Hit {
    float d;
    int materialId;
};

Hit sceneSDF(vec3 p) {
    p = adsWarp(p);

    Hit h;
    h.d = 1e6;
    h.materialId = 0;

    float a = sdSphere(p - uAnchorA, uAnchorRadius);
    float b = sdSphere(p - uAnchorB, uAnchorRadius);

    float anchors = smin(a, b, 0.18);
    if (anchors < h.d) {
        h.d = anchors;
        h.materialId = 1;
    }

    float phase = bridgeStrength();
    float bridge = membraneBridgeSDF(p, uAnchorA, uAnchorB, phase, uRTTension);
    if (bridge < h.d) {
        h.d = bridge;
        h.materialId = 2;
    }

    return h;
}

vec3 estimateNormal(vec3 p) {
    const vec2 e = vec2(0.001, 0.0);
    return normalize(vec3(
        sceneSDF(p + e.xyy).d - sceneSDF(p - e.xyy).d,
        sceneSDF(p + e.yxy).d - sceneSDF(p - e.yxy).d,
        sceneSDF(p + e.yyx).d - sceneSDF(p - e.yyx).d
    ));
}

// -----------------------------------------------------------------------------
// Field eval
// -----------------------------------------------------------------------------

struct FieldData {
    float wedgeA;
    float wedgeB;
    float overlap;
    float phase;
    float tear;
    float filaments;
    float anchorA;
    float anchorB;
};

FieldData evalFields(vec3 p) {
    vec3 pa = adsWarp(p);

    vec3 dirA = inwardDirForAnchor(uAnchorA);
    vec3 dirB = inwardDirForAnchor(uAnchorB);

    FieldData f;
    f.wedgeA = wedgeField(pa, uAnchorA, dirA, 2.6, 2.4);
    f.wedgeB = wedgeField(pa, uAnchorB, dirB, 2.6, 2.4);
    f.overlap = f.wedgeA * f.wedgeB * uSharedInteriorWeight;
    f.phase = bridgeStrength();
    f.tear = tearField(pa, uAnchorA, uAnchorB, f.phase);
    f.filaments = filamentField(pa, uAnchorA, uAnchorB, f.phase) * uThreadGain;
    f.anchorA = anchorGlyph(pa, uAnchorA);
    f.anchorB = anchorGlyph(pa, uAnchorB);
    return f;
}

// -----------------------------------------------------------------------------
// Shading
// -----------------------------------------------------------------------------

vec3 anchorPalette(float x) {
    vec3 a = vec3(0.16, 0.55, 1.1);
    vec3 b = vec3(0.72, 0.28, 1.25);
    vec3 c = vec3(1.0, 0.82, 0.45);
    return mix(a, b, smoothstep(0.2, 0.75, x)) + 0.12 * c * pow(x, 5.0);
}

vec3 membranePalette(float x) {
    vec3 cold = vec3(0.10, 0.35, 0.9);
    vec3 hot  = vec3(0.85, 0.35, 1.15);
    return mix(cold, hot, smoothstep(0.15, 0.85, x));
}

vec3 accumulateEmission(vec3 p, FieldData f) {
    vec3 col = vec3(0.0);

    col += anchorPalette(f.anchorA) * (0.5 * f.anchorA);
    col += anchorPalette(f.anchorB) * (0.5 * f.anchorB);

    col += vec3(0.22, 0.45, 0.95) * (f.wedgeA * 0.06 + f.wedgeB * 0.06);
    col += vec3(0.42, 0.18, 0.82) * f.overlap * 0.22;

    col += vec3(0.55, 0.85, 1.1) * f.filaments * 0.15;

    // Tear darkens / subtracts coherent bridge light
    col -= vec3(0.16, 0.08, 0.22) * f.tear * 0.22;

    return max(col, 0.0);
}

vec3 shadeSurface(vec3 p, vec3 rd, Hit hit, FieldData f) {
    vec3 n = estimateNormal(p);
    vec3 l = normalize(vec3(0.4, 0.8, -0.6));
    float ndl = 0.5 + 0.5 * dot(n, l);
    float rim = pow(1.0 - max(0.0, dot(n, -rd)), 2.5);

    if (hit.materialId == 1) {
        float g = max(f.anchorA, f.anchorB);
        vec3 c = anchorPalette(g);
        c *= (0.35 + 0.65 * ndl);
        c += vec3(0.2, 0.5, 1.1) * rim * 0.5;
        return c;
    }

    if (hit.materialId == 2) {
        float m = f.phase * (1.0 - 0.75 * f.tear);
        vec3 c = membranePalette(m);
        c *= (0.3 + 0.7 * ndl);
        c += vec3(0.9, 0.95, 1.0) * rim * 0.25 * uMembraneGlow;
        c += vec3(0.5, 0.15, 0.75) * f.overlap * 0.3;
        return c;
    }

    return vec3(0.0);
}

// -----------------------------------------------------------------------------
// Raymarch
// -----------------------------------------------------------------------------

float stepRule(vec3 p, float sdfD, FieldData f) {
    float zScale = clamp(0.16 * adsDepth(p), 0.02, 0.25);
    float membranePenalty = mix(1.0, 0.4, f.phase * (0.4 + 0.6 * f.overlap));
    return max(0.002, sdfD * zScale * membranePenalty);
}

vec3 background(vec3 rd) {
    float v = 0.5 + 0.5 * rd.y;
    vec3 bg = mix(vec3(0.012, 0.015, 0.028), vec3(0.05, 0.04, 0.08), v);
    float stars = pow(hash21(floor(rd.xy * 180.0)), 64.0);
    return bg + 0.1 * stars;
}

vec3 debugColor(FieldData f) {
    if (uDebugMode == 1) return vec3(f.wedgeA);
    if (uDebugMode == 2) return vec3(f.wedgeB);
    if (uDebugMode == 3) return vec3(f.overlap);
    if (uDebugMode == 4) return vec3(f.phase);
    if (uDebugMode == 5) return vec3(f.tear);
    if (uDebugMode == 6) return vec3(f.filaments);
    return vec3(0.0);
}

vec3 render(vec3 ro, vec3 rd) {
    float t = 0.0;
    vec3 color = vec3(0.0);

    for (int i = 0; i < 170; i++) {
        vec3 p = ro + rd * t;
        Hit h = sceneSDF(p);
        FieldData f = evalFields(p);

        if (uDebugMode != 0) {
            color += 0.045 * debugColor(f);
        } else {
            color += 0.035 * accumulateEmission(p, f);
        }

        if (h.d < 0.0015) {
            if (uDebugMode == 0) {
                color += shadeSurface(p, rd, h, f);
            }
            break;
        }

        t += stepRule(p, h.d, f);
        if (t > 18.0) {
            color += background(rd);
            break;
        }
    }

    return color;
}

// -----------------------------------------------------------------------------
// Entry
// -----------------------------------------------------------------------------

vec3 getRayDir(vec2 fragCoord) {
    vec2 uv = (2.0 * fragCoord - uResolution) / min(uResolution.x, uResolution.y);
    return normalize(uCamBasis * normalize(vec3(uv, 1.9)));
}

void mainImage(out vec4 fragColor, in vec2 fragCoord) {
    vec3 ro = uCamPos;
    vec3 rd = getRayDir(fragCoord);

    vec3 col = render(ro, rd);

    // soft bloom-ish lift
    col += uScreenBloom * 0.03 * col * col;

    // tonemap / gamma
    col = col / (1.0 + col);
    col = pow(col, vec3(0.4545));

    fragColor = vec4(col, 1.0);
}
```

---

# Minimal JS hookup

## `src/js/scene_rt_surface.js`

```js id="p5abpo"
export function getRTSurfaceUniforms(time, resolution) {
  return {
    uTime: time,
    uResolution: resolution,

    uCamPos: [0.0, 0.15, -4.4],
    uCamBasis: [
      1, 0, 0,
      0, 1, 0,
      0, 0, 1
    ],

    uAnchorA: [-1.25, 0.0, 0.0],
    uAnchorB: [ 1.25, 0.0, 0.0],
    uAnchorRadius: 0.34,

    uEntanglementWeight: 0.78,
    uBridgeThreshold: 0.44,
    uRTTension: 0.72,
    uSharedInteriorWeight: 0.72,
    uDisconnectSharpness: 0.16,
    uMembraneGlow: 1.3,
    uThreadGain: 0.9,
    uCutSeverity: 0.18,

    uBoundaryDepth: 0.03,
    uWarpIntensity: 0.12,
    uScreenBloom: 0.9,

    uDebugMode: 0
  };
}
```

---

# Best things to animate

## 1. Phase transition

Animate `uEntanglementWeight` across `uBridgeThreshold`.

That gives you the signature holographic switch:

* below threshold: two separate anchor regions
* near threshold: unstable bridge
* above threshold: shared membrane chamber

## 2. Tear event

Raise `uCutSeverity` while dropping `uEntanglementWeight`.

That makes the bridge feel like it is **structurally failing**, not just fading.

## 3. Shared interior swell

Increase `uSharedInteriorWeight` after connection.

That makes the bulk between anchors glow like a jointly owned wedge.

---

# Debug views worth using

* `1` wedge A
* `2` wedge B
* `3` overlap
* `4` bridge phase
* `5` tear field
* `6` filaments

Those are actually useful here, because this scene can otherwise look “pretty” even when the relation logic is wrong.

---

# What this scene is doing conceptually

This is not a literal minimal-surface solver.
It is a **minimal-surface-inspired membrane phase system**:

* anchors behave like boundary regions
* the bridge appears only when relation weight is strong enough
* overlap glows like shared interior ownership
* failure manifests as a tear, not a mere opacity fade

That is the right fake for RT art code.

---

# Best next file

The strongest third scene template is:

`src/glsl/scenes/scene_light_sheet_screen.glsl`

because then you’ll have the three major pillars:

* horizon membrane
* entanglement membrane
* null-projection screen stack

Say **next** and I’ll write that one.

